---
id: F-0003
title: Rebrand Kredy — copy de préstamos/crédito (sacar branding del tracker)
target: kredy
ui: true
acceptance:
  - app/layout.tsx refleja Kredy = préstamos/crédito. Title y openGraph.title NO contienen "cashflow", "tarjetas" ni "gastos"; siteName = "Kredy"; description habla de préstamos/crédito/cuotas
  - metadataBase apunta a la URL de prod de Kredy (https://kredy-ap.vercel.app) — ver nota sobre el rename de Vercel (checklist C)
  - No quedan strings user-facing "Spensiv" / "cashflow" / "tu motor de cashflow" en vistas de prestatario o AP (app/ap, app/dashboard/ap, app/share, app/simular, app/l). Reemplazadas por copy de Kredy
  - La TNA / tasa / TEA NUNCA aparece en vistas de prestatario (regresión — no introducir ninguna)
  - Si hay favicon/manifest/icon (app/icon.*, app/favicon.ico, app/manifest.ts) y referencia "Spensiv", se actualiza a Kredy
  - Typecheck y lint pasan sin errores

## Contexto

Kredy (préstamos) vive todavía en el repo originalmente llamado "spensiv" (carpeta local `spensiv/`,
target del orquestador = `kredy`). Heredó el branding del TRACKER de finanzas personales, que NO le
corresponde: "Spensiv - Tu motor de cashflow personal", "tarjetas de crédito", "gastos". Ese branding
es del otro producto (target `spensiv` = el tracker). Kredy necesita identidad de préstamos/crédito.

Estado verificado (jun 2026):
- `app/layout.tsx`: `title: 'Spensiv - Tu motor de cashflow personal'`, `description` sobre tarjetas/gastos,
  `openGraph.siteName: 'Spensiv'`, `metadataBase: https://spensiv.vercel.app`, `themeColor: '#09090b'` (neutro, OK).
- `AP_APP_NAME = 'Portal AP'` en `app/ap/page.tsx` (línea 26) — neutral, OK; verificar que el header AP no diga "Spensiv".
- Strings "Spensiv"/"cashflow" también aparecen en: `app/ap/page.tsx`, `app/dashboard/ap/page.tsx`,
  `app/share/[personId]/page.tsx`, `app/dashboard/simulator/page.tsx`, `app/api/cron/ap-reconcile/route.ts`.

## Pasos sugeridos (el planner puede refinar)

1. Reescribir el bloque `metadata`/`viewport` de `app/layout.tsx`: title y openGraph con copy de Kredy
   (préstamos/crédito/cuotas), `siteName: 'Kredy'`, `metadataBase: new URL('https://kredy-ap.vercel.app')`.
2. Grep en `app/` por `Spensiv`, `cashflow`, `tu motor de`, `tarjetas`, `gastos` y reemplazar el copy
   user-facing por lenguaje de Kredy/préstamos. NO tocar lógica, solo strings de UI/metadata.
3. Revisar el header del Portal AP y vistas de prestatario (`/l`, `/simular`, `/share`) para que el nombre
   visible sea Kredy donde corresponda.
4. Si existe favicon/manifest/app icon con marca vieja, actualizar.
5. Typecheck + lint.

## Fuera de alcance

- Cambios de schema / migraciones.
- Deploy a Vercel y el rename del project Vercel (checklist C — gate humano; Augusto lo hace en el dashboard).
- Tocar archivos de mutuo/pagaré.
- Mostrar TNA/tasa en cualquier vista de prestatario.

## Nota Vercel (checklist C)

Augusto renombró el project Vercel a `kredy-ap` (el subdominio `kredy.vercel.app` ya estaba tomado por otro
team; los `.vercel.app` son globales). La URL de prod es `https://kredy-ap.vercel.app` y por eso `metadataBase`
apunta ahí. El nombre comercial sigue siendo "Kredy"; `-ap` es solo limitación técnica de subdominio.
Augusto reenvía los links `/l` con el dominio nuevo.
