# Investigación Scout — F-0025

> Generado automáticamente. Evidencia verificada contra filesystem.

## Mapa del repo para: Argos mobile — Paridad con mockup: sin título de página, header compacto, gradiente visible

**Resumen:** El repo es una app financiera React + Vite + Tailwind (dark mode fijo) con layout responsivo que separa header desktop (Topbar) de header mobile (MobileHeader) y bottom nav (MobileNav). Actualmente MobileHeader NO tiene gradiente visible — usa fondo semitransparente con blur. El PageHeader se está removiendo progresivamente (ej: FundingEngine ya no lo usa). Para lograr paridad con mockup (sin título de página, header compacto, gradiente visible) hay que: (1) asegurar que ninguna ruta mobile use PageHeader con título visible, (2) modificar MobileHeader para que sea más compacto si es necesario y (3) agregar un gradiente CSS (no existe actualmente en el código) probablemente como background del header o como overlay decorativo.

### Archivos clave
- `src/components/common/MobileHeader.jsx`
- `src/components/common/MobileNav.jsx`
- `src/components/layout/AppLayout.jsx`
- `src/components/ui/PageHeader.jsx`
- `src/index.css`
- `tailwind.config.js`
- `src/pages/FundingEngine.jsx`
- `src/pages/Dashboard.jsx`
- `src/contexts/ThemeContext.jsx`
- `index.html`

### Patrones a seguir
- Mobile-first responsive: `hidden md:flex` para desktop-only, `md:hidden` para mobile-only
- CSS custom properties (var(--...)) para todos los colores/tokens definidos en :root y .dark en index.css
- Safe area insets: `env(safe-area-inset-top)` y `env(safe-area-inset-bottom)` en headers y navs
- Componentes de layout separados por responsividad: Topbar (desktop) + MobileHeader (mobile) + MobileNav (mobile bottom)
- Uso de `min-h-[44px]` y `min-w-[44px]` para targets táctiles accesibles en mobile
- Tailwind utility classes con custom tokens: bg-argos-bg, text-ink, border-argos-border
- backdropFilter con blur para semitransparencia en MobileHeader
- Lucide React icons (Eye, EyeOff, Settings, LayoutDashboard, etc.)

### Dependencias relevantes
- react@^18.2.0
- react-router-dom@^6.20.0
- lucide-react@^0.263.1
- tailwindcss@^3.3.2
- @tanstack/react-query@^5.90.19
- @supabase/supabase-js@^2.39.0

### Riesgos / Restricciones
- [NO VERIFICADO] No se encontró ningún `gradient`, `bg-gradient-to-*`, `linear-gradient` ni `radial-gradient` en el código actual (ni en CSS ni en JSX/JS). Si el mockup requiere un gradiente visible, habrá que agregarlo desde cero.
- El MobileHeader actual usa fondo sólido `rgba(10,10,14,0.96)` con backdropFilter blur, no gradiente. Cualquier cambio cromático rompe el contraste con los íconos e iniciales.
- El offset actual del body en mobile (`mobile-body-offset` en index.css) es `5rem` (80px), que corresponde a la suma de h-11 (44px) + h-9 (36px). Si se modifica la altura del header compacto, hay que recalcular este offset.
- MobileHeader se renderiza SIEMPRE en AppLayout (no es lazy ni condicional), así que cualquier cambio en su estructura impacta TODAS las rutas mobile.
- PageHeader se usa en múltiples páginas (Dashboard.jsx lo importa). La migración a 'sin título de página' requiere revisar cada página que use `<PageHeader>` y reemplazarlo por un header inline compacto (como ya se hizo en FundingEngine.jsx).

### Evidencia verificada
- **`src/components/layout/AppLayout.jsx`** — `AppLayout` (líneas 1-45, confianza 100%): Layout raíz que renderiza Topbar, MobileHeader, Sidebar, MobileNav. Es el orquestador principal: MobileHeader y MobileNav se renderizan siempre para todas las rutas mobile.
- **`src/components/common/MobileHeader.jsx`** — `MobileHeader` (líneas 10-89, confianza 100%): Header mobile actual: dos filas (avatar/logo/acción + selector portfolio/toggle ARS/USD). Alturas fijas h-11 + h-9. No tiene gradiente. Usa background semitransparente con backdropFilter blur.
- **`src/components/common/MobileNav.jsx`** — `MobileNav` (líneas 1-120, confianza 100%): Barra de navegación inferior mobile con 4 ítems fijos + botón 'Más' que abre BottomSheet. Padding bottom usa env(safe-area-inset-bottom).
- **`src/components/ui/PageHeader.jsx`** — `PageHeader` (líneas 1-17, confianza 100%): Componente de encabezado de página con title/subtitle/actions. La tarea pide 'sin título de página', lo que implica NO usar este componente (o adaptarlo para ocultar el título).
- **`src/index.css`** — `mobile-body-offset` (líneas 127-129, confianza 100%): Clase CSS que define el margen superior del body en mobile para compensar la altura del header (calc(env(safe-area-inset-top) + 5rem)). Crítico si se cambia la altura del header compacto.
- **`src/index.css`** — `--bg` (líneas 5-20, confianza 100%): Variables CSS de diseño: --bg (#0A0A0E), --surface, --teal, etc. Donde se deberían agregar variables de gradiente si el mockup las requiere.
- **`tailwind.config.js`** — `argos-bg` (líneas 39-50, confianza 100%): Definición de colores personalizados de Tailwind. No incluye gradientes. Si se necesitan bg-gradient-*, deben agregarse aquí o via clases utilitarias de Tailwind.
- **`src/pages/FundingEngine.jsx`** — `FundingEngine` (líneas 1-20, confianza 100%): Ejemplo de página donde ya se removió PageHeader: el comentario '// PageHeader removido — header compacto inline' es el patrón a seguir para otras rutas.
- **`src/contexts/ThemeContext.jsx`** — `ThemeProvider` (líneas 5-22, confianza 100%): Provider de tema. Siempre fuerza clase 'dark' en <html>. Relevante porque cualquier gradiente debería funcionar tanto en dark mode como en posibles temas futuros.
- **`index.html`** — `apple-mobile-web-app-status-bar-style` (líneas 8-9, confianza 100%): Meta tags PWA: viewport-fit=cover, apple-mobile-web-app-capable=yes, theme-color=#0A0A0E. El header compacto debe integrarse con el notch y la status bar.

## Investigar qué existe para implementar: Argos mobile — Paridad con mockup: sin título de página, header compacto, gradiente visible

**Resumen:** La gran mayoría de los cambios definidos en SPEC-mobile-overview-redesign.md (M1-M8) ya están implementados: el hero chart con gradiente visible y modo minimal (renderChart con parámetro minimal), el header compacto con ojo+settings (MobileHeader.jsx), la bottom nav con pill activo (MobileNav.jsx), el donut con contador central (AllocationPanel.jsx), las posiciones destacadas con MiniSpark (MobilePositionsList variant='featured'), y el reorden del overview mobile (DashboardOverview.jsx). El único cambio pendiente y crítico para lograr 'sin título de página' es ocultar el `<PageHeader title="Overview" />` en mobile dentro de Dashboard.jsx (línea 356), envolviéndolo en `className="hidden md:block"` — actualmente se renderiza en todos los viewports. La tarea es principalmente una verificación de que todo lo especificado ya esté correctamente cableado y un cambio puntual de visibilidad del PageHeader.

### Archivos clave
- `src/pages/Dashboard.jsx`
- `src/components/ui/PageHeader.jsx`
- `src/components/common/MobileHeader.jsx`
- `src/components/common/MobileNav.jsx`
- `src/features/portfolio/components/PortfolioHeroChart.jsx`
- `src/features/portfolio/components/DashboardOverview.jsx`
- `src/features/portfolio/components/AllocationPanel.jsx`
- `src/features/portfolio/components/MobilePositionsList.jsx`
- `src/components/ui/Sparkline.jsx`
- `src/hooks/useBalanceVisibility.js`
- `src/components/ui/MoneyValue.jsx`

### Patrones a seguir
- `className="md:hidden"` para aislar layout mobile (todo lo mobile dentro de bloques `md:hidden`, desktop en `hidden md:block`)
- `renderChart(gradId, { minimal })` con parámetro `minimal` para alternar entre chart con/sin ejes y gradiente más presente
- `variant="featured"` en MobilePositionsList para modo 'Posiciones Destacadas' (top-5 flat por valuación)
- `useBalanceVisibility()` hook global para censura, sincronizado vía localStorage + StorageEvent
- Pill activo en MobileNav: `background: 'var(--teal-dim)'`, `rounded-full`, `transition-all duration-200`
- Donut con contador central: `<text>` con `posCount` + 'pos.' superpuesto con `pointer-events-none`

### Dependencias relevantes
- recharts (ya instalado, usado en PortfolioHeroChart, AllocationPanel, Sparkline, MiniSpark)
- lucide-react (Eye, EyeOff, Settings, etc. ya en MobileHeader)
- @tanstack/react-query (staleTime 30s)
- TailwindCSS con tokens CSS: var(--teal), var(--teal-dim), var(--ink), var(--ink-faint), var(--ink-mute), var(--surface-*), var(--border)
- DM Sans tipografía

### Riesgos / Restricciones
- PageHeader con title='Overview' se renderiza en TODOS los tamaños de pantalla en Dashboard.jsx línea 356 — debe ocultarse en mobile con `hidden md:block` para lograr 'sin título de página' en mobile
- La censura (useBalanceVisibility) ya está en MobileHeader pero también existe un ojo dentro de PortfolioHeroChart que debe removerse/coordinarse con M2/M5 de la SPEC
- El gradiente visible (M1) ya existe en `renderChart(..., { minimal: true })` con stops 0%→0.35, 55%→0.08, 100%→0 — verificar que se esté usando correctamente en mobileEl
- MobileHeader ya está implementado con row1 (avatar·logo·ojo+settings) y row2 (cuenta+ARS/USD) — el 'header compacto' ya existe, validar que no haya regresión al centrar logo
- El orden del Overview mobile (M8) ya está implementado: Hero → AllocationPanel → MobilePositionsList(featured) → KPI carousel — verificar que no haya dobles paddings/separadores redundantes

### Evidencia verificada
- **`src/pages/Dashboard.jsx`** — `PageHeader title="Overview" subtitle="Portfolio"` (líneas 356, confianza 100%): Este PageHeader se renderiza en el tab 'resumen' SIN restricción de viewport. Para lograr 'sin título de página' en mobile, debe envolverse en `className="hidden md:block"` o similar. Es el punto clave de la tarea.
- **`src/components/common/MobileHeader.jsx`** — `MobileHeader` (líneas 13-95, confianza 100%): Header mobile ya implementado con Row 1 (avatar · logo · ojo + settings) y Row 2 (PortfolioSelector + ARS/USD toggle). Alturas: h-11 + h-9. Ya incluye `useBalanceVisibility` y el gate `isAdmin` para settings. El 'header compacto' del mockup ya existe functionalmente.
- **`src/features/portfolio/components/PortfolioHeroChart.jsx`** — `renderChart` (líneas 162-202, confianza 100%): Fución que renderiza el chart. Acepta parámetro `{ minimal }` que controla: sin CartesianGrid, sin XAxis, sin YAxis, altura 200px, gradiente más presente (stops 0%→0.35, 55%→0.08, 100%→0), strokeWidth 2.5. El 'gradiente visible' del mockup ya existe.
- **`src/features/portfolio/components/PortfolioHeroChart.jsx`** — `mobileEl` (líneas 210-250, confianza 100%): Layout mobile del hero: label 'Valor del Portfolio' → value 34px → P&L + badge → LiveDot → chart 200px full-bleed → period chips (7 items, justify-between). Ya coincide con M1+M2+M3 de la SPEC.
- **`src/features/portfolio/components/AllocationPanel.jsx`** — `AllocationPanel` (líneas 77-140, confianza 100%): Donut con contador central: Pie con innerRadius=32, outerRadius=46; centro con `posCount` (18px) + 'pos.' (10px). Leyenda con dots de color, labels truncados y % coloreados por slice. Toggle Por tipo/Por estrategia. Ya implementa M4 de la SPEC.
- **`src/features/portfolio/components/MobilePositionsList.jsx`** — `variant='featured'` (líneas 177-245, confianza 0%): [NO VERIFICADO] Variante 'featured' que renderiza 'Posiciones Destacadas' con top-5 por valuación, AssetLogo (36px), MiniSpark con área gradiente 72×28, valuación + % coloreado. Link 'Ver todas'. Ya implementa M6 de la SPEC.
- **`src/features/portfolio/components/MobilePositionsList.jsx`** — `MiniSpark` (líneas 13-35, confianza 100%): Sparkline de área con gradiente (teal o coral según signo), 72×28px, strokeWidth 2.5. Usa recharts AreaChart con gradiente linear. Ya implementado como componente local compartido.
- **`src/components/common/MobileNav.jsx`** — `MobileNav` (líneas 38-60, confianza 100%): Bottom nav con 4 ítems fijos + 'Más'. Ítem activo usa pill: `background: 'var(--teal-dim)'`, `rounded-full`, `transition-all duration-200`, color `var(--teal)`. Inactivos: `var(--ink-faint)`. Alto total 56px + safe-area. Ya implementa M7 de la SPEC.
- **`src/features/portfolio/components/DashboardOverview.jsx`** — `DashboardOverview` (líneas 192-230, confianza 100%): Layout mobile del overview (variant='full'): HeroChart → AllocationPanel → MobilePositionsList(variant='featured') → KPI carousel. Ya implementa el reorden M8 de la SPEC. Usa `md:hidden` para aislar mobile.
- **`src/hooks/useBalanceVisibility.js`** — `useBalanceVisibility` (líneas 7-31, confianza 100%): Hook global para censura vía localStorage + StorageEvent. Retorna `[hidden, toggle]`. Usado por MobileHeader, MoneyValue, PortfolioHeroChart, AllocationPanel. Sin prop-drilling necesario.
- **`src/components/ui/PageHeader.jsx`** — `PageHeader` (líneas 1-22, confianza 100%): Componente que renderiza título + subtítulo + acciones. Se usa en Dashboard.jsx para desktop. En mobile debe NO mostrarse (mockup sin título). El componente en sí es correcto; el cambio está en el call-site.
- **`src/components/ui/Sparkline.jsx`** — `Sparkline` (líneas 14-46, confianza 100%): Sparkline genérico del design system (AreaChart, gradiente, sin ejes). 40px default height. Usado como base; MiniSpark en MobilePositionsList es una variante especializada.

## Riesgos y restricciones técnicas para: Argos mobile — Paridad con mockup: sin título de página, header compacto, gradiente visible

**Resumen:** La implementación de la paridad con mockup para mobile tiene varios componentes ya pre-implementados (modo minimal del chart, pill activo en MobileNav, reorden del Overview, compact header con ojo). El riesgo principal es que el PageHeader con título 'Overview' se renderiza en mobile sin estar envuelto en `hidden md:block`, contradiciendo la SPEC de 'sin título de página'. También hay que coordinar la duplicación del botón de censura (ojos) entre MobileHeader (ya lo tiene) y PortfolioHeroChart, y verificar que los offsets CSS del layout no se rompan al modificar alturas del header. No se encontraron tests unitarios para los componentes afectados, lo que reduce la red de seguridad pero también elimina riesgos de rotura de tests existentes.

### Archivos clave
- `src/pages/Dashboard.jsx`
- `src/components/common/MobileHeader.jsx`
- `src/components/common/MobileNav.jsx`
- `src/features/portfolio/components/PortfolioHeroChart.jsx`
- `src/features/portfolio/components/AllocationPanel.jsx`
- `src/features/portfolio/components/MobilePositionsList.jsx`
- `src/features/portfolio/components/DashboardOverview.jsx`
- `src/components/ui/PageHeader.jsx`
- `src/components/ui/Sparkline.jsx`
- `src/components/layout/AppLayout.jsx`
- `src/index.css`

### Patrones a seguir
- `md:hidden` / `hidden md:flex` para bifurcar layouts mobile/desktop sin romper desktop
- `renderChart(gradId, { minimal })` — parametrización existente para variante minimal del chart
- `useBalanceVisibility()` hook para censura de saldos en todos los componentes que muestren montos
- `MoneyValue`, `maskMoney`, `HIDDEN` de `@/components/ui` para renderizado seguro de valores
- CSS custom properties (`--surface-*`, `--teal-dim`, `--ink*`, `--border`) sin colores hardcodeados fuera de TEAL/CORAL/PERIWINKLE
- `min-h-[44px]` en todos los touch targets
- `variant` prop en `MobilePositionsList` para bifurcar 'featured' vs 'grouped'

### Dependencias relevantes
- react@^18.2.0
- recharts@^2.10.0
- lucide-react@^0.263.1
- react-router-dom@^6.20.0
- tailwindcss@^3.3.2
- @tanstack/react-query@^5.90.19

### Riesgos / Restricciones
- R1 — PageHeader duplicado en mobile: Dashboard.jsx renderiza `<PageHeader title="Overview" ...>` dentro de `px-6 lg:px-8 pt-2 flex-shrink-0` y NO está envuelto en `hidden md:block`, por lo que aparece también en mobile. La SPEC pide "sin título de página" en mobile — hay que agregar la clase `hidden md:block` al contenedor del PageHeader o moverlo dentro del bloque desktop de DashboardOverview.
- R2 — Coordinación ojo/censura duplicado: M5 pide mover el botón ojo desde PortfolioHeroChart a MobileHeader, pero MobileHeader YA tiene `Eye`/`EyeOff` con `useBalanceVisibility()`. Si PortfolioHeroChart mantiene su propio `balanceHidden` (lo lee también con `useBalanceVisibility()`), podría haber dos botones de censora visibles simultáneamente. Verificar que el ojo del hero se remueva en la variante mobile.
- R3 — Altura del header compacto + safe-area: MobileHeader usa `paddingTop: env(safe-area-inset-top)` y `h-11`/`h-9` para las filas. El mockup pide header más denso. Cambiar alturas puede afectar el offset `mobile-body-offset` y `pb-mobile-nav` definidos en `index.css` (líneas 148-162) que controlan el padding del main content para evitar solapamiento.
- R4 — Reorden del Overview mobile (M8): DashboardOverview.jsx ya tiene el orden Hero → AllocationPanel → MobilePositionsList → KPI carousel, que coincide con la SPEC. Sin embargo, hay que verificar que los paddings `gap-0` y `px-4` no generen dobles separadores entre secciones ni clips del chart full-bleed.
- R5 — Gradiente del chart: PortfolioHeroChart.jsx ya implementa `minimal` con 3 stops (0.35/0.08/0) vs non-minimal con 2 stops (0.26/0). La SPEC pide que el gradiente sea 'más visible'. El código actual ya coincide con la SPEC, pero cualquier cambio en `stopOpacity` debe validarse que no afecte el renderizado desktop (non-minimal).
- R6 — AllocationPanel donut center text: El contador central con `posCount` y "pos." no estaba visible en el código leído hasta línea ~150 de AllocationPanel.jsx. [NO VERIFICADO — no se alcanzó a leer el bloque de `<text>` en el centro del donut]. Si no existe, hay que agregarlo sin romper el layout actual de 100×100px.
- R7 — MiniSpark extraído a componente compartido: M6 pide convertir a área con gradiente los sparklines de las posiciones destacadas. Actualmente `MiniSpark` dentro de `MobilePositionsList.jsx` ya es un área con gradiente de 72×28px, y existe `Sparkline` en `src/components/ui/Sparkline.jsx` que también es área con gradiente pero con strokeWidth 1.5. Riesgo de duplicación/diverencia entre ambos.
- R8 — Tests: No se encontraron archivos `.test.*` en el repo para los componentes afectados (PortfolioHeroChart, MobileHeader, AllocationPanel, MobileNav, MobilePositionsList). Cualquier refactor no romperá tests existentes, pero tampoco hay cobertura que valide el cambio.
- R9 — Dependencia con PageHeader en múltiples rutas: PageHeader se usa en Admin, Dashboard, Fci, FciComparador, FciExplorador, FciPortfolio, Financiacion, FundingCrypto, FundingEngine, NexoLoans, StrategyTab. Cualquier cambio en su firma (props, estilos) impacta en cascada a todas esas páginas.
- R10 — Convención de tokens CSS: La SPEC exige no hardcodear colores fuera de TEAL `#2FD4CD` / CORAL `#E5616A` / PERIWINKLE `#5F89DD`. Se verificó que el código usa estos tokens inline o vía `var(--teal)`, `var(--teal-dim)`, etc. Sin embargo, AllocationPanel.jsx usa `#16151C`, `#20202A`, `#1E1E27` hardcodeados en estilos inline — son colores de superficie/fondo, no se consideran del tema, pero podrían violar la invariante si se interpreta estrictamente.

### Evidencia verificada
- **`src/pages/Dashboard.jsx`** — `PageHeader` (líneas 356-359, confianza 100%): El PageHeader con title='Overview' se renderiza dentro de un contenedor sin clase `hidden md:block`, por lo que aparece en mobile. La SPEC pide 'sin título de página' en mobile.
- **`src/components/common/MobileHeader.jsx`** — `toggleBalance` (líneas 19,51-61, confianza 100%): MobileHeader ya implementa el botón de censura (Eye/EyeOff) con useBalanceVisibility. Riesgo de duplicación si PortfolioHeroChart mantiene su propio ojo en mobile.
- **`src/features/portfolio/components/PortfolioHeroChart.jsx`** — `renderChart` (líneas 148-193, confianza 100%): La función renderChart acepta `{ minimal }` y en modo minimal ya saca CartesianGrid, XAxis, YAxis, usa 3 stops de gradiente (0.35/0.08/0) y strokeWidth 2.5. El modo no-minimal (desktop) mantiene ejes y grilla. La SPEC M1 ya está implementada.
- **`src/components/common/MobileNav.jsx`** — `rounded-full` (líneas 57-67, confianza 100%): El ítem activo de MobileNav ya usa `background: var(--teal-dim)`, `rounded-full`, `transition-all duration-200` y color teal. La SPEC M7 (pill activo) ya está implementada.
- **`src/features/portfolio/components/DashboardOverview.jsx`** — `md:hidden flex flex-col gap-0` (líneas 205-208, confianza 100%): El layout mobile de DashboardOverview ya tiene el orden Hero → AllocationPanel → MobilePositionsList → KPI carousel (M8). El padding bottom contempla nav + safe-area.
- **`src/components/ui/PageHeader.jsx`** — `PageHeader` (líneas 3-19, confianza 100%): Componente PageHeader que renderiza title en `<h1 className='text-[20px]'>`. La SPEC pide ocultarlo en mobile para el Overview.
- **`src/features/portfolio/components/AllocationPanel.jsx`** — `innerRadius` (líneas 117, confianza 100%): Donut con innerRadius=32 y outerRadius=46 (~69.5% ratio, cerca del 68% de la SPEC M4). paddingAngle=3, cornerRadius=4 ya configurados.
- **`src/index.css`** — `mobile-body-offset` (líneas 148-165, confianza 100%): Definiciones de padding-top y padding-bottom para evitar solapamiento con MobileHeader y MobileNav. Cualquier cambio en alturas del header/nav requiere actualizar estas variables.
- **`src/features/portfolio/components/AllocationPanel.jsx`** — `#16151C` (líneas 96-98, confianza 100%): Color de fondo hardcodeado (#16151C, #20202A) en AllocationPanel que podría no seguir la convención de tokens CSS. Posible deuda técnica menor.
