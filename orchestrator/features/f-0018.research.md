# Investigación Scout — f-0018

> Generado automáticamente. Evidencia verificada contra filesystem.

## Mapa del repo para: 3 Salteñas — Gastos operativos, Retiros y saldo del negocio

**Resumen:** El repo es una app Next.js 16 + tRPC v11 + Prisma 5.x para gestionar un microemprendimiento de empanadas. El modelo de datos ya incluye GastoOperativo y Retiro en el schema Prisma, y el router de gastos (gastoRouter) está implementado con list y byPeriod. Sin embargo, el router de retiros (retiroRouter) está completamente vacío, no existen páginas UI para gastos ni retiros, y el cálculo de 'saldo del negocio' (ingresos − compras − gastos − retiros) mencionado en la especificación aún no está implementado. El feature F-0018 (Gastos/Retiros) figura como último en el backlog, detrás de Producción, Ventas y Dashboard, por lo que GastosOperativos, Retiros y saldo del negocio son los módulos pendientes más importantes del MVP.

### Archivos clave
- `SPEC-MVP.md`
- `SESSION-HANDOFF.md`
- `NEXT-STEPS.md`
- `package.json`
- `prisma/schema.prisma`
- `src/server/api/routers/gasto.ts`
- `src/server/api/routers/retiro.ts`
- `src/server/api/routers/dashboard.ts`
- `src/server/api/routers/compra.ts`
- `src/server/api/routers/lote.ts`
- `src/server/api/routers/venta.ts`
- `src/server/api/routers/insumo.ts`
- `src/server/api/routers/producto.ts`
- `src/server/api/routers/config.ts`
- `src/server/api/root.ts`
- `src/server/api/trpc.ts`
- `src/lib/costeo.ts`
- `src/lib/lotes.ts`
- `src/lib/produccion.ts`
- `src/lib/compras.ts`
- `src/lib/insumos.ts`
- `src/app/page.tsx`
- `src/app/compras/page.tsx`
- `src/__tests__/costeo.test.ts`
- `src/__tests__/dashboard.test.ts`

### Patrones a seguir
- Router tRPC v11 con createTRPCRouter, publicProcedure, .query()/.mutation()
- Prisma ORM con schema en prisma/schema.prisma y $transaction para operaciones multi-tabla
- Lógica de negocio en funciones puras dentro de src/lib/ (costeo.ts, lotes.ts, produccion.ts, etc.)
- Páginas client-side con "use client", useState y hooks tRPC (trpc.xxx.useQuery, useMutation)
- Validación de inputs con Zod en cada procedure tRPC
- Formateo ARS con Intl.NumberFormat('es-AR', { style: 'currency', currency: 'ARS' })
- Seed de datos vía prisma/seed.ts con IDs fijos ('empanada-jyq', 'config-mod')
- Patrón de invalidador de queries tras mutation (utils.xxx.invalidate())

### Dependencias relevantes
- next@16.2.10
- react@19.2.4 / react-dom@19.2.4
- @trpc/server@^11.18.0 / @trpc/client@^11.18.0 / @trpc/react-query@^11.18.0
- @prisma/client@^5.22.0 / prisma@^5.22.0
- zod@^4.4.3
- superjson@^2.2.6
- @tanstack/react-query@^5.101.2
- @clerk/nextjs@^7.5.14
- tailwindcss@^4

### Riesgos / Restricciones
- retiroRouter está vacío (export const retiroRouter = createTRPCRouter({})) — no hay implementación de crear/listar retiros
- No existen rutas UI para Gastos ni Retiros (faltan src/app/gastos/ y src/app/retiros/)
- No hay cálculo de 'saldo del negocio' como derivado (ingresos − compras − gastos − retiros) — mencionado en SPEC pero no implementado
- Dashboard prorratea costoOperativo contra unidades VENDIDAS, no contra unidades PRODUCIDAS como dice SPEC §5
- IDs fijos hardcodeados: producto 'empanada-jyq' y config 'config-mod' — pueden romper si cambia el seed
- Prisma debe pinsearse en 5.x — npm install prisma@latest rompe el schema (gotcha #1 en SESSION-HANDOFF)
- Auth pública sin restricciones (Clerk sin invite-only, middleware mínimo) — dato sensible (costoUnitarioActual) expuesto
- Push automático a main sin deploy real ni gate de seguridad (gotcha #9 en SESSION-HANDOFF)

### Evidencia verificada
- **`prisma/schema.prisma`** — `model GastoOperativo` (líneas 83-93, confianza 100%): Modelo Prisma completo para gastos operativos con campos fecha, categoria (packaging/delivery/otro), descripcion, monto. Base de datos del módulo Gastos.
- **`prisma/schema.prisma`** — `model Retiro` (líneas 95-103, confianza 100%): Modelo Prisma completo para retiros con campos fecha, monto, nota. Base de datos para el módulo Retiros/Mi plata.
- **`src/server/api/routers/gasto.ts`** — `gastoRouter` (líneas 4-31, confianza 100%): Router tRPC de Gastos: implementa list (todos los gastos) y byPeriod (agregado de monto por rango de fechas). Usa ctx.db.gastoOperativo.
- **`src/server/api/routers/retiro.ts`** — `retiroRouter` (líneas 4-4, confianza 100%): Router tRPC de Retiros: ESTÁ VACÍO (createTRPCRouter({})). No hay procedures list, create, ni summary. Es el principal gap a implementar.
- **`src/server/api/routers/dashboard.ts`** — `dashboardRouter` (líneas 42-169, confianza 100%): Router del Dashboard: procedure resumenPorPeriodo que integra gastos (via gastoOperativo.aggregate), ventas, configMOD y producto para calcular costo total por empanada y margen.
- **`src/lib/costeo.ts`** — `prorratearCostoOperativo` (líneas 11-17, confianza 100%): Función pura que prorratea gastos del período contra unidades vendidas. Retorna 0 si no hay unidades o gastos. Usada en dashboard para calcular costoOperativo por empanada.
- **`src/lib/costeo.ts`** — `calcularCostoTotalEmpanada` (líneas 26-32, confianza 100%): Suma las tres componentes de costo: materia prima + MOD + operativo. Fórmula del SPEC §5.
- **`src/lib/costeo.ts`** — `calcularCostoMOD` (líneas 3-9, confianza 100%): costoMOD = sueldoObjetivoSemanal / empanadasEstimadasSemana. El retiro de $150.000/semana de Dani se modela como ConfigManoDeObra, no como Retiro.
- **`src/app/page.tsx`** — `sinGastosCargados` (líneas 97-103, confianza 100%): UI del Dashboard: muestra alerta 'Sin gastos cargados este período' cuando totalGastos === 0. El dashboard ya tiene soporte parcial para gastos operativos.
- **`src/app/page.tsx`** — `DashboardPage` (líneas 50-124, confianza 100%): Componente principal del dashboard. Muestra KPIs (unidades, ingresos, costo, margen, producibles) con selector de período (hoy/semana/mes). No muestra retiros ni saldo del negocio.
- **`src/server/api/root.ts`** — `appRouter` (líneas 12-22, confianza 100%): Router raíz que monta todos los routers incluyendo gasto: gastoRouter y retiro: retiroRouter. La estructura de integración tRPC ya está lista.
- **`SPEC-MVP.md`** — `GastoOperativo` (líneas 59-60, confianza 100%): Especificación del modelo GastoOperativo: fecha, categoría (packaging/delivery/otro), descripción, monto. Se prorratea contra unidades del período.
- **`SPEC-MVP.md`** — `Retiro` (líneas 61-62, confianza 100%): Especificación del modelo Retiro: fecha, monto, nota. 'Plata que Dani saca para sí, separada de la caja del negocio'.
- **`SPEC-MVP.md`** — `saldo del negocio` (líneas 108-108, confianza 100%): El saldo del negocio es un cálculo derivado: ingresos − compras − gastos − retiros. No hay caja bancaria real. Pendiente de implementar.
- **`SESSION-HANDOFF.md`** — `F-0018 (Gastos/Retiros)` (líneas 37-37, confianza 100%): F-0018 (Gastos/Retiros) es el último feature del backlog, después de F-0015 (Producción), F-0016 (Ventas), F-0017 (Dashboard). Aún no iniciado.
- **`src/__tests__/costeo.test.ts`** — `prorratearCostoOperativo` (líneas 24-33, confianza 100%): Tests que verifican que prorratearCostoOperativo distribuye gastos entre unidades, maneja gastos=0 y unidades=0. Cobertura de casos borde.
- **`src/__tests__/dashboard.test.ts`** — `buildMockDb` (líneas 22-46, confianza 100%): Función de test que construye un mock de DB con gastoOperativo.aggregate. Usada en 10 tests del dashboard para verificar integración gastos → costos.
- **`src/server/api/routers/dashboard.ts`** — `rangoFechasPeriodo` (líneas 13-45, confianza 100%): Función que calcula rangos de fechas en UTC-3 para hoy/semana/mes. Relevante porque los gastos y retiros se filtran por período.

## Investigar qué existe para implementar: 3 Salteñas — Gastos operativos, Retiros y saldo del negocio

**Resumen:** El repo tiene el modelo de datos completo (GastoOperativo y Retiro en Prisma), el router de gastos parcialmente implementado (list y byPeriod, sin create), y el router de retiros completamente vacío. El dashboard ya integra gastos en el cálculo de costo por empanada pero no muestra retiros ni saldo del negocio. No existen páginas UI para gastos ni retiros. El implementador deberá: (1) agregar create/update/delete a gastoRouter, (2) desarrollar retiroRouter desde cero (create, list, byPeriod), (3) extender dashboardRouter para calcular saldo del negocio (ingresos − compras − gastos − retiros), (4) crear las páginas src/app/gastos/page.tsx y src/app/retiros/page.tsx, y (5) agregar secciones de retiros y saldo en el dashboard.

### Archivos clave
- `prisma/schema.prisma`
- `src/server/api/routers/gasto.ts`
- `src/server/api/routers/retiro.ts`
- `src/server/api/routers/dashboard.ts`
- `src/server/api/root.ts`
- `src/lib/costeo.ts`
- `src/app/page.tsx`
- `src/__tests__/costeo.test.ts`
- `src/__tests__/dashboard.test.ts`
- `prisma/seed.ts`
- `SPEC-MVP.md`

### Patrones a seguir
- Router tRPC con publicProcedure.query y publicProcedure.mutation, usando ctx.db.<modelo>
- createTRPCRouter({}) con export const nombreRouter
- Archivos de página en src/app/<ruta>/page.tsx con 'use client' y trpc.react hooks
- Inclusión del router en src/server/api/root.ts dentro de createTRPCRouter
- Mutations que usan ctx.db.$transaction para operaciones atómicas (ver compra.ts, lote.ts)
- Funciones puras de cálculo en src/lib/ importadas desde los routers
- Seed con upsert usando IDs fijos ("empanada-jyq", "config-mod", etc.)
- Patrón de tests con buildMockDb para mockear ctx.db en tests de routers

### Dependencias relevantes
- @trpc/server
- @trpc/react-query
- @trpc/client
- @prisma/client
- prisma
- zod
- superjson
- vitest

### Riesgos / Restricciones
- retiroRouter está completamente vacío ({}) — no hay ningún procedure implementado
- gastoRouter tiene list y byPeriod pero NO tiene create — no se pueden crear gastos desde la UI
- No existen páginas en src/app/gastos/ ni src/app/retiros/ — hay que crearlas desde cero
- El dashboard (src/app/page.tsx) muestra aviso de 'sin gastos cargados' pero NO muestra retiros ni saldo del negocio — el SPEC dice que debe mostrar 'retiros vs. saldo del negocio'
- dashboardRouter.resumenPorPeriodo no consulta retiros ni calcula saldo del negocio. La fórmula 'saldo = ingresos − compras − gastos − retiros' (SPEC-MVP) no está implementada en ningún lado
- El gastoRouter.byPeriod ya usa aggregate con _sum:monto, mismo patrón reusable para retiroRouter
- Los IDs fijos del seed ('empanada-jyq', 'config-mod') están hardcodeados en dashboard.ts y varios routers — cualquier nuevo procedimiento que necesite el producto o config debe usar esos mismos IDs
- No existe función en src/lib/ para calcular saldo del negocio (habría que crearla o extender costeo.ts o dashboard.ts)

### Evidencia verificada
- **`prisma/schema.prisma`** — `GastoOperativo` (líneas 82-90, confianza 100%): Modelo Prisma para gastos operativos con campos: id, fecha, categoria (packaging/delivery/otro), descripcion, monto, createdAt. Totalmente listo para usar.
- **`prisma/schema.prisma`** — `Retiro` (líneas 92-98, confianza 100%): Modelo Prisma para retiros con campos: id, fecha, monto, nota, createdAt. Totalmente listo para usar.
- **`src/server/api/routers/gasto.ts`** — `gastoRouter` (líneas 4-34, confianza 100%): Router tRPC existente con list (findMany orderBy fecha desc) y byPeriod (aggregate _sum:monto con filtro fecha gte/lte). No tiene create, ni update, ni delete — hay que agregarlos.
- **`src/server/api/routers/retiro.ts`** — `retiroRouter` (líneas 3-4, confianza 100%): Router completamente vacío: createTRPCRouter({}). No tiene ningún procedure implementado. Hay que desarrollar create, list, byPeriod, etc. desde cero.
- **`src/server/api/root.ts`** — `appRouter` (líneas 16-22, confianza 100%): Ya incluye gasto: gastoRouter y retiro: retiroRouter en el router raíz. Los nuevos procedures creados en esos routers serán accesibles automáticamente.
- **`src/server/api/routers/dashboard.ts`** — `resumenPorPeriodo` (líneas 53-129, confianza 100%): El procedure del dashboard ya consulta ctx.db.gastoOperativo.aggregate y retorna totalGastos, costoOperativo y sinGastosCargados. Pero NO consulta retiros NI calcula saldo del negocio. Hay que extenderlo.
- **`src/lib/costeo.ts`** — `prorratearCostoOperativo` (líneas 10-15, confianza 100%): Función lista para prorratear gastos: totalGastosPeriodo / unidadesVendidasPeriodo. Usada por dashboard. Ya testeada en costeo.test.ts.
- **`src/app/page.tsx`** — `DashboardPage` (líneas 47-126, confianza 100%): Dashboard actual muestra KpiCards con unidades, ingresos, costo, margen, producibles. Muestra aviso sinGastosCargados. NO muestra retiros ni saldo del negocio. Hay que agregar esas secciones.
- **`SPEC-MVP.md`** — `saldo del negocio` (líneas 106-107, confianza 100%): Define que 'saldo del negocio' es un cálculo derivado: ingresos − compras − gastos − retiros. No hay código que implemente esto todavía.
- **`src/__tests__/costeo.test.ts`** — `prorratearCostoOperativo` (líneas 25-36, confianza 100%): Tests existentes que verifican distribución de gastos, caso sin gastos (retorna 0) y caso sin unidades vendidas (retorna 0). Sirven como referencia para nuevos tests de retiro/saldo.
- **`prisma/seed.ts`** — `configManoDeObra` (líneas 45-52, confianza 100%): Seed con upsert de config-mod (sueldoObjetivoSemanal=150000, empanadasEstimadasSemana=170). Si se necesita seed de gastos o retiros de ejemplo, seguir este mismo patrón de upsert con IDs fijos.
- **`src/server/api/routers/gasto.ts`** — `byPeriod` (líneas 14-33, confianza 100%): Patrón de procedure con input z.object({ desde: z.date(), hasta: z.date() }) que hace aggregate _sum:monto. Este mismo patrón puede reusarse para retiroRouter.byPeriod y para calcular el total de retiros en el dashboard.
- **`src/app/page.tsx`** — `sinGastosCargados` (líneas 99-104, confianza 100%): El dashboard ya maneja el estado 'sin gastos cargados' con un banner amber. Patrón a seguir cuando se agregue 'sin retiros cargados' o cálculo de saldo dependiente de datos.

## Riesgos y restricciones técnicas para: 3 Salteñas — Gastos operativos, Retiros y saldo del negocio

**Resumen:** Los módulos de Gastos operativos, Retiros y Saldo del negocio requieren implementación casi completa desde cero: retiroRouter está vacío, gastoRouter carece de mutations create/update/delete, no existen UI pages, y el cálculo de 'saldo del negocio' (ingresos − compras − gastos − retiros) no está implementado en el dashboard. Los principales riesgos son: (1) modificar el dashboard existente para incluir saldo puede romper los tests actuales de dashboard.test.ts, (2) todos los routers usan publicProcedure exponiendo datos financieros sensibles, (3) el executor tiende a introducir auth global no solicitada (gotcha #8), y (4) los IDs hardcodeados (PRODUCTO_ID, CONFIG_MOD_ID) son frágiles ante cambios futuros en la semilla de datos.

### Archivos clave
- `prisma/schema.prisma`
- `src/server/api/routers/gasto.ts`
- `src/server/api/routers/retiro.ts`
- `src/server/api/routers/dashboard.ts`
- `src/server/api/root.ts`
- `src/lib/costeo.ts`
- `src/app/page.tsx`
- `src/__tests__/costeo.test.ts`
- `src/__tests__/dashboard.test.ts`
- `SESSION-HANDOFF.md`
- `SPEC-MVP.md`

### Patrones a seguir
- Usar createTRPCRouter + publicProcedure (o protectedProcedure si se decide auth) consistente con compraRouter/ventaRouter/loteRouter
- Usar ctx.db.$transaction cuando se modifiquen múltiples tablas (ej: gasto + actualización de saldo)
- UI mobile-first con Tailwind, mismo patrón de layout que src/app/page.tsx (max-w-lg mx-auto p-4)
- Input validation con zod z.object() en cada procedure (seguir patrón de venta.ts y lote.ts)
- Tests con vitetest usando buildMockDb y createCallerFactory, mismo patrón que dashboard.test.ts
- fetch invalidation post-mutation: utils.gasto.list.invalidate() y utils.retiro.list.invalidate() tras cada creación

### Dependencias relevantes
- zod
- @trpc/server
- @prisma/client
- superjson
- @/lib/costeo
- @/server/api/trpc
- @/lib/db

### Riesgos / Restricciones
- retiroRouter está vacío (createTRPCRouter({})) — no tiene ningún procedure implementado, hay que crearlos desde cero (create, list, byPeriod, etc.)
- gastoRouter no tiene mutation create — solo list y byPeriod (read-only); falta create/update/delete con validación de categoría ('packaging'|'delivery'|'otro')
- No existe UI (/app) para gastos ni retiros — no hay carpeta gastos/ ni retiros/ en src/app; hay que crear páginas enteras
- Cálculo de 'saldo del negocio' (ingresos − compras − gastos − retiros) no existe en ningún lado — el dashboard actual no lo calcula ni lo muestra (SPEC-MVP §4: 'derivado, no cuenta real')
- Dashboard actual (src/app/page.tsx) no tiene sección de retiros vs saldo — hay que agregar nuevos KPIs y posiblemente modificar el router dashboard.ts, lo que podría romper tests existentes en dashboard.test.ts
- Todos los routers usan publicProcedure — datos financieros sensibles (gastos, retiros, saldo) quedan expuestos sin autenticación. Riesgo real si se despliega sin cerrar auth antes
- El executor (augusto-os) tiene tendencia documentada a colar auth global en middleware.ts sin que se lo pidan (gotcha #8 de SESSION-HANDOFF) — puede introducir regresiones en webhooks tRPC
- Prisma 5.x pineado manualmente — si alguien corre npm install prisma@latest se rompe el schema (migración a v7 incompatible). El gastoRouter usa fechas (z.date()) que requieren superjson; cualquier cambio de versión de Prisma puede afectar el serializador
- No existe proyecto Vercel — push a main ocurre igual sin deploy real (gotcha #9). La feature de gastos/retiros podría mergearse y pushearse sin tener un ambiente productivo donde verificar
- Los tests de costeo.test.ts usan prorratearCostoOperativo con división por cero — cualquier cambio en la fórmula de prorrateo (ej: pasar a usar unidades producidas vs vendidas) rompe tests existentes
- El dashboard usa IDs hardcodeados (PRODUCTO_ID='empanada-jyq', CONFIG_MOD_ID='config-mod') — si se agregan múltiples productos o configs en el futuro, el saldo del negocio podría calcularse incorrectamente

### Evidencia verificada
- **`src/server/api/routers/retiro.ts`** — `retiroRouter` (líneas 4, confianza 100%): Router vacío (createTRPCRouter({})) — no tiene procedures implementados. Hay que crear create, list, byPeriod desde cero.
- **`src/server/api/routers/gasto.ts`** — `gastoRouter` (líneas 4-31, confianza 100%): Router de gastos solo tiene list y byPeriod (ambas queries). No existe mutation create/update/delete — hay que implementarlas.
- **`prisma/schema.prisma`** — `GastoOperativo` (líneas 62-68, confianza 100%): Modelo GastoOperativo con campos fecha, categoria (enum string), descripcion?, monto. Categorías válidas: 'packaging'|'delivery'|'otro'.
- **`prisma/schema.prisma`** — `Retiro` (líneas 77-81, confianza 100%): Modelo Retiro con fecha, monto, nota?. Plata que Dani saca para sí, separada de caja del negocio.
- **`src/server/api/routers/dashboard.ts`** — `resumenPorPeriodo` (líneas 55-130, confianza 100%): Dashboard actual: calcula costoMateriaPrima, costoMOD, costoOperativo, margen PERO NO incluye retiros ni saldo del negocio. Habría que modificar este query para agregar retiros y fórmula de saldo.
- **`src/app/page.tsx`** — `DashboardPage` (líneas 60-145, confianza 100%): UI del dashboard: muestra KPIs (unidades, ingresos, costo, margen) pero NO tiene sección de 'retiros vs saldo del negocio' como especifica SPEC-MVP §6. Hay que agregarla.
- **`src/lib/costeo.ts`** — `prorratearCostoOperativo` (líneas 10-16, confianza 100%): Fórmula de prorrateo: totalGastosPeríodo / unidadesVendidasPeríodo. Si se cambia a 'unidades producidas' (como sugiere SPEC-MVP §5), rompe tests existentes.
- **`src/server/api/routers/dashboard.ts`** — `PRODUCTO_ID` (líneas 13, confianza 100%): ID de producto hardcodeado como 'empanada-jyq'. Si cambia el seed o se agregan productos, el dashboard deja de funcionar.
- **`src/server/api/routers/dashboard.ts`** — `CONFIG_MOD_ID` (líneas 14, confianza 100%): ID de config de mano de obra hardcodeado como 'config-mod'. Mismo riesgo que PRODUCTO_ID.
- **`src/server/api/trpc.ts`** — `publicProcedure` (líneas 13, confianza 100%): Todos los routers (gasto, retiro, dashboard, etc.) usan publicProcedure — sin autenticación. Datos financieros expuestos.
- **`src/__tests__/dashboard.test.ts`** — `buildMockDb` (líneas 25-45, confianza 100%): Mock de DB para tests del dashboard. Si se agregan retiros al query resumenPorPeriodo, estos tests se rompen porque el mock no incluye retiro.
- **`src/__tests__/costeo.test.ts`** — `prorratearCostoOperativo` (líneas 25-31, confianza 100%): Test existente que verifica la fórmula de prorrateo. Cualquier cambio en la función (ej: prorratear contra unidades producidas en vez de vendidas) rompe este test.
- **`SESSION-HANDOFF.md`** — `gotcha #8` (líneas 75-83, confianza 100%): El executor tiende a colar auth global en middleware.ts sin que se lo pidan — riesgo de regresión al implementar gastos/retiros si toca middleware.
- **`SESSION-HANDOFF.md`** — `gotcha #1` (líneas 33-36, confianza 100%): Prisma pinneado a mano en 5.x. Si alguien corre npm install prisma@latest se rompe el datasource (schema incompatible con v7).
- **`SPEC-MVP.md`** — `saldo del negocio` (líneas 108, confianza 100%): El saldo del negocio es un cálculo derivado: ingresos − compras − gastos − retiros. No existe implementado en ningún lado del código actual.
