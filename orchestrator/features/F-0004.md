---
id: F-0004
title: Confirmar branding Spensiv (tracker) y limpiar referencias cruzadas
target: spensiv
ui: true
acceptance:
  - El nombre visible de la app es "Spensiv" en title, openGraph.siteName y header de UI
  - El branding de cashflow/tarjetas/gastos/proyección de deuda se mantiene (le corresponde al tracker)
  - metadataBase apunta a la URL de prod del tracker (https://spensiv-tracker.vercel.app)
  - No quedan referencias a préstamos / crédito / Portal AP / Kredy heredadas por error (este repo es el tracker, no Kredy)
  - Typecheck y lint pasan sin errores

## Contexto

`spensiv-tracker/` (target del orquestador = `spensiv`) es el producto de finanzas personales. Su branding
actual ("Spensiv - Tu motor de cashflow personal", tarjetas/gastos) ES el correcto para este repo. Esta
feature es mayormente de confirmación + limpieza: asegurar que el nombre visible sea "Spensiv" donde falte
y que no haya quedado nada de la capa de préstamos/AP/Kredy en el copy.

Estado verificado (jun 2026): `app/layout.tsx` del tracker tiene title/description/siteName de cashflow
(correcto). Falta confirmar metadataBase = `https://spensiv-tracker.vercel.app` y barrer referencias cruzadas.

## ⚠️ Prerrequisito (gate del loop)

El target `spensiv` (tracker) todavía NO tiene sandbox dev. El guard del orquestador (`assertNoProdDb`)
ABORTA cualquier corrida mientras `SPENSIV_DEV_DATABASE_URL` esté en `<COMPLETAR>` — esto es correcto:
evita correr contra la prod `ep-floral-mud`. Antes de correr este loop:
  1. Crear un proyecto Neon dev nuevo para el tracker (o branch).
  2. Pegar su URL en `orchestrator/.env` como `SPENSIV_DEV_DATABASE_URL`.
  3. `cd spensiv-tracker && npx prisma db push` contra esa URL para materializar el schema.

## Pasos sugeridos (el planner puede refinar)

1. Confirmar/ajustar `app/layout.tsx`: `siteName: 'Spensiv'`, metadataBase del tracker.
2. Grep por `préstamo`, `crédito`, `Portal AP`, `Kredy`, `AP_APP_NAME`, `/l/` y remover lo que no aplique al tracker.
3. Asegurar nombre visible "Spensiv" en headers/UI.
4. Typecheck + lint.

## Fuera de alcance

- Cambios de schema / migraciones.
- Deploy a Vercel.
