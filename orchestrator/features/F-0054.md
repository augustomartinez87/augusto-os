---
id: F-0054
title: Argos — aplicar rescates de FCI detectados automáticamente (consumo de lotes PPC/FIFO)
target: argos
ui: true
resolves: [AR-007]
acceptance:
  - "En algún lugar visible del módulo de FCI (ej. arriba de la tabla de lotes/rescates
    existente, o como una card en el dashboard si ya existe un patrón de 'pendientes'),
    aparece una sección 'Rescates detectados pendientes de aplicar' SOLO cuando existe
    al menos una fila en `fci_rescates` con `external_ref is not null` y
    `mutations = '[]'::jsonb` (o `jsonb_array_length(mutations) = 0`) — si no hay
    ninguna, la sección no se muestra"
  - "Cada rescate pendiente se lista con: nombre del fondo (join a `fci_master.nombre`
    vía `fci_id`), fecha, cuotapartes, vcp_salida, monto_rescatado — todos datos que ya
    están en la fila, sin necesidad de que Augusto los reingrese"
  - "Cada fila pendiente tiene un botón 'Aplicar' que, al hacer click, ejecuta el
    consumo de lotes (PPC por default, ver Restricciones) contra las `fci_lots` activas
    de ese `fci_id`+`tipo`, actualiza esas lotes (mismo efecto que
    `applyRedemptionPPC`/`applyRedemptionFIFO` ya hacen para un rescate nuevo) y
    completa el campo `mutations` de la fila YA EXISTENTE en `fci_rescates` — sin crear
    una fila nueva"
  - "Si al momento de aplicar no hay cuotapartes activas suficientes en `fci_lots`
    para cubrir el `cuotapartes` del rescate pendiente (evento posible si hubo otro
    rescate manual de por medio), se muestra un error claro a Augusto en vez de aplicar
    un consumo parcial silencioso — el rescate queda pendiente hasta que él lo revise"
  - "Después de aplicar exitosamente, la fila desaparece de la sección de pendientes
    (porque `mutations` ya no está vacío) y el rescate se ve exactamente igual que uno
    cargado a mano en el resto de la UI (tabla de rescates, cálculo de rendimiento, etc.)"
  - "La notificación de este rescate (que ya salió automáticamente al crearse la fila
    en F-0053, vía `sync_fci_notifications`) no cambia ni se duplica al aplicar —
    aplicar es solo completar `mutations`, no crea una notificación nueva"
  - "Typecheck y tests pasan sin errores"
---

## Contexto

**Nota de terminología (por qué NO es AR-011):** AR-011 ("conciliación de lugares
contra el portfolio real de la ALyC") fue descartado explícitamente por Augusto
2026-08-26 — comparar la posición calculada por Argos contra lo que reporta la web de
Alycbur. Este feature es otra cosa completamente distinta que, por mala elección de
palabra en una versión anterior de este spec, quedó nombrada igual ("conciliar") y
generó confusión — se renombró a "aplicar" en todo este documento para evitarla. Acá no
se compara nada contra ningún reporte externo; es simplemente completar, para un
rescate que YA está insertado en `fci_rescates`, el cálculo de qué lotes de `fci_lots`
consume esa venta.

F-0053 (target `caucion-sync`) agrega un pipeline automático que detecta rescates de
FCI en el informe semanal de Alycbur y los inserta en `fci_rescates` con los datos ya
conocidos (fecha, cuotapartes, vcp_salida, monto_rescatado, `external_ref` = número de
comprobante) pero con `mutations: []` — deliberadamente no toca `fci_lots`, porque
calcular qué lotes se consumen (PPC o FIFO, con matemática Decimal-precisa) es lógica
que ya existe y está probada en el frontend (`fciService.applyRedemptionPPC` /
`applyRedemptionFIFO`, `src/features/fci/services/fciService.js`), y reimplementarla en
Python sin esos mismos tests arriesga corromper el costo base real de la cartera de
Augusto.

Este feature es el otro lado: dejar que Argos "termine" esos rescates ya detectados,
reusando el código de consumo de lotes que YA funciona, en vez de que Augusto tenga que
recargarlos a mano (lo cual sería redundante — la fila en `fci_rescates` ya existe,
sería un duplicado). Augusto confirmó (2026-09-07) que quiere este paso manual — un
click explícito antes de que se toquen los lotes — y no aplicación 100% automática sin
supervisión.

**Cómo identificar "pendiente de aplicar" sin campo nuevo:** un rescate cargado a
mano SIEMPRE tiene `mutations` con al menos un elemento (así es como
`applyRedemptionPPC`/`applyRedemptionFIFO` lo insertan hoy — ver líneas ~405-420 y la
función PPC análoga en `fciService.js`). Un rescate insertado por F-0053 tiene
`mutations: []` y `external_ref` no-null. Esa combinación (`external_ref is not null`
AND `mutations` vacío) es inequívoca — no hace falta agregar una columna de estado.

**Qué método de consumo usar:** el default en toda la UI de Argos es PPC
(`useFciLotEngine.js` línea 368: `method = 'PPC'`), no FIFO — `redeem`/`applyRedemption`
solo usan FIFO si algo pasa `method: 'FIFO'` explícitamente. Aplicar un rescate
pendiente debe usar el mismo default que usaría Augusto si lo cargara a mano, salvo que
él haya estado usando FIFO consistentemente para este fondo en particular (revisar
`fci_lots.tipo` y el historial de rescates de ese `fci_id` antes de asumir — si hay
ambigüedad, preguntar a Augusto en vez de asumir).

## Pasos sugeridos (el planner puede refinar)

1. En `fciService.js`, refactorizar `applyRedemptionPPC` y `applyRedemptionFIFO` para
   separar "calcular y aplicar el consumo de lotes" (dado portfolioId, fciId, cantidad,
   tipo, método → devuelve `mutations` y actualiza `fci_lots`) de "insertar la fila
   nueva en `fci_rescates`" — hoy ambas cosas están mezcladas en la misma función. El
   objetivo es poder reusar la primera mitad para un rescate que YA EXISTE (aplicar)
   sin duplicar la lógica de cálculo.
2. Agregar `applyPendingRescate(rescateId, method = 'PPC')` a `fciService.js`: lee
   la fila de `fci_rescates` por id (debe tener `mutations` vacío), corre el cálculo de
   consumo compartido del paso 1 contra `fci_id`/`cuotapartes`/`tipo` de esa fila,
   aplica los `update` a `fci_lots`, y hace `update` de esa MISMA fila de
   `fci_rescates` seteando `mutations` — nunca un `insert` nuevo.
3. Query/hook para listar pendientes: `fci_rescates` donde `external_ref is not null`
   y `mutations` es un array vacío (filtrar client-side con
   `.eq('mutations', '[]')` o el equivalente que soporte el cliente de Supabase-js
   para jsonb, verificar sintaxis correcta contra la versión instalada), con join a
   `fci_master` para el nombre del fondo.
4. UI: sección/alert nueva (buscar el patrón visual ya usado para alerts/pendientes en
   el resto de Argos, ej. `arancelAudit`/`CaucionesTable` u otro componente similar, para
   mantener consistencia) con la lista y el botón "Aplicar" por fila, manejando el
   estado de carga y el error de cuotapartes insuficientes de forma clara (toast o
   inline, seguir el patrón que ya use el resto de la app).
5. Tests: (a) `applyPendingRescate` con lotes activos suficientes — verifica que
   `mutations` queda poblado igual que lo haría un rescate nuevo con los mismos
   parámetros, y que `fci_lots` se actualiza igual; (b) caso de cuotapartes
   insuficientes — debe tirar error y NO modificar ningún lote ni la fila de rescate;
   (c) que un rescate cargado a mano (mutations ya poblado) nunca aparece en la lista
   de pendientes.
6. Correr typecheck y tests, corregir lo que rompa.

## Fuera de alcance

- Cualquier cambio al pipeline de ingesta (`caucion-sync`/`fci_sync.py`, F-0053) — este
  feature solo consume lo que esa tabla ya tiene.
- Permitir editar los valores parseados (fecha/cuotapartes/vcp/monto) antes de
  aplicar — si algo vino mal del PDF, el flujo correcto es que Augusto lo descarte
  (fuera de alcance también: un botón "descartar", evaluar si hace falta en una vuelta
  posterior) y lo cargue a mano como siempre.
- Cambiar el método default (PPC) para nadie que no sea este flujo — no es una
  oportunidad para revisar la política general de FIFO vs PPC de Augusto.
- Suscripciones — F-0053 ya las inserta completas (`fci_lots` con todos los campos),
  no requieren ningún paso de este feature.
- Aplicación automática sin click — Augusto confirmó explícitamente que quiere el paso
  manual (2026-09-07). No convertir esto en un cron/trigger que aplique solo.
- Cualquier forma de comparar la posición calculada por Argos contra el portfolio real
  reportado por la web de Alycbur — eso es AR-011, descartado, no reabrir acá.

## Restricciones clave

- **Nunca insertar una fila nueva en `fci_rescates` al aplicar** — la fila ya existe
  desde F-0053, aplicar es completar `mutations` sobre esa misma fila.
- **Nunca aplicar un consumo parcial de cuotapartes sin dejarlo explícito** — si no
  alcanzan los lotes activos, es un estado de error a mostrar, no un cálculo silencioso
  con lo que haya disponible.
- **El botón "Aplicar" es un click explícito de Augusto, no una acción automática** —
  ver Fuera de alcance.
- La TNA/tasa nunca debe mostrarse en vistas de prestatario — no aplica directamente a
  este feature, pero es una restricción absoluta del proyecto a no romper en cualquier
  copy nuevo.
- Sin librerías nuevas.
