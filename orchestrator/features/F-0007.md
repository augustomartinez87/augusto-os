---
id: F-0007
title: Documentos pendientes del préstamo (foto del mutuo firmado + pagaré)
target: kredy
ui: true
acceptance:
  - Un préstamo computa un `documentStatus` a partir de `LoanAttachment`- requiere al menos una foto del mutuo firmado (type "mutual") y una del pagaré (type "pagare"). Si falta alguna → `documentStatus = "pendiente"`; si están ambas → "completo".
  - En la vista del préstamo (al menos en préstamos activos), si `documentStatus = "pendiente"` se muestra un badge/checklist "Pendiente- subir contrato firmado y pagaré", indicando cuál de los dos falta.
  - El AP puede subir ambas fotos REUSANDO el flujo de upload existente de `LoanAttachment` (el mismo que usa `transfer_receipt`), guardándolas con type "mutual" y "pagare" respectivamente.
  - `documentStatus` es INFORMATIVO- NO bloquea pre-aprobar, aprobar ni activar el préstamo. Solo aparece como pendiente.
  - Typecheck, lint y tests pasan sin errores.
---

## Contexto

Cuando un préstamo se activa, el contrato firmado y el pagaré quedan en papel. Queremos que esas fotos se carguen en Kredy para tener registro, mostrándolo como pendiente mientras falten — sin frenar el ciclo del préstamo.

Lo que el loop debe REUSAR:
- `LoanAttachment` (model ya existente) con `type` que ya contempla `"mutual"`, `"pagare"`, `"transfer_receipt"`, `"other"`. Las fotos van acá- no hace falta tabla nueva.
- El flujo de upload que YA existe para `transfer_receipt` (mismo endpoint/almacenamiento)- reusarlo cambiando el `type`. NO crear un mecanismo de storage nuevo.
- La vista de detalle del préstamo donde hoy se ven los attachments / el estado del préstamo.

## Pasos sugeridos (el planner puede refinar)

1. Agregar un helper `getLoanDocumentStatus(loanId | attachments)` que devuelva `{ status: "pendiente" | "completo", missing: ("mutual"|"pagare")[] }` mirando los `LoanAttachment` del préstamo. Typecheck y lint.
2. Exponer `documentStatus` en la query del préstamo (router de loans) sin alterar el resto del payload. Typecheck y lint.
3. En la vista del préstamo activo, renderizar el badge/checklist de pendientes cuando `status = "pendiente"`, mostrando cuál falta. Typecheck y lint.
4. Conectar la subida de las dos fotos al flujo de upload existente de LoanAttachment con type "mutual"/"pagare". Typecheck y lint.
5. Test- con 0/1/2 attachments el `documentStatus` y `missing` son correctos; subir una foto actualiza el estado; el estado NO altera el flujo de aprobación. Tests, typecheck y lint pasan.

## Fuera de alcance

- Hacer el documentStatus bloqueante (es informativo por decisión).
- Recordatorios/notificaciones automáticas al AP por documentos faltantes (follow-up).
- Validación del contenido de la foto (OCR, verificación de firma).
- Cambiar el backend de storage (se reusa el de transfer_receipt).

## Restricciones clave

- NO bloqueante- no debe interferir con pre-aprobar/aprobar/activar.
- Sin migración de schema (LoanAttachment ya existe con los types necesarios).
- Reusar el flujo de upload existente; no introducir un storage nuevo.
- Columnas camelCase sin @map; es ui:true → QA visual cuando haya server.
