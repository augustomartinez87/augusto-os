# Investigación Scout — F-0036

> Generado automáticamente. Evidencia verificada contra filesystem.

## Mapa del repo para: tres-saltenas — /recetas expone jerga interna a Dani + nav sin indicio de que hay más pestañas

**Resumen:** El repositorio tres-saltenas es una app Next.js 16 + tRPC + Prisma para costeo de empanadas. La página /recetas es un placeholder sin implementar, y según SPEC-MVP.md §6 la UI debe usar lenguaje de Dani (nunca jerga contable/técnica). La NavBar tiene 9 links en overflow-x-auto sin indicador visual de scroll, por lo que el usuario no sabe que existen más pestañas (incluyendo Recetas). El schema Prisma expone términos como RecetaItem y cantidadPorUnidad que no deben filtrarse al frontend. Se necesita implementar /recetas con lenguaje coloquial y agregar un indicador de scroll en la NavBar.

### Archivos clave
- `package.json`
- `SPEC-MVP.md`
- `prisma/schema.prisma`
- `prisma/seed.ts`
- `src/app/layout.tsx`
- `src/app/page.tsx`
- `src/app/recetas/page.tsx`
- `src/components/NavBar.tsx`
- `src/server/api/root.ts`
- `src/server/api/trpc.ts`
- `src/server/api/routers/producto.ts`
- `src/lib/costeo.ts`
- `src/lib/insumos.ts`
- `src/lib/db.ts`

### Patrones a seguir
- Next.js App Router con layouts anidados y `use client` en páginas con estado
- tRPC v11: routers en `src/server/api/routers/`, contexto con Clerk auth, `protectedProcedure`
- Prisma ORM + PostgreSQL: schema en `prisma/schema.prisma`, seed en `prisma/seed.ts`
- Clerk auth con `@clerk/nextjs` — `Show when="signed-in"` para condicionalmente mostrar NavBar
- Tailwind CSS v4: clases utilitarias, `@import "tailwindcss"`, diseño mobile-first max-w-lg
- Patrón tRPC query/mutation: `trpc.X.useQuery()`, `trpc.X.useMutation({ onSuccess: () => utils.X.invalidate() })`
- Constantes hardcodeadas para alertas de valores atípicos (PRECIO_TOTAL_ALERTA, CANTIDAD_PRODUCIDA_ALERTA, etc.)
- Helper `parseLocalDate()` duplicado en Compras, Producción y Ventas para evitar bug UTC-3
- Formularios con estado local useState, validación manual con setErrors, sin librería de formularios

### Dependencias relevantes
- next@16.2.10
- react@19.2.4
- @clerk/nextjs@^7.5.14
- @prisma/client@^5.22.0 / prisma@^5.22.0
- @trpc/server@^11.18.0 / @trpc/client@^11.18.0 / @trpc/react-query@^11.18.0
- @tanstack/react-query@^5.101.2
- superjson@^2.2.6
- zod@^4.4.3
- tailwindcss@^4
- typescript@^5

### Riesgos / Restricciones
- La página `/recetas` (src/app/recetas/page.tsx) es un placeholder que solo muestra 'Módulo pendiente' — no tiene UI de receta, expone jerga interna si se dejan nombres de modelo (RecetaItem, cantidadPorUnidad) sin traducir al lenguaje de Dani ('gramos de jamón', 'tapas por empanada')
- SPEC-MVP.md §6 exige explícitamente: 'Lenguaje de la interfaz: nunca costos fijos/variables ni jerga contable. Compras, Producción, Ventas, Gastos, Mi plata — como Dani ya piensa el negocio.' — implementar RecetasItems con nombres técnicos viola esta regla
- NavBar (src/components/NavBar.tsx) usa `overflow-x-auto` sin ningún indicador visual (flecha, fade gradient, dots) de que hay más pestañas — el usuario no sabe que puede scrollear para ver Recetas y otros links
- El seed (prisma/seed.ts) usa IDs como 'receta-insumo-paleta' que son jerga interna del schema, expuestos si se muestran crudos en UI
- La constante PRECIO_UNITARIO_DEFAULT = 1500 y PRODUCTO_ID = 'empanada-jyq' están hardcodeadas en Ventas — si cambian hay que modificar el código

### Evidencia verificada
- **`src/app/recetas/page.tsx`** — `RecetasPage` (líneas 1-7, confianza 100%): Página /recetas: placeholder que dice 'Módulo pendiente' — no implementa nada, es donde se expondría jerga interna si se usan nombres de modelo sin traducción.
- **`src/components/NavBar.tsx`** — `NAV_LINKS` (líneas 5-16, confianza 100%): Array con 9 links de navegación. El último es '/recetas'. La nav usa overflow-x-auto sin indicador visual de que hay más elementos scrolleables.
- **`src/components/NavBar.tsx`** — `overflow-x-auto` (líneas 24, confianza 100%): Contenedor con overflow-x-auto pero sin fade, flecha ni dots que indiquen scroll horizontal — el usuario no sabe que hay más pestañas.
- **`SPEC-MVP.md`** — `Lenguaje de la interfaz` (líneas 95-96, confianza 100%): Especificación explícita: 'nunca costos fijos/variables ni jerga contable. Compras, Producción, Ventas, Gastos, Mi plata — como Dani ya piensa el negocio.'
- **`prisma/schema.prisma`** — `RecetaItem` (líneas 46-53, confianza 100%): Modelo RecetaItem con campos productoId, insumoId, cantidadPorUnidad — jerga técnica que no debe filtrarse al frontend de Dani.
- **`prisma/seed.ts`** — `receta-insumo-paleta` (líneas 31-37, confianza 0%): [NO VERIFICADO] Seed crea recetaItems con IDs como 'receta-insumo-paleta' — expone jerga interna de base de datos si se muestra en UI.
- **`src/app/layout.tsx`** — `NavBar` (líneas 18, confianza 100%): Root layout renderiza NavBar condicionalmente con Show when='signed-in' — define la estructura global de navegación.
- **`src/app/ventas/page.tsx`** — `PRODUCTO_ID` (líneas 6, confianza 100%): Constante hardcodeada 'empanada-jyq' usada al crear ventas — acopla la UI al ID de seed.
- **`SPEC-MVP.md`** — `Receta (fija, sin variación) — empanada JyQ` (líneas 29-36, confianza 100%): Spec detalla la receta real: paleta 24g, queso 34g, tapa 1 unidad — estos valores debe mostrar la UI, no los nombres internos del schema.
- **`src/server/api/root.ts`** — `appRouter` (líneas 1-19, confianza 100%): Router tRPC que registra todos los sub-routers. No hay un router 'receta' — confirma que el módulo Recetas no está implementado del lado servidor.

## Investigar qué existe para implementar: tres-saltenas — /recetas expone jerga interna a Dani + nav sin indicio de que hay más pestañas

**Resumen:** El repo tiene dos problemas claros para la tarea: (1) la página /recetas es un stub que muestra texto interno ('Módulo pendiente — ver features F-0013..F-0018') violando la regla de SPEC-MVP §6 de no usar jerga técnica con Dani; (2) el NavBar tiene 9 enlaces con overflow-x-auto pero ningún indicador visual (fade, gradiente, flecha) que señale que hay más pestañas scrolleables. Ya existen los tipos RecetaItemProduccion/RecetaItemConCosto/RecetaItemConStock, el helper toUnidadNativa, y el modelo Prisma RecetaItem con datos precargados que la UI de /recetas debería consumir limpiamente, usando el lenguaje que Dani entiende (sin 'costoMOD', 'margenPct', etc.).

### Archivos clave
- `src/components/NavBar.tsx`
- `src/app/recetas/page.tsx`
- `src/app/layout.tsx`
- `src/lib/produccion.ts`
- `src/lib/lotes.ts`
- `src/lib/costeo.ts`
- `prisma/schema.prisma`
- `SPEC-MVP.md`
- `src/app/page.tsx`
- `src/app/mi-plata/page.tsx`
- `src/app/gastos/page.tsx`

### Patrones a seguir
- NavBar con NAV_LINKS array de 9 rutas, overflow-x-auto + flex + shrink-0 + whitespace-nowrap sin indicador visual de scroll extra
- Páginas modulares con 'use client', layout max-w-lg mx-auto p-4, título h1 semántico, y patrón de KpiCard
- trpc hooks (useQuery, useMutation) con invalidación en onSuccess
- Dashboard con selector de período (hoy/semana/mes) usando botones con estado activo bg-blue-600
- formatARS para formato de moneda ARS con Intl.NumberFormat

### Dependencias relevantes
- next/navigation (usePathname)
- next/link (Link)
- @clerk/nextjs (ClerkProvider, Show)
- @/trpc/react (TRPCProvider, trpc)
- @prisma/client
- React useState/usePathname para estado de UI

### Riesgos / Restricciones
- La página /recetas actualmente expone texto de desarrollador ('Módulo pendiente — ver features F-0013..F-0018 en augusto-os.') directamente al usuario Dani
- NavBar tiene 9 ítems y usa overflow-x-auto sin ningún indicador visual (fade, gradient, sombra) que advierta al usuario que hay más pestañas fuera del viewport
- SPEC-MVP.md §6 exige explícitamente: 'nunca costos fijos/variables ni jerga contable. ...como Dani ya piensa el negocio' — cualquier implementación de /recetas debe evitar términos como costoMateriaPrima, costoMOD, costoOperativo, margenPct
- El modelo RecetaItem en Prisma ya existe (3 filas fijas: paleta 24g, queso 34g, tapa 1u) pero la UI de /recetas es un stub vacío — implementar sin exponer jerga interna es el desafío

### Evidencia verificada
- **`src/components/NavBar.tsx`** — `NAV_LINKS` (líneas 6-15, confianza 100%): Array con los 9 enlaces de navegación. No hay ningún mecanismo que indique visualmente que hay más pestañas de las que se ven en el viewport al hacer scroll horizontal.
- **`src/components/NavBar.tsx`** — `overflow-x-auto` (líneas 21, confianza 100%): Clase CSS que permite scroll horizontal pero sin ningún fade/gradient/indicador que señale al usuario que hay contenido oculto a los lados.
- **`src/app/recetas/page.tsx`** — `RecetasPage` (líneas 1-6, confianza 100%): Página stub de /recetas que muestra texto interno de desarrollo ('Módulo pendiente — ver features F-0013..F-0018 en augusto-os.') — jerga interna expuesta directamente a Dani.
- **`SPEC-MVP.md`** — `§6. Módulos MVP` (líneas 85-88, confianza 0%): [NO VERIFICADO] Especifica explícitamente que el lenguaje de la interfaz nunca debe usar jerga contable ('costos fijos/variables') y debe usar términos que Dani ya usa: Compras, Producción, Ventas, Gastos, Mi plata.
- **`src/app/layout.tsx`** — `NavBar` (líneas 19, confianza 100%): Root layout que monta NavBar condicionalmente cuando el usuario está autenticado (Clerk Show signed-in). Es el punto único de entrada del NavBar.
- **`src/lib/produccion.ts`** — `RecetaItemProduccion` (líneas 3-7, confianza 100%): Tipo existente que modela los ítems de una receta (insumoId, cantidadPorUnidad, unidad). Reusable para la UI de /recetas.
- **`src/lib/lotes.ts`** — `toUnidadNativa` (líneas 3-5, confianza 100%): Helper de conversión de unidades (kg → gramos). Clave si la UI de /recetas muestra cantidades en gramos/unidades.
- **`prisma/schema.prisma`** — `RecetaItem` (líneas 33-40, confianza 100%): Modelo Prisma que asocia Producto con Insumo y cantidadPorUnidad. Ya existen las 3 filas fijas (paleta 24g, queso 34g, tapa 1u) listas para ser mostradas en la UI de /recetas.
- **`src/app/page.tsx`** — `formatARS` (líneas 10-16, confianza 100%): Formateador de moneda ARS reutilizado en varias páginas. Patrón a seguir para cualquier display de costos en /recetas.
- **`src/app/page.tsx`** — `KpiCard` (líneas 18-33, confianza 100%): Componente de tarjeta KPI reutilizado en dashboard y mi-plata. Patrón de UI a seguir si /recetas muestra métricas.
- **`src/lib/costeo.ts`** — `calcularCostoMateriaPrima` (líneas 20-26, confianza 0%): [NO VERIFICADO] [NO VERIFICADO] Función que calcula costo de materia prima desde RecetaItemConCosto. No se confirmó si está expuesta al frontend por tRPC pero es la base lógica que /recetas podría usar.

## Riesgos y restricciones técnicas para: tres-saltenas — /recetas expone jerga interna a Dani + nav sin indicio de que hay más pestañas

**Resumen:** La página /recetas es un placeholder que expone jerga interna del desarrollo (augusto-os, F-0013..F-0018) al usuario final Dani, lo que es confuso y poco profesional. La NavBar tiene 9 pestañas con overflow-x-auto pero cero indicadores visuales de que hay más opciones fuera del viewport — en mobile Dani no sabe que existen módulos como Gastos, Retiros, Mi Plata o Recetas. No existe un recetaRouter dedicado (se accede indirectamente desde loteRouter/dashboardRouter). Además, Prisma 5.x está pineado, la auth está incompleta (middleware.ts mínimo), y la regla eslint react-hooks/set-state-in-effect puede bloquear formularios editables si no se usa el patrón key={} en vez de useEffect.

### Archivos clave
- `src/app/recetas/page.tsx`
- `src/components/NavBar.tsx`
- `src/server/api/routers/lote.ts`
- `src/server/api/routers/dashboard.ts`
- `src/server/api/root.ts`
- `prisma/schema.prisma`
- `prisma/seed.ts`
- `src/lib/lotes.ts`
- `src/lib/produccion.ts`
- `src/app/layout.tsx`
- `package.json`
- `SESSION-HANDOFF.md`

### Patrones a seguir
- NavBar: usar overflow-x-auto sin fade/gradient/chevron indicador de scroll horizontal
- /recetas: placeholder con jerga interna (augusto-os, F-0013..F-0018) expuesta al usuario final Dani
- RecetaItem no tiene router propio; se accede vía loteRouter y dashboardRouter con Prisma directo
- PRODUCTO_ID hardcodeado como 'empanada-jyq' en dashboard.ts y ventas/page.tsx
- parseLocalDate() replica el mismo patrón en 4 páginas (Compras, Ventas, Producción, Gastos/Retiros)
- protectedProcedure en todos los routers pero middleware.ts sin auth.protect() por ruta
- Prisma 5.x pineado — npm install prisma@latest rompe schema

### Dependencias relevantes
- @prisma/client@^5.22.0
- prisma@^5.22.0
- next@16.2.10
- eslint-config-next@16.2.10
- @clerk/nextjs@^7.5.14
- @trpc/server@^11.18.0
- @trpc/react-query@^11.18.0
- zod@^4.4.3

### Riesgos / Restricciones
- La página /recetas muestra 'Módulo pendiente — ver features F-0013..F-0018 en augusto-os' que expone jerga interna de desarrollo (nombres de features, sistema orchestrador) a Dani, un usuario no técnico — riesgo de confusión y pérdida de confianza
- NavBar tiene 9 items con overflow-x-auto pero sin ningún indicador visual (fade, gradiente, sombra, chevron, 'más...') de que hay elementos fuera del viewport — en mobile (max-w-lg) sólo entran ~4-5 pestañas, el usuario no sabe que existen Recetas, Mi Plata, Retiros, Gastos
- No existe recetaRouter en src/server/api/root.ts — la receta se accede indirectamente desde loteRouter y dashboardRouter. Implementar la UI real de /recetas requerirá decidir si crear un router nuevo o reusar el acceso a RecetaItem vía Prisma desde el cliente
- PRODUCTO_ID hardcodeado ('empanada-jyq') en dashboard.ts y ventas/page.tsx — frágil ante cambios en el seed o multi-producto futuro
- Auth no está consolidada: middleware.ts usa clerkMiddleware() mínimo sin route protection, pero todos los routers usan protectedProcedure. Insufficiente para datos sensibles como costoUnitarioActual
- eslint-config-next 16 tiene regla react-hooks/set-state-in-effect que ya bloqueó F-0014 step 6 — cualquier sincronización de estado local con props en formularios de recetas debe usar key={} en vez de useEffect
- Prisma 5.x está pineado; npm install prisma@latest rompe schema (datasource url/directUrl no compatibles con v7). Cualquier migración/prisma generate requiere mantener versión
- Las páginas Compras, Ventas, Producción, Gastos, Retiros ya tienen implementación completa con formularios y listados — /recetas es el único placeholder vacío, creando asimetría de funcionalidad y experiencia de navegación incompleta

### Evidencia verificada
- **`src/app/recetas/page.tsx`** — `RecetasPage` (líneas 1-7, confianza 100%): Placeholder de /recetas que expone la jerga interna 'ver features F-0013..F-0018 en augusto-os' al usuario final Dani — debe reemplazarse con UI real de consulta de receta (3 líneas fijas: paleta 24g, queso 34g, tapa 1u).
- **`src/components/NavBar.tsx`** — `NAV_LINKS` (líneas 4-14, confianza 100%): Array con 9 rutas de navegación. Con overflow-x-auto en un contenedor max-w-lg, en mobile solo se ven ~4-5 pestañas (Dashboard, Ventas, Compras, Producción, quizás Insumos). No hay fade/gradiente/chevron ni texto 'más...' que indique la existencia de las restantes (Gastos, Retiros, Mi Plata, Recetas).
- **`src/components/NavBar.tsx`** — `overflow-x-auto` (líneas 20, confianza 100%): Único mecanismo de scroll horizontal en la navbar. Sin indicador visual de que hay contenido oculto. Los links tienen shrink-0 y whitespace-nowrap, por lo que no se adaptan al ancho disponible.
- **`src/server/api/root.ts`** — `appRouter` (líneas 1-18, confianza 100%): Router raíz de tRPC. No existe recetaRouter — solo compra, insumo, producto, lote, venta, gasto, retiro, config, dashboard. La receta se accede indirectamente vía loteRouter (findMany RecetaItem) y dashboardRouter (producto.recetaItems).
- **`src/server/api/routers/dashboard.ts`** — `PRODUCTO_ID` (líneas 15, confianza 100%): PRODUCTO_ID hardcodeado como 'empanada-jyq' — frágil ante cambios en el seed o migración a multi-producto.
- **`src/app/layout.tsx`** — `Show` (líneas 15, confianza 100%): NavBar se renderiza solo cuando el usuario está signed-in (Clerk). Dani verá la navbar con el problema de overflow invisible apenas se loguee.
- **`prisma/schema.prisma`** — `RecetaItem` (líneas 51-56, confianza 100%): Modelo RecetaItem: productoId, insumoId, cantidadPorUnidad (Float). 3 filas fijas seedeadas (paleta 24g, queso 34g, tapa 1u). No hay router dedicado; la UI futura de /recetas puede leer directo desde aquí.
- **`prisma/seed.ts`** — `recetaItem.upsert` (líneas 32, confianza 70%): [NO VERIFICADO] Seed de las 3 recetas fijas. Corrido contra Neon dev. Si se modifica la receta (ej. Dani ajusta gramajes), el seed debe actualizarse.
- **`SESSION-HANDOFF.md`** — `react-hooks/set-state-in-effect` (líneas 50, confianza 90%): Regla de eslint-config-next 16 que ya bloqueó F-0014 step 6. Prohíbe useEffect(() => setState(prop), [prop]) para sincronizar estado local con props — usar key={} en su lugar. Aplica a cualquier formulario editable en el futuro módulo de recetas.
- **`package.json`** — `prisma` (líneas 29, confianza 100%): Prisma pineado a ^5.22.0 en dependencies. npm install prisma@latest rompe schema (v7 requiere prisma.config.ts + driver adapters). Cualquier migración debe mantenerse en 5.x.
