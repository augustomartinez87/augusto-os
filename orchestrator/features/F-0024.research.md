# Investigación Scout — F-0024

> Generado automáticamente. Evidencia verificada contra filesystem.

## Mapa del repo para: Argos mobile — Reorden del Overview (allocation y destacadas above the fold, KPIs al final)

**Resumen:** El repo es una app React 18 + Vite + Tailwind CSS de portfolio tracker llamada 'Argos'. Los componentes clave para el reorden mobile del Overview ya están implementados (PortfolioHeroChart con chart minimal, AllocationPanel con donut+leyenda, MobilePositionsList con variant='featured', MobileHeader con ojo+settings). El archivo central es DashboardOverview.jsx, donde el layout mobile actual tiene orden Hero → KPIs → Allocation → Posiciones, y debe reordenarse a Hero → Allocation → Posiciones Destacadas → KPIs (al final). Desktop permanece intacto dentro de bloques `hidden md:`. Los invariantes principales son: censura de saldos obligatoria vía useBalanceVisibility/MoneyValue, colores restringidos al palette TEAL/CORAL/PERIWINKLE, touch targets ≥ 44px, y Recharts con isAnimationActive={false}.

### Archivos clave
- `SPEC-mobile-overview-redesign.md`
- `package.json`
- `src/features/portfolio/components/DashboardOverview.jsx`
- `src/features/portfolio/components/PortfolioHeroChart.jsx`
- `src/features/portfolio/components/AllocationPanel.jsx`
- `src/features/portfolio/components/MobilePositionsList.jsx`
- `src/components/common/MobileHeader.jsx`
- `src/components/common/MobileNav.jsx`
- `src/components/ui/KpiCard.jsx`
- `src/components/ui/MoneyValue.jsx`
- `src/components/ui/index.js`
- `src/hooks/useBalanceVisibility.js`

### Patrones a seguir
- Mobile/Desktop split via `md:hidden` / `hidden md:` Tailwind classes en cada componente — todo cambio mobile dentro de `md:hidden`, desktop intacto
- `variant` prop pattern: DashboardOverview recibe `variant='full'|'positions'`; MobilePositionsList recibe `variant='featured'|'grouped'`
- `renderChart(gradId, { minimal })` parametrizado en PortfolioHeroChart — `minimal=true` suprime CartesianGrid, XAxis, YAxis para mobile
- Balance visibility vía `useBalanceVisibility()` hook + `MoneyValue`/`maskMoney` — todo valor monetario pasa por este patrón, nunca crudo
- CSS custom tokens `--surface-*`, `--border`, `--teal`, `--ink*`, `--teal-dim` — sin colores hardcodeados nuevos fuera de TEAL #2FD4CD / CORAL #E5616A / PERIWINKLE #5F89DD
- Touch targets ≥ 44px via `min-h-[44px]` / `min-w-[44px]`
- Recharts con `isAnimationActive={false}` en todas las series
- Donut con centro: `innerRadius={32}` `outerRadius={46}` + `<div>` absolute centrado para contador en AllocationPanel

### Dependencias relevantes
- recharts (^2.10.0) — ComposedChart, Area, Line, PieChart, Pie, Cell, Tooltip, ResponsiveContainer, YAxis
- @tanstack/react-query (^5.90.19) — data fetching usado por servicios de precios
- lucide-react (^0.263.1) — iconos Eye, EyeOff, Settings, TrendingUp, DollarSign, Activity, Calendar, Plus, Layers, ChevronRight, MoreHorizontal, LayoutDashboard, Wallet, PieChart, Zap
- react-router-dom (^6.20.0) — navegación y links
- tailwindcss (^3.3.2) — clases utilitarias y responsive (md:hidden, hidden md:)
- date-fns (^4.1.0) — formateo de fechas
- decimal.js (^10.6.0) — precisión decimal financiera
- @supabase/supabase-js (^2.39.0) — backend/auth

### Riesgos / Restricciones
- Desktop NO debe tocarse — todo cambio va dentro de bloques `md:hidden`; hay que verificar que no haya regresiones en desktop
- Censura de saldos obligatoria: ningún módulo puede renderizar montos crudos, siempre via `MoneyValue` / `maskMoney` / `useBalanceVisibility`
- Colores hardcodeados prohibidos — solo TEAL #2FD4CD, CORAL #E5616A, PERIWINKLE #5F89DD y tokens CSS existentes
- Touch targets deben mantener ≥ 44px en todos los botones mobile
- Recharts `isAnimationActive={false}` debe mantenerse en todas las series (como está hoy)
- Al mover el ojo (censura) de PortfolioHeroChart a MobileHeader, coordinar con M2 para no duplicar ni perder funcionalidad
- El reorden en DashboardOverview.jsx debe mantener la variante 'positions' intacta (solo tocar el bloque mobile de variant='full')
- MiniSpark (72×28 área con gradiente) debe compartirse entre featured y la vista agrupada actual — verificar que no rompa el sparkline existente en MobilePositionsList

### Evidencia verificada
- **`SPEC-mobile-overview-redesign.md`** — `M8 — Reorden del Overview mobile` (líneas 127-141, confianza 100%): Define el orden final: Hero → AllocationPanel → Posiciones Destacadas → KPI carousel al final. Es el objetivo central del reorden.
- **`src/features/portfolio/components/DashboardOverview.jsx`** — `DashboardOverview` (líneas 80-180, confianza 100%): Componente principal del Overview. Contiene el layout mobile actual (Hero → KPI carousel → Allocation → Posiciones). Es el archivo a modificar para M8.
- **`src/features/portfolio/components/DashboardOverview.jsx`** — `variant === 'positions'` (líneas 111-136, confianza 100%): Variante 'positions' NO debe tocarse. Renderiza MobilePositionsList con showMax=50 en mobile, GroupedPositionsTable en desktop.
- **`src/features/portfolio/components/DashboardOverview.jsx`** — `kpiItems` (líneas 97-103, confianza 100%): Array de 4 KPIs (Invertido, Valuación, P&L All Time, P&L Hoy) que actualmente se renderiza antes que AllocationPanel. Debe moverse al final en el reorden.
- **`src/features/portfolio/components/PortfolioHeroChart.jsx`** — `renderChart` (líneas 129-168, confianza 100%): Firma `renderChart(gradId, { minimal })` — con minimal=true suprime CartesianGrid, XAxis, YAxis para mobile (M1). Ya implementado.
- **`src/features/portfolio/components/PortfolioHeroChart.jsx`** — `mobileEl` (líneas 175-265, confianza 100%): Bloque mobile del hero. Contiene label 'Valor del Portfolio', valor text-[34px], P&L, LiveDot centrado, chart full-bleed 200px, period chips (M1+M2+M3). Ya implementado.
- **`src/features/portfolio/components/AllocationPanel.jsx`** — `AllocationPanel` (líneas 93-180, confianza 100%): Donut con centro (posCount + 'pos.'), leyenda coloreada por slice, toggle tipo/estrategia (M4). Layout donut left ~100px + legend right. Ya implementado.
- **`src/features/portfolio/components/AllocationPanel.jsx`** — `posCount` (líneas 91-97, confianza 100%): Contador de posiciones (excluye CASH) usado en el centro del donut. variable derivada de positions.filter.
- **`src/features/portfolio/components/MobilePositionsList.jsx`** — `variant === 'featured'` (líneas 125-210, confianza 100%): Bloque variant='featured' (M6): top-5 flat por valuación, con AssetLogo (36px), MiniSpark (área con gradiente), MoneyValue, % coloreado, link 'Ver todas'. Ya implementado.
- **`src/features/portfolio/components/MobilePositionsList.jsx`** — `featuredPositions` (líneas 117-124, confianza 100%): useMemo que filtra y ordena top-5 por valuación (currency-aware). Usa defaultTypeOf para excluir cash.
- **`src/features/portfolio/components/MobilePositionsList.jsx`** — `MiniSpark` (líneas 23-42, confianza 100%): Componente de sparkline área con gradiente (72×28). Color TEAL o CORAL según signo del último punto. strokeWidth 2.5, gradiente 0%→100%. Compartido entre featured y grouped.
- **`src/components/common/MobileHeader.jsx`** — `MobileHeader` (líneas 15-80, confianza 100%): Header mobile: avatar, logo centrado, ojo (Eye/EyeOff via toggleBalance), settings (solo admin). Row 2 con PortfolioSelector y toggle ARS|USD. (M5) Ya implementado.
- **`src/components/common/MobileNav.jsx`** — `MobileNav` (líneas 17-100, confianza 100%): Bottom nav con 4 ítems fijos + 'Más'. Estado activo usa color 'var(--teal)' vs 'var(--ink-faint)'. Falta implementar el pill activo (M7) — actualmente solo cambia color.
- **`src/hooks/useBalanceVisibility.js`** — `useBalanceVisibility` (líneas 8-30, confianza 100%): Hook que retorna [hidden, toggle] con persistencia en localStorage y sincronización cross-component via StorageEvent. Patrón obligatorio para censura.
- **`src/components/ui/MoneyValue.jsx`** — `MoneyValue` (líneas 12-18, confianza 100%): Componente canónico que lee useBalanceVisibility internamente y muestra HIDDEN ('••••••') si hidden=true. Debe usarse para todo valor monetario.
- **`src/components/ui/MoneyValue.jsx`** — `maskMoney` (líneas 7-9, confianza 100%): Helper puro para contextos no-JSX: maskMoney(formatted, hidden) → HIDDEN o string original.
- **`package.json`** — `recharts` (líneas 16, confianza 100%): Dependencia recharts ^2.10.0 instalada. Usada para ComposedChart, AreaChart, PieChart, etc. en hero y allocation.
- **`src/components/ui/KpiCard.jsx`** — `KpiCard` (líneas 74-115, confianza 100%): Componente KPI canónico con dos modos: default (accent color, badge, sparkline) y metric (left-border variant). Usa useBalanceVisibility internamente.

## Investigar qué existe para implementar: Argos mobile — Reorden del Overview (allocation y destacadas above the fold, KPIs al final)

**Resumen:** La implementación del reorden del Overview mobile requiere modificar exclusivamente `DashboardOverview.jsx`: mover el bloque KPI carousel (actualmente entre Hero y AllocationPanel) al final del layout mobile, debajo de MobilePositionsList. El resto de los componentes ya está implementado según los mockups: PortfolioHeroChart.jsx ya tiene el hero minimal (M1-M3), AllocationPanel.jsx ya tiene el donut con centro y leyenda coloreada (M4), MobilePositionsList.jsx ya soporta variant='featured' con top-5 y MiniSpark de área (M6), MobileHeader.jsx ya tiene ojo+settings (M5). MobileNav.jsx aún necesita el pill activo (M7) pero es un cambio aislado. Desktop layout no debe tocarse.

### Archivos clave
- `SPEC-mobile-overview-redesign.md`
- `src/features/portfolio/components/DashboardOverview.jsx`
- `src/features/portfolio/components/PortfolioHeroChart.jsx`
- `src/features/portfolio/components/AllocationPanel.jsx`
- `src/features/portfolio/components/MobilePositionsList.jsx`
- `src/components/common/MobileHeader.jsx`
- `src/components/common/MobileNav.jsx`
- `src/hooks/useBalanceVisibility.js`
- `src/features/portfolio/hooks/useSparklines.js`
- `src/pages/Dashboard.jsx`
- `src/features/portfolio/components/GroupedPositionsTable.jsx`
- `src/components/ui/MoneyValue.jsx`
- `src/components/ui/KpiCard.jsx`

### Patrones a seguir
- Bloques md:hidden / hidden md:* para separar layouts mobile y desktop en el mismo componente
- renderChart(gradId, { minimal: true }) con parámetro minimal para variante mobile del hero chart
- variant='featured' vs variant='grouped' en MobilePositionsList para cambiar entre lista destacada plana y agrupada
- kpiItems array compartido que se renderiza como carousel en mobile y grid en desktop
- useBalanceVisibility() + MoneyValue/maskMoney/HIDDEN para censura de saldos en todos los montos
- PieChart con innerRadius/outerRatio + texto centrado con posCount y 'pos.' para donut de asignación
- MiniSpark con área con gradiente para sparklines de posiciones destacadas
- sparklineFor callback (useCallback en Dashboard.jsx) que mapea ticker → array de precios

### Dependencias relevantes
- recharts (ComposedChart, Area, Pie, PieChart, Cell, ResponsiveContainer, Tooltip)
- lucide-react (Eye, EyeOff, Settings, TrendingUp, DollarSign, Activity, Calendar, ChevronRight, Plus, Layers)
- @/components/ui (MoneyValue, maskMoney, HIDDEN, KpiCard)
- @/utils/formatters (formatARS, formatUSD, formatNumber)
- @/hooks/useBalanceVisibility
- @/features/portfolio/hooks/useSparklines
- @/features/portfolio/services/priceService (usePrices)
- @/components/common/AssetLogo
- @/components/common/BottomSheet

### Riesgos / Restricciones
- El orden actual en DashboardOverview.jsx mobile (lines 198-250) es: Hero → KPI carousel → AllocationPanel → separator → MobilePositionsList(featured). Para el nuevo orden (KPIs al final) hay que mover el bloque KPI carousel debajo de MobilePositionsList y verificar que los px-4/pb-2 no generen padding duplicado.
- MobileNav.jsx (M7) aún NO tiene el pill activo con background teal-dim; solo cambia el color del texto. Es implementación pendiente.
- MobileHeader.jsx (M5) YA tiene ojo + settings en el grupo derecho. No re-implementar, solo verificar que el ojo no siga también en PortfolioHeroChart (M2 pide quitarlo de ahí si está).
- Desktop layout (hidden md:flex) NO debe modificarse ni reordenarse; cualquier cambio en DashboardOverview.jsx debe estar dentro del div className='md:hidden'.
- El KPI carousel y el AllocationPanel ya están implementados con el diseño M3/M4 del mockup — no redebuggear, solo reordenar.
- Los import de DashboardOverview incluyen GroupedPositionsTable y BottomSheet que no se usan en mobile variant='full' pero se usan en variant='positions' — no eliminar.

### Evidencia verificada
- **`SPEC-mobile-overview-redesign.md`** — `M8 — Reorden del Overview mobile` (líneas 124-134, confianza 100%): Spec detallada del orden final: 1. Hero 2. AllocationPanel 3. Posiciones Destacadas 4. KPI carousel 5. FAB. Es el plano de lo que hay que implementar.
- **`src/features/portfolio/components/DashboardOverview.jsx`** — `DashboardOverview` (líneas 106-250, confianza 100%): Componente principal del overview. La sección mobile (md:hidden) lines 198-250 contiene el orden ACTUAL: Hero → KPI carousel → AllocationPanel → separator → MobilePositionsList(featured). Es el archivo a modificar para reordenar.
- **`src/features/portfolio/components/DashboardOverview.jsx`** — `kpiItems` (líneas 131-136, confianza 100%): Array de 4 KPIs (Invertido, Valuación, P&L All Time, P&L Hoy) definido con label/value/color/icon. Se renderiza como carousel en mobile y grid en desktop. Debe moverse al final en mobile pero permanecer donde está en desktop.
- **`src/features/portfolio/components/DashboardOverview.jsx`** — `MobileActionFab` (líneas 13-61, confianza 100%): FAB flotante para nueva operación/FCI. Debe quedar al final del layout mobile, sin cambios.
- **`src/features/portfolio/components/PortfolioHeroChart.jsx`** — `mobileEl` (líneas 183-256, confianza 100%): Layout mobile del hero chart. Ya implementa M1+M2: label → valor(34px) → P&L+badge → LiveDot → chart(200px) → period chips (justify-between) → firma chips. renderChart con minimal=true sin ejes/grilla.
- **`src/features/portfolio/components/PortfolioHeroChart.jsx`** — `renderChart` (líneas 120-156, confianza 100%): renderChart(gradId, { minimal }) parametrizado con minimal=true para mobile (sin CartesianGrid, XAxis, YAxis; gradiente con 3 stops; strokeWidth 2.5; margin 4/0/0/0).
- **`src/features/portfolio/components/AllocationPanel.jsx`** — `AllocationPanel` (líneas 49-170, confianza 100%): Ya implementa M4: donut con centro (posCount + 'pos.'), leyenda coloreada con % del color del slice, toggle tipo/estrategia, StatBar de objetivo USD. innerRadius 32, outerRadius 46, paddingAngle 3, cornerRadius 4.
- **`src/features/portfolio/components/MobilePositionsList.jsx`** — `variant` (líneas 111, 152-190, confianza 100%): Soporta variant='featured' (top-5 planas por valuación) y variant='grouped' (agrupadas con toggle). featured ya renderiza header 'Posiciones Destacadas' + 'Ver todas' + lista plana con AssetLogo(36px), MiniSpark(área), MoneyValue, PositionDetailSheet.
- **`src/features/portfolio/components/MobilePositionsList.jsx`** — `MiniSpark` (líneas 15-31, confianza 100%): Componente de sparkline tipo área con gradiente (72×28), coloreado por signo (TEAL/CORAL). Usa AreaChart de recharts. Ya está en el formato que pide M6.
- **`src/features/portfolio/components/MobilePositionsList.jsx`** — `featuredPositions` (líneas 152-160, confianza 100%): useMemo que filtra cash, ordena por valuación descendente y toma top 5. Lógica correcta para M6.
- **`src/components/common/MobileHeader.jsx`** — `MobileHeader` (líneas 18-91, confianza 100%): Ya implementa M5: row 1 con avatar · logo · [ojo, settings], row 2 con portfolio selector + ARS/USD toggle. Ojo usa useBalanceVisibility, settings gatea con isAdmin.
- **`src/components/common/MobileNav.jsx`** — `MobileNav` (líneas 30-89, confianza 100%): Bottom nav actual con 4 items pinned + 'Más'. M7 (pill activo) NO está implementado: el ítem activo solo cambia color a var(--teal), sin pill background. Requiere modificación.
- **`src/hooks/useBalanceVisibility.js`** — `useBalanceVisibility` (líneas 1-33, confianza 100%): Hook que retorna [hidden, toggle]. Usa localStorage + StorageEvent para sincronizar múltiples consumer sin Context. Es la fuente de verdad de censura.
- **`src/features/portfolio/components/GroupedPositionsTable.jsx`** — `TYPE_GROUPS` (líneas 15-21, confianza 100%): Definiciones de grupos (acciones, cedears, bonos, fcis, cash) compartidas con MobilePositionsList. Única fuente de verdad para clasificación.
- **`src/features/portfolio/components/GroupedPositionsTable.jsx`** — `defaultTypeOf` (líneas 35-43, confianza 100%): Función clasificadora de tipo de activo, importada por MobilePositionsList y AllocationPanel para mantener consistencia.
- **`src/features/portfolio/components/GroupedPositionsTable.jsx`** — `defaultStrategyOf` (líneas 45-52, confianza 100%): Función clasificadora de estrategia (core1/core2/satelite), compartida entre componentes.
- **`src/pages/Dashboard.jsx`** — `sparklineFor` (líneas 157, confianza 100%): useCallback que mapea ticker → sparkline, pasada como prop a DashboardOverview. Es la conexión entre useSparklines y los componentes visuales.
- **`src/pages/Dashboard.jsx`** — `DashboardOverview` (líneas 374-397, confianza 100%): Call-site que instancia DashboardOverview con variant='full' y variant='positions'. Pasa sparklineFor, targetUsd, filteredPositions, etc.
- **`src/features/portfolio/hooks/useSparklines.js`** — `useSparklines` (líneas 1-79, confianza 100%): Hook que descarga precios históricos de data912, back-ajusta por splits, y retorna mapa ticker→prices para sparklines. Cachea en localStorage (TTL 24h).
- **`src/components/ui/MoneyValue.jsx`** — `MoneyValue` (líneas 15-19, confianza 100%): Componente que envuelve children con HIDDEN cuando balanceHidden=true. Todos los montos deben pasar por acá.
- **`src/components/ui/MoneyValue.jsx`** — `HIDDEN` (líneas 3, confianza 100%): Constante '••••••' exportada para usar en contextos no-JSX (aria-label, title, etc.).

## Riesgos y restricciones técnicas para: Argos mobile — Reorden del Overview (allocation y destacadas above the fold, KPIs al final)

**Resumen:** El reorden del Overview mobile (M8) es un cambio puramente estructural en DashboardOverview.jsx: mover el KPI carousel al final, después de AllocationPanel y MobilePositionsList. Varios módulos involucrados (M1-M7) ya están parcial o totalmente implementados en el código actual, lo que reduce el riesgo pero exige verificar padding consistente entre secciones y que el above-the-fold (390×844) muestre valor + chart + períodos + inicio de asignación. No hay tests unitarios que puedan romperse, pero tampoco hay cobertura de regresión. El riesgo principal es la acumulación de padding entre secciones al reordenar y la posible incompletitud del pill activo en MobileNav si se incluye en el mismo deploy.

### Archivos clave
- `src/features/portfolio/components/DashboardOverview.jsx`
- `src/features/portfolio/components/PortfolioHeroChart.jsx`
- `src/features/portfolio/components/AllocationPanel.jsx`
- `src/features/portfolio/components/MobilePositionsList.jsx`
- `src/components/common/MobileHeader.jsx`
- `src/components/common/MobileNav.jsx`
- `src/components/ui/KpiCard.jsx`
- `src/features/portfolio/hooks/useSparklines.js`

### Patrones a seguir
- Todo bloque mobile dentro de <div className="md:hidden ..."> para no afectar desktop
- Valores monetarios siempre pasan por MoneyValue / useBalanceVisibility / maskMoney
- Touch targets ≥ 44px con min-h-[44px] y min-w-[44px]
- Recharts con isAnimationActive={false} en todas las series
- CSS tokens: --surface-*, --border, --teal, --ink*, --teal-dim; sin colores hardcodeados fuera de TEAL #2FD4CD / CORAL #E5616A / PERIWINKLE #5F89DD
- sparklineFor recibe un callback (ticker => number[]) para evitar descargas masivas

### Dependencias relevantes
- recharts (ComposedChart, Area, Pie, PieChart, Cell, ResponsiveContainer)
- lucide-react (TrendingUp, DollarSign, Activity, Calendar, Eye, EyeOff, Settings)
- @/hooks/useBalanceVisibility
- @/components/ui/MoneyValue
- @/components/ui/KpiCard
- @/utils/formatters (formatARS, formatUSD, formatNumber)
- @/features/portfolio/hooks/useSparklines

### Riesgos / Restricciones
- Padding duplicado entre secciones al reordenar: KPI carousel (px-4 pt-4 pb-2), AllocationPanel (px-4 pb-2), MobilePositionsList (padding propio en mx-4) — puede generar espacios inconsistentes al cambiar el orden
- MobilePositionsList ya implementa variant='featured' (top-5 por valuación) — verificar que no haya duplicación de lógica si se mueve
- AllocationPanel ya tiene donut con contador central y leyenda coloreada — la SPEC describe como tarea nueva, pero el código ya la implementa; riesgo de refactor innecesario
- PortfolioHeroChart.mobileEl ya renderiza chart con { minimal: true } — parte de M1 está hecha; tocar podría romper el layout existente
- MobileNav NO tiene pill activo (M7 pendiente): solo cambia color, no hay background pill. Si se implementa junto con el reorden,增大 el scope
- Shared kpiItems array se usa tanto para mobile carousel como para desktop grid — mover el carousel al final en mobile no afecta desktop, pero hay que aislar correctamente el bloque
- No se encontraron tests unitarios para DashboardOverview, AllocationPanel, MobilePositionsList ni PortfolioHeroChart — bajo riesgo de rotura de tests pero también sin cobertura de regresión
- La SPEC exige que above the fold en 390×844 muestre 'valor + chart + períodos + inicio del card de asignación' — reordenar requiere verificar que esto se cumpla con las alturas actuales (chart 200px, header ~88px por safe-area + h-11 + h-9)

### Evidencia verificada
- **`src/features/portfolio/components/DashboardOverview.jsx`** — `kpiItems` (líneas 108-113, confianza 100%): Array de KPIs compartido entre mobile carousel y desktop grid. Al mover el carousel al final en mobile, la definición queda igual pero el JSX de renderizado mobile debe reordenarse.
- **`src/features/portfolio/components/DashboardOverview.jsx`** — `// ═══════════════ MOBILE LAYOUT ═══════════════` (líneas 130-160, confianza 0%): [NO VERIFICADO] Sección mobile actual: Hero → KPI carousel → AllocationPanel → separador → MobilePositionsList. El reorden M8 requiere mover KPI carousel al final (después de MobilePositionsList).
- **`src/features/portfolio/components/DashboardOverview.jsx`** — `px-4 pt-4 pb-2` (líneas 135-140, confianza 100%): Padding actual del KPI carousel. Al moverlo al final, este padding puede generar espacio extra no deseado debajo de MobilePositionsList. Revisar consistencia de espaciado entre secciones.
- **`src/features/portfolio/components/MobilePositionsList.jsx`** — `featuredPositions` (líneas 140-150, confianza 100%): Ya existe la variante 'featured' que toma top-5 por valuación. El reorden M8 la usa directamente; pero verificar que no haya padding conflictivo al ser el penúltimo bloque antes de los KPIs.
- **`src/features/portfolio/components/AllocationPanel.jsx`** — `innerRadius={32} outerRadius={46}` (líneas 73-74, confianza 100%): AllocationPanel ya tiene donut con centro (posCount + 'pos.') y leyenda coloreada a la derecha. La SPEC describe esto como M4, pero el código ya lo implementa — riesgo de duplicación si se refactoriza sin revisar.
- **`src/features/portfolio/components/PortfolioHeroChart.jsx`** — `minimal` (líneas 165-170, confianza 100%): mobileEl ya usa renderChart('heroFill-m', { minimal: true }) — chart sin grilla, sin ejes, gradiente especial. La SPEC describe esto como M1 pero ya está implementado.
- **`src/components/common/MobileNav.jsx`** — `style={{ color: active ? 'var(--teal)' : 'var(--ink-faint)' }}` (líneas 54-56, confianza 100%): MobileNav solo cambia color en estado activo, NO tiene pill background como pide M7. Si se implementa junto con M8, aumenta el scope del cambio.
- **`src/components/common/MobileHeader.jsx`** — `toggleBalance` (líneas 47-56, confianza 100%): MobileHeader ya tiene botón ojo (Eye/EyeOff) con toggleBalance y settings con isAdmin gate. M5 ya implementado sin cambios necesarios.
- **`src/features/portfolio/hooks/useSparklines.js`** — `useSparklines` (líneas 1-70, confianza 100%): Hook existente que cachea y descarga sparklines por ticker con back-adjust. MobilePositionsList ya lo recibe via prop sparklineFor. No necesita cambios para el reorden.
