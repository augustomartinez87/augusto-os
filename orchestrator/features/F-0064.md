---
id: F-0064
title: Argos compact desktop — paddings y gaps generales del bloque desktop
target: argos
ui: true
resolves: [AR-038]
acceptance:
  - "Con COMPACT_OVERVIEW_ENABLED=true y viewport compact: px-6 lg:px-8 / py-3 del bloque desktop de DashboardOverview.jsx bajan un escalón, y los gaps del grid hero+KPI (gap-3/gap-2) bajan un escalón"
  - Full desktop sin cambios, con el flag en cualquier valor
  - "Con COMPACT_OVERVIEW_ENABLED=false: paddings/gaps son siempre los de hoy"
  - Typecheck, lint y tests pasan sin errores
---

## Contexto

Módulo M6 (último) de `SPEC-compact-desktop-overview-redesign.md` (raíz del repo). Depende de F-0059 (flag); ejecutar después de F-0060/F-0061/F-0062 para ajustar sobre el espacio que esos módulos ya liberaron, no antes.

Archivo: `src/features/portfolio/components/DashboardOverview.jsx`, bloque `hidden md:flex ... px-6 lg:px-8 py-3` (línea ~254) y el grid `gap-3` (línea ~257).

**Patrón de flag obligatorio**: igual que los módulos anteriores, ramificar contra `COMPACT_OVERVIEW_ENABLED`.

## Pasos sugeridos

1. Ramificar `px-6 lg:px-8` → agregar `compact:md:px-4` cuando el flag está en `true`.
2. Ramificar `py-3` → `compact:md:py-2` cuando el flag está en `true`.
3. Ramificar los `gap-3`/`gap-2` del grid hero+KPI a un escalón menos en tier compact, cuando el flag está en `true`.

## Fuera de alcance

- Cualquier cambio a mobile.
- Reordenar secciones del Overview.

## Restricciones clave

- Con el flag en `false`, paddings/gaps son siempre los de hoy — este es el último módulo, buen punto para hacer la pasada de verificación final de los 6 (con el flag en ambos valores) antes de release.
