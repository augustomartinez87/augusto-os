---
id: F-0024
title: Argos mobile — Reorden del Overview (allocation y destacadas above the fold, KPIs al final)
target: argos
ui: true
acceptance:
  - Orden del Overview mobile: hero → AllocationPanel → Posiciones Destacadas → KPI carousel (Invertido/Valuación/P&L All Time/P&L Hoy) → FAB sin cambios
  - En viewport 390×844 quedan above the fold el valor, el chart, los períodos y el inicio del card de Asignación
  - Sin separadores dobles ni padding duplicado entre secciones (revisar los px-4/pb-2 y el divider actual)
  - La variante positions (/portfolio/posiciones) y el layout desktop quedan sin cambios
  - Typecheck, lint y tests pasan sin errores
---

## Contexto

Módulo M8 de `SPEC-mobile-overview-redesign.md` (raíz del repo argos). **Depende de F-0021 y F-0022** (correr después). Cierre del rediseño: la jerarquía de la referencia prioriza asignación + destacadas; los KPIs detallados bajan al final del scroll (siguen accesibles, dejan de competir above the fold).

Archivo único: `src/features/portfolio/components/DashboardOverview.jsx`, bloque `md:hidden` de la variante `full`. Es un reorden de JSX ya existente — no crear componentes nuevos ni tocar props/data flow. El KPI carousel (scroll horizontal con `kpiItems`) se mueve tal cual debajo de la sección de destacadas.

## Pasos sugeridos

1. Reordenar los bloques del layout mobile `full` según el orden de acceptance.
2. Normalizar spacing entre secciones (un solo criterio de gap; eliminar el divider `mx-4` si quedó redundante).
3. Verificación visual en 390px: fold correcto, sin doble padding, FAB no tapa el último KPI (padding-bottom del contenedor).

## Fuera de alcance

- Cambios internos a hero, AllocationPanel, destacadas o KpiCard (ya cubiertos por F-0019/21/22).
- Layout desktop y variante positions.
- Eliminar el KPI carousel (se conserva, solo baja).

## Restricciones clave

- Solo reorden y spacing; cero cambios de lógica, hooks o props.
- El FAB mantiene su offset actual sobre el bottom nav (`--safe-bottom` + 56px + 16px).
