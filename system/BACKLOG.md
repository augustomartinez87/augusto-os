# Backlog — augusto-os

Formato: `P<n>` = prioridad (1 = más urgente) · `[target]` = repo destino · `Ejecutor` = quién ejecuta (ver CONVENTIONS §5)

---

## Sistema (orchestrator / augusto-os)

| ID | P | Descripción | Estado | Ejecutor |
|----|---|-------------|--------|----------|
| S-000 | ✅ | Aislamiento de prod: DB no-prod + guard anti-prod en el loop | done 2026-06-19 | cc |
| S-000b | ✅ | Dev DB de Spensiv = Neon; secreto gitignored; esquema materializado | done 2026-06-19 | cc |
| S-000c | ✅ | Seed data de prueba para Spensiv en Neon (no hay seed script — datos vacíos) | done 2026-07-03 (F-0010, resuelto también SPT-001) | cc |
| S-001 | ✅ | **Code Reviewer** — rol independiente (Opus, solo lectura) que revisa el `git diff HEAD` de cada step antes de commitear: scope creep vs spec, reglas de dominio (TNA, mutuo/pagaré, prisma), errores de lógica que tsc/tests no agarran. Insertado entre Verifier/QA y commitStep, mismo tope de 3 reintentos compartido, sin gate humano salvo rechazo persistente | done 2026-06-26 (Claude Code) | cc |
| S-002 | ✅ | **Gating por OPERATOR_STATE** — `operator-state.ts` centraliza lectura de `OPERATOR_STATE.yaml` (fail-safe a PRODUCT). Telegram (`notifyGate`/`notifyDeployed`/`notifyReleaseFailed`) muteado en SLEEP, acortado en OFFICE+short, sin cambios en PRODUCT. El gate humano sigue bloqueando igual en SLEEP, solo no avisa. Base de S-004 | done 2026-06-26 (Claude Code) | cc |
| S-003 | ✅ | Fase 3: Multi-target routing (spensiv vs argos por feature) — `targets.json` define `verifyCmd`/`lintCmd`/`testCmd`/`qaBaseUrl` por target, consumido por `targets.ts`/`executor.ts`/`planner.ts`; features target-aware desde F-0001 | done (retroactivo, reconciliado 2026-07-07) | cc |
| S-007b | ✅ | **Toggle remoto de OPERATOR_STATE** — selector PRODUCT/OFFICE/SLEEP en el dashboard (3 chips arriba del header), `sync.ts` pullea `orch_operator_state` cada 5s y reescribe `OPERATOR_STATE.yaml` local si cambió (dedup por `updated_at`, preserva comentarios de cabecera). Tabla nueva con RLS anon select+update solo en id=1, sin insert/delete. Fast-follow de S-007/S-002 | done 2026-06-26 (Claude Code) | cc |
| S-004 | ✅ | **Loops nocturnos** — `autopilot.ts`, llamado desde `sync.ts` cada tick: en modo SLEEP y con el loop libre, toma la primera item `pending`/P2+/sin keywords de riesgo (dinero/prod/legal/migración) del backlog por orden de prioridad, fuerza el spec vía Architect (target tomado de la sección, no por keyword-detection), spawnea `npm start <featureId>` fire-and-forget. Marca la fila `armado (autopilot)` al elegir y `done (autopilot)` al deployar; si falla el release no se reintenta solo. Cap 5/día, lock con timeout 10min, ADR-log de la decisión. Gates duros (S-002) siguen pausando igual en silencio | done 2026-06-27 (Claude Code) | cc |
| S-005 | 5 | Fase 5: Product Analyst (backlog desde métricas de uso real) | pending | cc |
| S-006 | ✅ | Celu Fase 1: bot de Telegram (AlantORCH) — notifica gates y permite aprobar/idear desde el celu. `telegram.ts`+`bot.ts`+hook en gates+`npm run bot`, sin deps (fetch nativo). Probado- responde y guarda ideas | done 2026-06-25 | cc |
| S-007 | ✅ | **Dashboard web mobile-first** (control plane Supabase) — v1 entregado como HTML estático single-file (`dashboard/index.html`), NO Next.js (decisión real, ver RUNBOOK): tabs Loop/Backlog/Ideas, polling 5s, deploy Vercel auto. Fase A (sync) + Fase B (la web) completas. Fast-follow: agent presence (S-015), realtime, diagramas (S-016) | done 2026-06-26 | cc |
| S-008 | ✅ | **Intake + Architect agent** — split en 2: (a) **Intake barato** (heurística, sin LLM): detecta target, busca ADR/features/backlog relacionados (grep), clasifica bug/feature/arquitectura, resume contexto; (b) **Architect (Opus on-demand)** solo cuando la idea necesita arquitectura → escribe el `features/F-XXXX.md`. CLI `npm run intake`. ADR-0028/0029 | done 2026-06-26 (Claude Code) | cc |
| S-016 | ✅ | **Diagramas de flujo de trabajo** — guía visual de qué hacer en cada caso (gate falla, deploy, etc.); candidata a pestaña en el dashboard mobile (S-007). Idea de Augusto | done 2026-07-06 (F-0011, Claude Code) | cc |
| S-017 | ✅ | Fix ADR auto-log: inserta los ADR nuevos DENTRO del fence del template (rompe el markdown). Anclar la inserción después del bloque de template, no tras el header | done 2026-06-26 (Claude Code) | cc |
| S-018 | ✅ | Auto-deploy en verde + sin gates por-step + avisos deploy/fallo por Telegram (ADR-0019) | done 2026-06-25 | cc |
| S-019 | ✅ | **Hardening del loop** (review OpenCode): `getNextPendingStep` saltea steps `blocked`; `pushMain` verifica estar en main; dedup `appendProgress` al resumir; remover `awaitingPushApproval` (muerto); centralizar strings de modelo en config (sirve para S-014) | done 2026-06-26 (Claude Code) | cc |
| S-020 | ✅ | **Builder ve restricciones del spec** — pasar "Fuera de alcance" + "Restricciones clave" del F-XXXX.md al prompt del executor (hoy solo ve `step.desc`). Afecta calidad/cumplimiento de reglas de dominio | done 2026-06-26 (Claude Code) | cc |
| S-021 | ✅ | **Tests del core** — `state.ts`, `index.ts` (loop), `gates.ts`. Hoy solo existe `adr.test.ts`; el sistema que deploya a prod es el menos testeado | done 2026-06-26 (Claude Code) | cc |
| S-022 | ✅ | **Dashboard → vista Operaciones** (rediseño de IA: roster honesto de etapas reales, hero "quién tiene la posta", feed de deltas no-logs; preserva ideas/mode/backlog). Fast-follow de S-007/S-015 | done 2026-06-27 (Claude Code) | cc |
| S-025 | ✅ | **Rotación/retención de logs** — `log-cleanup.ts`: disco 30d (`logs/*.log`, `loop-*.log`, `blocked.log`; preserva `orchestrator.log` + run activo), Supabase 7d (`orch_logs` DELETE por `ts`), throttle 1h desde `sync.ts`. ADR-0036 | done 2026-06-28 (Claude Code) | cc |
| S-024 | ✅ | **Evaluador de posts de X** — pegás un post guardado y el Architect lo juzga contra tu arquitectura real (lee `system/`): ¿ya implementado? / ¿vale la pena + beneficio concreto? / ¿es bait y por qué? Versión free = un prompt/skill; conecta con S-008 | done 2026-07-07 (F-0012, Claude Code) | cc |
| S-034 | ✅ | **Fallback + visibilidad de saldo DeepSeek (Scout KGL)** — `deepseek.ts`: `DeepSeekInsufficientBalanceError` por status code real (402), nunca substring; `AbortController` compartido entre las 3 investigaciones para cortarlas de una apenas se detecta. `fetchDeepSeekBalance`/`shouldRunBalanceCheck` (throttle 7min) integrados en `sync.ts` → tabla `orch_scout_status` (Supabase). Barra + pill en `dashboard/index.html` (single-file, sin deps nuevas). 12 tests nuevos | done 2026-07-07 (Claude Code) | cc |
| S-023 | 4 | **Closed learning loop** (idea de Hermes) — cuando el loop resuelve un patrón recurrente, auto-generar un spec/template reutilizable para no re-razonar. Evolución del ADR/specs, en NUESTRO sistema (no adoptar Hermes como motor) | pending | cc |
| S-014 | 2 | **Routing multi-modelo** — Builder barato (DeepSeek V4 vía Claude Code Router) con Opus de Planner; el Verifier cubre el riesgo. Pilot en 1 feature, medir reintentos. Eval en `system/MODEL-ROUTING.md` | pending | cc |
| S-036 | ✅ | **Hardening de limits + STATE** (bugs reales vistos 2026-07-11 en F-0019): (a) `parseResetTime` fallback = ahora+5h ciego cuando el output de CC no trae hora de reset; (b) `saveState` no atómico, corrompía STATE.json con Ctrl+C a mitad de escritura. F-0028, 7/7 steps completos y deployado (`ccd2747`). `saveState` con temp+`renameSync`, `loadState` tolerante a basura trailing; `probeAvailability()`/`isProbeAvailable()` en `limits.ts` (ping barato vía `MODEL_INTAKE`, decide disponibilidad por `isUsageLimitError`, no por exit code — evita el falso negativo del `--max-turns 1`); `handleUsageLimit` sondea cada `PROBE_INTERVAL_MS` cuando no hay hora de reset parseable; probe al arranque si `pausedUntil` es futuro. Step 4 se trabó por reviewer 2 veces y escaló a Opus (S-039) en condiciones reales por primera vez — el fixer lo resolvió en 1 intento, sin marcar el step bloqueado; validación real de S-039 registrada. **Nota de calidad:** el reviewer pidió en step 5 extraer los regexes de `parseResetTime`/`hasExplicitResetTime` a constantes compartidas (riesgo de drift si se edita una sin la otra) — el fix aprobado NO lo hizo, quedaron duplicados inline; no es un bug activo hoy (son idénticos) pero es deuda real que pasó el review sin resolverse | done 2026-07-14 (loop, con escalación S-039 validada en vivo) | cc |
| S-037 | 3 | **QA visual ignora `qaBaseUrl` del target** — `index.ts:356` hace `process.env.QA_BASE_URL ?? 'http://localhost:3000'` sin leer `targets.json` (argos = 5173): el QA de argos nunca corre salvo env var manual. Fix: default = `target.qaBaseUrl`, env var solo como override. Visto 2026-07-11 en F-0020 | pending | cc |
| S-041 | ✅ | **STATE.json puede desincronizarse del git real bajo contención de lock** — incidente real 2026-07-13/14 en F-0028 step 3: el fixer (S-039) resolvió el step correctamente (commit `099927c`, 75 inserciones válidas, verificado a mano), pero `STATE.json` quedó registrando `blocked`/`commit: null` con un mensaje de error que ya no correspondía al resultado final. **Causa raíz confirmada (Cowork, 2026-07-14, ver `state.ts`/`index.ts`/`autopilot.ts`):** `STATE_PATH` es una ruta única y fija (`state.ts:7`), sin namespacing por feature/target; `index.ts main()` (run manual) nunca llama `acquireLock()`/`releaseLock()` — el mutex `AUTOPILOT.lock` solo protege la ventana de spawn de autopilot contra auto-duplicarse, un run manual no lo consulta; cada target tiene un único working tree fijo (`targets.json`), sin `git worktree` en ningún lado. Cualquier par de runs concurrentes (manual+autopilot o manual+manual, mismo target o no) comparten el mismo STATE.json y pueden chocar en el working tree. Fix diseñado: extender `acquireLock`/`releaseLock` (ya testeados, ya usan heartbeat de `loop-heartbeat.ts` para liveness) para que cubran la ejecución completa de `index.ts main()`, no solo el spawn de autopilot — resuelve STATE.json y working tree como efecto colateral, sin tocar `state.ts`/`git.ts`. Prompt listo: `system/prompts/S-041-lock-cross-run-git-state.md`. **Implementado** (Claude Code, 2026-07-14): `acquireRunLock()` nueva en `index.ts`, envuelve `acquireLock`/`releaseLock` existentes (sin tocarlos); adquirida como primera acción de `main()` (arranque y resume), liberada en `finally` que cubre todo el run. Guard `isDirectRun` agregado para que `index.test.ts` pueda importar sin disparar el CLI real (ADR-0081). 4 tests nuevos en `index.test.ts`. `state.ts`/`git.ts` intactos. Verificado por Cowork: diff (156 inserciones/38 borrados, solo `index.ts`+`index.test.ts`) leído completo y coherente con el spec, lógica de lock/try-finally correcta, `isDirectRun` correcto al final del archivo, master/`HEAD` sin tocar (branch `fix/S-041-lock-cross-run`, commit `3eeccc2`, sin merge/push). ADR-0082 (decisión central) + ADR-0081 (guard de testing) | ✅ done 2026-07-14 (mergeado a `master` por Augusto sobre commit `3eeccc2` — sin push todavía) | cc |
| S-043 | ✅ | **`_prisma_migrations` de Kredy prod (Neon) sin reconciliar + `prisma/migrations` nunca versionado** — descubierto 2026-07-15 al aplicar la migración de SP-014: `prisma migrate status` mostraba 15 pendientes en vez de 1. Causa raíz doble, confirmada de punta a punta: (a) el paso 2 de `system/MIGRATION-kredy-to-neon.md` (S-010, corte 2026-07-08) usó `prisma db push` para materializar el schema en Neon — sincroniza estructura pero nunca escribe en `_prisma_migrations`, dejando el tracking vacío aunque la estructura real estuviera al día; (b) hallazgo mayor no anticipado: `.gitignore` tenía `prisma/migrations` desde siempre — la carpeta ENTERA nunca estuvo versionada, ni siquiera la migración nueva de SP-014 (cada checkout podía tener un historial distinto). De las 14 migraciones "viejas" pendientes, 12 eran reconciliación de tracking legítima (estructura ya existía, solo faltaba el registro — resueltas vía `prisma migrate resolve --applied`, con reintentos espaciados por contención de advisory lock en Neon, causa exacta no determinada pero descartada relación con deploys de Vercel — verificado vía API que no había ningún deploy corriendo); 2 (`add_card_closing_schedule`, `add_tags`) eran basura real de la era "Spensiv" (antes del pivot a Kredy) sin ningún modelo correspondiente en `schema.prisma` actual (confirmado por grep) — una de ellas había quedado marcada `--applied` por error (la tabla no existe en prod), corregida con `--rolled-back` antes de borrar la carpeta. Resultado: `migrate deploy` aplicó únicamente `20260715120000_collection_reminders_phone_alias` (la única real), backfill corrido (19 Persons sin phone, 0 con Contact vinculado — 0 backfilled, esperado), `tsc` limpio. `prisma/migrations` sacado del `.gitignore` y las 13 migraciones reales versionadas por primera vez (commit `aa47da4`, pusheado a main). Ver `system/prompts/SP-014-aplicar-migracion-prod.md` (evolucionó varias veces durante la sesión, cada gate resuelto antes de avanzar al siguiente). Pendiente menor: `system/MIGRATION-kredy-to-neon.md` no quedó actualizado con la nota de reconciliar `_prisma_migrations` tras un `db push` — de baja prioridad ahora que ya no hay next arrastre pendiente | done 2026-07-15 (Claude Code + Cowork, verificado paso a paso) | cc |
| S-042 | 2 | **Sesiones manuales de Claude Code CLI fuera del loop pueden truncar archivos en carrera** — incidente real 2026-07-15 en `kredy`: 2 sesiones de Claude Code corridas directo (fuera de `npm start`, sin pasar por `index.ts`/`acquireRunLock` de S-041) dejaron 6 archivos truncados a mitad de escritura (`app/ap/page.tsx`, `app/dashboard/loans/page.tsx`, `prisma/schema.prisma`, `server/routers/ap.ts`, `server/routers/loans/index.ts`, `server/routers/persons.ts`) + `.git/index.lock` huérfano (ningún proceso vivo lo sostenía al diagnosticar — el git que lo creó ya había muerto). El fix de S-041 (`acquireRunLock` en `index.ts main()`) NO cubre este caso: solo protege runs que pasan por el orquestador (`npm start <featureId>`), no sesiones CLI directas del estilo `claude -p "..."` corridas a mano en un repo target. Recuperación validada en vivo (Cowork + Augusto, 2026-07-15): matar procesos huérfanos si los hay, `Remove-Item .git\index.lock -Force`, `git checkout HEAD -- <archivos afectados>` (seguro cuando el diff muestra solo truncación/deleciones, nunca contenido nuevo mezclado — confirmar con `git diff HEAD` antes de descartar), `npx tsc --noEmit` para verificar. Documentado en `RUNBOOK.md` §5. Pendiente: (a) evaluar si vale la pena un `check-repo-health` (git status + tsc) que las sesiones manuales corran antes/después de trabajar, o si alcanza con la disciplina de "no correr 2 sesiones de Claude Code en el mismo repo a la vez" (documentada, sin enforcement técnico) | pending | cc |
| S-039 | ✅ | **Escalación automática de steps trabados (Opus-fixer) en vez de gate/parada manual** — reemplaza `haltBlocked` (S-038/0c53d49) por `escalateOrHalt`: cuando un step falla 3 veces (builder/verifier/QA/reviewer), antes de bloquear se escala a `MODEL_FIXER` (Opus, `models.ts`) en sesión fresca (no resume la sesión trabada del builder) con el historial completo de fallos acumulado en `Step.failureHistory` (`state.ts`, cap 6 entradas × 800 chars, `appendFailureHistory`) — no solo el último error. Nuevo `orchestrator/src/escalation.ts`: `escalateStep()` corre el verifier tras cada intento del fixer (presupuesto propio de `MAX_FIXER_ATTEMPTS=2`), reusa `RESTRICCIONES_ABSOLUTAS` (extraída de `executor.ts`) sin relajarlas para el fixer. Si el fixer resuelve el step, nunca se marca `blocked` de cara al usuario y el flujo sigue normal (si fue el reviewer quien originó la escalación, se re-corre el reviewer sobre el fix antes de commitear — no bypasea ese control). Si el fixer también falla, ahí sí se marca `blocked`, se notifica por Telegram y el loop continúa (sin `return` forzado) hasta que `getNextPendingStep` corta limpio por falta de trabajo elegible. Gates de dinero/prod/legal (`checkHumanGate`) sin tocar. 11 tests nuevos (`escalation.test.ts` + `appendFailureHistory` en `state.test.ts`) — 351/351 en la suite completa, typecheck limpio. **Verificación real pendiente**: cubierto con mocks de `execa`/verifier inyectados (patrón `ReviewerOpts`), no se disparó una escalación real contra un target vivo (requiere una sesión real de Claude Code + repo target) — validar en el próximo step que se trabe de verdad | done 2026-07-13 (Claude Code) | cc |
| S-038 | ✅ | **ADR auto-log genera ADRs vacíos** — `executor.ts` pasaba el stdout crudo JSON-stringificado a `parseAdrBlocks` en vez del `.text` ya extraído por `parseClaudeJson`; sin saltos de línea reales, la regex multilínea de `extractField` no matchea nada. Fix: `executor.ts` ahora usa el texto plano extraído por `parseClaudeJson`, con fallback al output crudo solo si el JSON no parsea. Defensa agregada en `appendAdr` (adr.ts): si título+decisión vienen vacíos, no escribe y devuelve `null` (no quema ID). 2 tests de regresión en `adr.test.ts` (roundtrip con JSON real vs. bug reproducido) + 2 tests de la defensa nueva — 19/19 verdes, 340/340 en la suite completa. Saneadas ADR-0075 a 0078 (vacíos post-2026-07-11) en DECISIONS.md, backup `DECISIONS.md.bak-20260713`. **Verificado con dato real**: se corrió el fix contra `orchestrator/logs/F-0025-step2.log` (el log real que generó el ADR-0076 vacío) — antes del fix da campos vacíos, después extrae correctamente target/titulo/decision/contexto/alternativas/consecuencias | done 2026-07-13 (Claude Code) | cc |
| S-015 | ✅ | **Agent team view** — presencia real con heartbeat: tabla `orch_presence` (planner+builder), sync.ts emite cada 5s, staleness >30s→"sin señal" />2min→"posible cuelgue". Dashboard usa presencia como fuente de verdad del roster; `derivePosta` como fallback. Muestra modelo por rol (Opus/Sonnet) | done 2026-06-27 (Claude Code) | cc |
| S-013 | ✅ | Guard anti-reejecución (avisa "ya finalizó" + `--force` antes de gastar tokens) + reintento del aviso de gate por Telegram | done 2026-06-25 | cc |
| S-009 | ✅ | **ADR auto-log** — el loop escribe en DECISIONS.md cada decisión de diseño con su Origen. Helper `appendAdr()` + `parseAdrBlocks` + prompt del executor + idempotencia. 13 tests verdes | done 2026-06-25 (Claude Code) | cc |
| S-010 | ✅ | **Migrar Kredy prod: Supabase → Neon** — unificar todo lo Prisma en Neon (prod + dev). Runbook en `system/MIGRATION-kredy-to-neon.md`. Toca dinero real → ejecutó Augusto con OK explícito | done 2026-07-13 (Augusto) — corte de tráfico + smoke test OK (2026-07-08); proyecto viejo `jymdblurkpadupdqzfzo` borrado definitivamente por Augusto (verificado: ya no aparece en `list_projects`, org `supabase.amartinez` queda con solo `argoscapital` + augusto-os) | manual |
| S-035 | 2 | **Consolidar cuentas Supabase** — mover augusto-os desde su cuenta Supabase actual ("supabase2", org `martinezstreet21Org`) a la org `supabase.amartinez` (`gbztwnshsmcwqnqjouhi`, "supabase1"), donde ya vive Argos, vía Project Transfer nativo de Supabase. Hecho 2026-07-08 (Cowork): invitado `martinezstreet21@gmail.com` como Developer en `supabase.amartinez`, transfer ejecutado desde el lado supabase2 (Brave), confirmado vía MCP — proyecto `exyhnrynpuflbuprmdto` ("martinezstreet21@gmail.com's Project") ahora vive en `supabase.amartinez`, mismo ref/host/keys, sin downtime, sin cambios en Vercel/código. Estado final: **supabase1 = Argos + augusto-os + Kredy viejo (pausado)**; supabase2 (`martinezstreet21Org`) quedó con 0 proyectos, dado de baja de facto (no se eliminó la cuenta, solo se vació); Neon = Spensiv + Kredy | done 2026-07-08 (Cowork) | manual |
| S-026 | ✅ | **Reconciliación del backlog** — auditoría de estados reales cruzando BACKLOG/PROGRESS/prompts/git; corrección de S-009 en CONVENTIONS.md (era "spec, no implementado", está implementado); nota de mapeo S-022(old)→S-025; regla append-only de IDs (CONVENTIONS §3); huecos S-011/S-012 documentados | done 2026-06-27 (Claude Code) | cc |
| S-027 | ✅ | **Hardening heartbeat del loop + lock por liveness** — `LOOP_HEARTBEAT.json` desde `index.ts`; `orch_presence` con rol `loop`; `acquireLock` respeta lock si heartbeat fresco (nunca pisa un proceso vivo); `markBacklogState` retorna bool; dashboard usa heartbeat del loop para Builder | done 2026-06-27 (Claude Code) | cc |
| S-028 | ✅ | **Fix: aprobación remota desde Telegram no desbloqueaba el loop** — `pollApprovalOnce()` exportado de `telegram.ts`, embebido en el gate-wait de `runLoop` (~8s latencia máx). `npm run bot` ya no es requisito para que el botón ✅ funcione. ADR-0034 | done 2026-06-28 (Claude Code) | cc |
| S-029 | ✅ | **Fix: 409 Conflict Telegram al correr bot + loop simultáneamente** — `bot-heartbeat.ts`; bot escribe `BOT_HEARTBEAT.json` en cada ciclo; loop chequea `isBotAlive()` antes de `pollApprovalOnce()`: si bot vivo → solo pollea STATE.json, si bot caído → fallback automático en ≤30s. ADR-0035 | done 2026-06-28 (Claude Code) | cc |
| S-030 | ✅ | **Clasificación de ejecutor del backlog (auto/cc/manual) + autopilot por allowlist** — campo `Ejecutor` en BACKLOG.md (fuente de verdad), espejado a `orch_backlog`; autopilot cambia a allowlist primaria (`Ejecutor=auto`), keywords de riesgo como red de seguridad secundaria. ADR-0037 | done 2026-06-28 (Claude Code) | cc |
| S-031 | ✅ | **Fix max-turns en Architect/Planner/Reviewer + visibilidad de errores** — `MAX_TURNS=15` centralizado en `models.ts`; los tres agentes usan `--max-turns 15` en lugar del literal `1` que cortaba el loop cuando claude emitía `tool_use` en turno 1. Error de visibilidad: stdout incluido en el `throw` de los tres. Planner refactorizado con `PlannerOpts.callClaude?` injectable (patrón de Architect/Reviewer). ADR-0038 | done 2026-06-29 (Claude Code) | cc |

## Kredy [kredy]  (préstamos/crédito — ex "Spensiv", renombrado 2026-06-20)

| ID | P | Descripción | Estado | Ejecutor |
|----|---|-------------|--------|----------|
| SP-001 | ✅ | Sprint S-D: Búsqueda unificada por CUIL/DNI en consola — resuelto por el buscador de Personas (`p.cuit.includes(search)`; el DNI queda embebido en las posiciones 2-9 del CUIL, así que un DNI suelto matchea por substring) sobre el identity backbone de SP-003 | done 2026-06-11 (commit `32f2bc7`, reconciliado 2026-07-08 — Cowork) | manual |
| SP-002 | ✅ | Sprint S-E: Límite de originación por CUIL en frontend — `lib/risk/debtorLimit.ts` + banners de identidad/exposición en `components/loans/pre-approved-loan-card.tsx` | done 2026-06-11 (commit `32f2bc7`, reconciliado 2026-07-08 — Cowork) | manual |
| SP-003 | ✅ | Migración SA identity backbone — `lib/identity/resolvePerson.ts` (CUIL→DNI→nombre→crear) + campos `dni`/`identityStatus` en `Person` | done 2026-06-10/11 (commits `6ee650a`, `32f2bc7`, reconciliado 2026-07-08 — Cowork) | manual |
| SP-004 | ✅ | Migración AP Commission V2 — `docs/migration-ap-commission-v2.sql` aplicada en prod (`ap_withdrawals`/`ap_ledger_events` con datos reales), `server/services/commission.service.ts` (`realizeCommissionsForPayment`), consumida hoy por el cron `/api/cron/ap-reconcile` activo en prod | done 2026-06-16 (commits `5988972`/`9c40c03`; hecho fuera del loop F-XXXX, sin spec formal; reconciliado 2026-07-08 — Cowork) | manual |
| SP-005 | 3 | Refinanciación Fase 2: UI de propuesta de refinanciación | pending | manual |
| SP-006 | 3 | Refinanciación Fase 3: router refinanceLoan + Prisma RefinancingRequest | pending | manual |
| SP-007 | 4 | Refinanciación Fase 4: webhook Stripe / flujo de pago | pending | manual |
| SP-008 | ✅ | AP Score como gate en pre-aprobación | done 2026-06-20 (F-0002, mergeado a main) | cc |
| SP-009 | ✅ | **Gate de límite por vínculo** (conocido 500k / referido 200k+aval / desconocido 0) en preApprove. F-0005 | done 2026-06-25 (deployado) | cc |
| SP-010 | ✅ | **Mutuo + pagaré generados** — pagaré pre-llenado por el total (sin protesto), cláusula TERCERA fija 8% mensual, SEXTA con intimación previa 5 días, selector CABA/Mar del Plata, gate de datos del Person. F-0006, revisión legal cerrada 2026-06-26 | done 2026-06-26 | cc |
| SP-011 | ✅ | **Documentos pendientes del préstamo** — fotos de mutuo firmado + pagaré vía LoanAttachment, badge "pendiente" NO bloqueante. F-0007 | done 2026-06-25 (deployado) | cc |
| SP-012 | 3 | UI para que el AP elija/edite el tier de vínculo y el referente (follow-up de SP-009) | pending | manual |
| SP-013 | 3 | Eliminar préstamos de testing completamente — un préstamo creado desde link de AP y aprobado sigue apareciendo en el historial sin poder borrarse | pending (verificar si sigue vigente) | cc |
| SP-014 | ✅ | **Recordatorios masivos de cobranza** — botón "Recordar cobro" en `/dashboard/loans` (cartera de Augusto) y `/ap?token=...` (portal del AP): panel con la cuota más próxima/vencida por deudor, link `wa.me` por fila (monto, vencimiento, alias de cobro — sin TNA nunca) + "Copiar todos" (no se abren N pestañas, bloqueado por el navegador — ADR-0083). Dos endpoints en vez de uno scoped por `userId` porque Kredy es single-tenant y el AP opera sin sesión Clerk, vía token (ADR-0084). `normalizePhone` (E.164, 6 tests), `persons.setPhone` para cargar teléfonos faltantes. Commit `40e6a1e`, deployado (Vercel `dpl_2MtpFTvwjGUeDqYzzguUJYbwXnQx`, READY). Migración (`Person.phone`+`AgentConfig.collectionAlias`) aplicada a prod con OK explícito de Augusto 2026-07-15 — destapó S-043 en el camino (14 migraciones fantasma + `prisma/migrations` gitignoreado, ver esa entrada). Backfill corrido: 19 Persons sin phone, 0 con Contact vinculado con phone → 0 backfilled (esperado, no error) — quedan 19 deudores sin teléfono cargado, hay que completarlos a mano desde el dashboard (`persons.setPhone`) a medida que se necesite mandar el recordatorio | done 2026-07-15 (Claude Code + Cowork, migración prod verificada paso a paso) | cc |
| SP-015 | ✅ | **Nav mobile incompleta** — el bottom nav (`app/dashboard/layout.tsx`) solo exponía 4 de los 10 destinos del sidebar. Fix: 5to ítem "Más" con `Sheet` (bottom, ya disponible en `components/ui/sheet.tsx`, sin dependencias nuevas) que lista los 6 antes inalcanzables (Riesgo, Consulta 360°, Reglas de Tasas, Agente (AP), Políticas de Riesgo, Usuarios). La lista del sheet se deriva de `navigation` filtrando lo ya presente en `mobileNavBase`/`mobileNavAdmin` — no duplica la fuente de verdad del sidebar de desktop, así que un ítem nuevo en el sidebar aparece solo en el sheet sin tocar este archivo de nuevo. "Más" se marca activo cuando el pathname actual es uno de los 6 ocultos. No tocó desktop, `mobileNavBase`/`mobileNavAdmin` como conjunto, Perfil, ni `/ap`. Commit `e216c23`, deploy `dpl_A5YEeLem4kmatdwp1h3s1onRAUSS` verificado READY en producción (Vercel API). `tsc`/lint limpios; verificación de DOM real no fue posible por requerir sesión Clerk (documentado por Claude Code, no bloqueante) | done 2026-07-16 (Claude Code, deploy verificado por Cowork) | cc |
| SP-016 | 3 | Plazo personalizado en el simulador del AP (`/ap`) — hoy solo se puede elegir entre `config.terms` (preset fijo); el backend (`ap.ts`, `termMonths: z.number().int().min(1).max(360)`) ya soporta cualquier plazo, falta el input libre en el frontend | pending | cc |
| SP-017 | 3 | Confirmación + redirect tras crear/pre-aprobar préstamo — `createLoanMutation`/`preApproveMutation` en `app/dashboard/simulator/page.tsx` solo cierran el modal (sin toast ni redirect a `/dashboard/loans`) | pending | cc |
| SP-018 | ✅ | **Límite de originación: unificar exposición cross-AP por CUIL** — `getGlobalDebtorExposureByCuil(db, cuil, excludeUserId?)` nueva en `lib/risk/debtorExposure.ts`, suma cross-AP por CUIL normalizado; `checkDebtorLimit` la usa como numerador, denominador (% del portfolio) sigue por-AP. `getDebtorExposure`/`reconcileExposure` intactas. 8 tests nuevos (`tests/debtor-exposure-cross-ap.test.ts`), 194/194 verdes, tsc limpio. ADR-0080 | done 2026-07-14 (commit `478e836`, Claude Code, sin deploy manual — solo push a main) | cc |

## Spensiv (tracker finanzas personales) [spensiv]

| ID | P | Descripción | Estado | Ejecutor |
|----|---|-------------|--------|----------|
| SPT-001 | ✅ | Seed data de prueba en el Neon dev `spensiv-dev` (hoy schema vacío, sin datos) | done 2026-07-03 (F-0010, ver S-000c) | cc |

## Argos [argos]

| ID | P | Descripción | Estado | Ejecutor |
|----|---|-------------|--------|----------|
| AR-001 | ✅ | Path (`portfolio-tracker`) + stack + cmds de Argos en targets.json | done 2026-06-20 | cc |
| AR-002 | ✅ | DB dev de Argos: NO aplica — `dbModel: none` (Supabase-client, build/test no tocan DB). Guard exime targets no-Prisma | done 2026-06-20 | cc |
| AR-003 | 4 | Polish: prompt del executor condicional por `dbModel` (sacar frases Prisma-céntricas para targets Supabase) | pending | cc |
| AR-004 | 4 | Si en algún momento hace falta QA visual con datos: crear Supabase branch dev de Argos | pending | cc |
| AR-005 | 2 | **Costo de arancel en TNA (informativo, solo lectura)** — calcular el arancel efectivo de las cauciones, anualizarlo a TNA (vía `annualizeNominalTNA`) y mostrarlo en una vista para comparar contra la TNA de arancel pactada (~1,5) y detectar si la ALyC cobra de más. ESTRICTAMENTE solo lectura: NO modificar `calcularSpreadPorCaucion`, ni la anualización canónica, ni el P&L, ni el spread — solo un cálculo nuevo + vista informativa. Diagnóstico primero: dónde viven los cargos de las cauciones y qué campos traen. Archivos probables: `features/financing/`. (= ARCH-002 de PENDIENTES.md de Argos) | done 2026-06-29 (autopilot) | auto |
| AR-006 | 2 | Guardado de distribución (%) de estrategias — hoy vive en localStorage, debe persistir por usuario en DB para verse igual en otro dispositivo | pending | cc |
| AR-007 | 3 | Notificaciones basadas en el parser de comprobantes (Alycbur) — implementado fuera del loop pero no funciona como debería (solo debe dispararse por movimientos detectados en PDF —rescate/suscripción/compra/venta/renta/amortización—, nunca por carga manual); requiere revisión. Detalle (nota de Augusto, reconciliado 2026-07-08): la alerta más importante es rescate/suscripción detectado en los comprobantes del día; UI candidata = campanita junto al ojo de censura de saldos; el scope es únicamente el portfolio Alycbur (el que usa el parser) | pending (rework) | cc |
| AR-008 | 2 | **Fix "usuarios activos" en Admin** — `userService.getSystemStats()` lee `user_profiles.last_sign_in_at`, pero ningún código de la app lo escribe (`grep` sin resultados fuera de las 2 lecturas); si no hay trigger de Supabase Auth sincronizándolo desde `auth.users`, el conteo de activos está estructuralmente roto. Reconciliado 2026-07-08 | pending | cc |
| AR-009 | 3 | Sparkline no condice con el % mostrado — `MobilePositionsList.jsx:271` muestra el sparkline (últimos ~8 cierres diarios de `useSparklines`) junto a `resultPct` (P&L acumulado desde compra); debería ir junto a `dailyPct` (ya calculado en el mismo archivo, línea 49) para que ambos hablen de la misma ventana temporal. Reconciliado 2026-07-08 | pending | cc |
| AR-010 | 3 | Cauciones: separar arancel como campo propio en el parser/ingestor — `csvSpreadIngestor.ts` solo trae `interes` agregado, sin desglose de arancel; AR-005 ya calcula el arancel en TNA pero de forma informativa aparte, no a nivel de ingesta. Motivo de Augusto: dejó de operar en la ALyC y le empezaron a cobrar aranceles — quiere visibilidad de cuánto le cobran | pending | cc |
| AR-011 | 4 | Lugares: conciliación contra el portfolio real reportado por la ALyC — ya existe el concepto "lugar" (FCI), falta comparar la posición que Argos calcula para un lugar contra el portfolio real de esa cuenta en la web de la ALyC, con recordatorio periódico (mensual/quincenal) para hacerlo | pending | cc |
| AR-012 | ✅ | Revisión UX/UI de mobile por Claude (Sonnet) — pedido explícito de Augusto, sin código a cambiar de entrada, es una auditoría. Salida: `portfolio-tracker/SPEC-mobile-overview-redesign.md` (gap analysis vs mockups de referencia) + specs ejecutables F-0019..F-0024 (= AR-013..AR-018) | done 2026-07-11 (Cowork) | manual |
| AR-013 | 2 | **Mobile Overview: hero chart limpio** — chart sin ejes/grilla full-bleed 200px, jerarquía centrada (label → valor 34px → P&L → En vivo), period selector minimal en una fila. Spec listo: `features/F-0019.md` (M1+M2+M3 del SPEC). Correr primero: F-0020 depende de este | done 2026-07-11 (F-0019 released) | cc |
| AR-014 | 2 | **Mobile: ojo de censura + settings en MobileHeader** — censura toggleable desde cualquier página; settings gated por permisos → /sistema/administracion; sin campana (no hay backend de notificaciones — eso es AR-007). Spec listo: `features/F-0020.md` (M5). Depende de AR-013/F-0019 | done 2026-07-11 (F-0020 released) | cc |
| AR-015 | 2 | **AllocationPanel: donut con contador central + leyenda coloreada** — anillo fino con "N pos." al centro, % de leyenda en el color de cada slice; toggle tipo/estrategia se conserva. Spec listo: `features/F-0021.md` (M4). Independiente | done 2026-07-11 (F-0021 released) | cc |
| AR-016 | 2 | **Mobile Overview: Posiciones Destacadas** — top-5 plano por valuación con sparklines de área 72×28 y "Ver todas ›"; la lista agrupada queda solo en /portfolio/posiciones. Spec listo: `features/F-0022.md` (M6). Toca `MiniSpark` en `MobilePositionsList.jsx` — mismo archivo que AR-009: si AR-009 se hace, coordinar orden para evitar conflicto | done 2026-07-12 (F-0022 released) | cc |
| AR-017 | 3 | **Mobile: bottom nav con pill activo** — indicador teal-dim en ítem activo (incl. "Más" con sheet abierto), sin saltos de layout. Spec listo: `features/F-0023.md` (M7). Independiente, cualquier momento | done 2026-07-12 (F-0023 released) | cc |
| AR-018 | 3 | **Mobile Overview: reorden final** — hero → asignación → destacadas → KPI carousel al final; normalizar spacing. Spec listo: `features/F-0024.md` (M8). Correr último: depende de AR-015 y AR-016 | done 2026-07-12 (F-0024 released) | cc |
| AR-019 | ✅ | **Mobile Overview iteración 2: paridad con mockup** — sacar PageHeader "Overview" solo en mobile, selector de portfolio con chevron + row 2 compacta, gradiente del hero visible (hoy casi negro), recortar gap valor→chart. Feedback de Augusto con screenshots 2026-07-12. Decisión: period chips quedan bajo el chart (no dropdown en header) | done 2026-07-12 (F-0025, 4/4 steps, merged+pushed a main — reconciliado 2026-07-14, Cowork) | cc |
| AR-020 | ✅ | **Bottom nav: pill prominente estilo mockup** — el pill de F-0023 quedó tímido (teal-dim 12% casi invisible); subir presencia, padding, strokeWidth del icono activo | done 2026-07-12 (F-0026, 3/3 steps, merged+pushed a main — reconciliado 2026-07-14, Cowork) | cc |
| AR-021 | ✅ | **Sparklines faltantes para bonos** — causa real: `data912` no tiene histórico para T15E7/TTD26 (letras/bonos CER cortos, responde 200 con `{"Error":...}`, no array). Fix: `useSparklines.js` cae a `getEodSeries` (mismo fallback ya usado por `PositionDetailModal`/`portfolioHistoryService`, sin cliente nuevo) cuando data912 no devuelve array; `MobilePositionsList.jsx` reserva el espacio del sparkline (`div w-[72px] h-[28px]`) cuando no hay data, en los 2 puntos (featured + lista agrupada). El loop automático (`npm start F-0027`) se trabó en step 1 por un límite de scoping del plan (diagnóstico-only vs. implementación), no por el código — el fixer (Opus, vía S-039) ya había encontrado la causa y el fix correctos en su primer intento, rechazados por el reviewer solo por tocar el fetch antes de tiempo. Cerrado manual fuera del loop sobre la misma rama, sin descartar el trabajo del fixer. Verificado en Supabase (lectura): T15E7 resuelve 352 filas, TTD26 354 vía `daily_prices`. `tsc` limpio, 99/99 tests. Commit `ad2b285`, mergeado a main y pusheado, deploy `dpl_7L7fhuvn9DcZXVHnGYExAnxwe9g8` verificado READY (Vercel API) | done 2026-07-16 (cierre manual Claude Code + Cowork, loop quedó en `blocked` sin tocar) | cc |
| AR-022 | 2 | **Reclasificación carry/portfolio de `fci_lots` no es atómica + posible desincronización UI↔DB** — `fciService.js` hacía `UPDATE fci_lots.tipo` y el `INSERT` de auditoría (`fci_lot_context_changes`) en dos llamadas separadas sin transacción. **Fix causa #1:** RPC `reclassify_fci_lot` (`supabase/migrations/031_reclassify_fci_lot_rpc.sql`, `FOR UPDATE` + UPDATE+INSERT atómicos), `fciService.moveLotContext` migrado a llamarla, 4 tests de regresión en `fciService.moveLotContext.test.js` (camino feliz, fallo revierte, no-op cuando ya está en destino) — **migración aplicada a `wwzocpcolgdzkvcigchj` (Cowork, 2026-07-13, OK explícito de Augusto), función `reclassify_fci_lot` verificada en `pg_proc`**. Mergeado a `main` y pusheado (`61564f3`, 2026-07-13) — falta el deploy para que el código en prod use la RPC. **Causa #2 (investigada, resultado distinto al diagnóstico original):** a la fecha de este fix (2026-07-13, mismo día) los 4 lotes de Alpha Renta Capital Pesos - Clase B en Alycbur tienen `tipo='carry'` en la base — NO `'portfolio'` como decía el diagnóstico previo — y coincide con el último registro de auditoría (`fci_lot_context_changes`, 2026-06-06 02:10 UTC) y con lo que muestra la UI; no hay desync activo hoy, así que no se tocó ningún dato. `updated_at` de 3 de los 4 lotes no cambia desde 2026-06-06 (confirma que nadie escribió `tipo` desde entonces vía trigger `update_fci_lots_updated_at`), por lo que el diagnóstico original probablemente leyó mal la consulta, no un cambio de estado posterior. Sí se encontró evidencia directa de la causa #1 en el propio historial: para estos mismos 4 lotes, el tramo 2026-06-05 22:29→2026-06-06 02:10 muestra un `tipo_anterior` en el audit que no coincide con el último `tipo_nuevo` logueado — un UPDATE aplicado sin su INSERT de auditoría correspondiente, el patrón exacto que la RPC corrige. React Query/cache descartado como causa (esta data no pasa por react-query, se carga con `useState`+`useEffect` directo en `useFciLotEngine`, sin persistencia en localStorage); RLS descartado (policy simple `auth.uid()=user_id`, no filtra por valor). **Confirmado por Augusto en UI (2026-07-20):** ve 'carry' para los 4 lotes, coincide con DB/auditoría — es la clasificación correcta, sin acción adicional. Rama `fix/AR-022-reclasificacion-carry-portfolio-atomica` mergeada a `main` y pusheada a `portfolio-tracker` (sin deploy manual — depende del pipeline de Vercel). Prompt: `system/prompts/AR-022-fix-reclasificacion-carry-portfolio.md` | done 2026-07-13 — migración aplicada + código mergeado/pusheado a main (`61564f3`); deploy a Vercel confirmado en vivo (`dpl_D38c8aCnzMjBxACWTedP9pqNTYDS`, READY, producción, verificado vía Vercel API por Cowork); tipo real de los 4 lotes en disputa sin corregir (ya estaban en 'carry', consistentes con DB/UI/auditoría, no hacía falta) | cc |

## 3 Salteñas [tres-saltenas]

Microemprendimiento de empanadas de Dani (no Augusto). Augusto = backup/analista, Dani = mano de obra que carga datos desde el celular. Spec completa en `tres-saltenas/SPEC-MVP.md`. Target agregado a `targets.json` 2026-07-08 (Cowork) con placeholders — falta Neon real (ver TS-001).

| ID | P | Descripción | Estado | Ejecutor |
|----|---|-------------|--------|----------|
| TS-001 | 1 | Bootstrap del repo (Next.js/tRPC/Prisma/Clerk) según `BOOTSTRAP-PROMPT.md`. `tsc` verde, `prisma db push` + seed corridos contra `tres-saltenas-dev` (Producto "Empanada JyQ" + receta + ConfigManoDeObra reales). Repo en GitHub `augustomartinez87/tres-saltenas` (privado), commit `126ec6e` con SPEC-MVP/BOOTSTRAP-PROMPT/NEXT-STEPS ya versionados | done 2026-07-08 (Cowork + Claude Code local) — único pendiente no bloqueante: Clerk real (hoy `.env` tiene keys placeholder, no impide typecheck/build) | cc |
| TS-002 | 2 | `targets.json` + `orchestrator/.env` completos 2026-07-08: prod `tres-saltenas` (host `ep-tiny-voice-atetw10j`) en `prodDbPatterns`; dev `tres-saltenas-dev` (host `ep-frosty-wildflower-atkvtbza`) en `TRES_SALTENAS_DEV_DATABASE_URL` y en `tres-saltenas/.env` (DATABASE_URL pooled + DIRECT_URL direct) | done 2026-07-08 (Cowork) | cc |
| TS-003 | 3 | ~~Cerrar preguntas abiertas del spec con Dani~~ — resuelto por Augusto directamente 2026-07-08 sin necesidad de Dani (ver SPEC-MVP.md §8): patrón de ventas no se hardcodea, sueldo $170k confirmado ajeno al negocio, precios de compra se cargan en runtime | done 2026-07-08 (Cowork) | manual |
| TS-004 | 3 | Reportes mensuales comparativos + gráfico de evolución de costo/margen — pedido explícito de Dani | pending (esperar feedback real de Dani antes de construir esto — ver TS-012) | cc |
| TS-012 | ✅ | **Sin navegación entre pantallas — bloquea que Dani lo use en la práctica** — `layout.tsx` no tenía ningún nav ni links entre secciones (verificado 2026-07-23, Cowork). Resuelto vía orquestador (`F-0029`, 4/4 steps, primer feature de tres-saltenas corrido 100% por `npm start` en vez de prompt manual — cambio de flujo pedido explícitamente por Augusto). `src/components/NavBar.tsx` nuevo (tira de pills `overflow-x-auto`, `max-w-lg mx-auto`, sticky, pill activa vía `usePathname()`), 9 destinos (Dashboard/Ventas/Compras/Producción/Insumos/Gastos/Retiros/Mi Plata/Recetas). Montado en `layout.tsx` envuelto en `<Show when="signed-in">` de `@clerk/nextjs` (no `<SignedIn>` — el reviewer lo marcó como ADR-0085 "supuesto del agente" para revisión; **verificado contra doc oficial de Clerk (`@clerk/nextjs` `^7.5.14` real en `package.json`): `<Show when="signed-in">` es API real y actual de v7, no alucinada — ADR confirmado, sin acción**). Incluyó también `/sign-up` (ver TS-013). Step 1 tuvo un CHANGES_REQUESTED real del reviewer (NavBar se montaba sin gating, visible en /sign-in) resuelto en el reintento — control funcionando como debe. Commit merge `e910d288`, push a main, deploy `dpl_2qxoHHbQqcEboWSQo9mbVZQkubhK` verificado READY en producción (Vercel API) | done 2026-07-23 (orquestador `npm start F-0029`, verificado Cowork) | auto |
| TS-013 | 1 | **Auto-registro sin invitación** — solo existía `/sign-in`, ninguna forma de que alguien (Dani, o cualquiera con el link) se registre solo; había que invitar a mano desde el dashboard de Clerk. Resuelto en el mismo `F-0029`: `src/app/sign-up/[[...sign-up]]/page.tsx` con `<SignUp/>` (mismo layout centrado que sign-in), cross-links agregados en ambas páginas (`¿No tenés cuenta? → Registrate` / `¿Ya tenés cuenta? → Iniciá sesión`). **Pendiente fuera de código, acción de Augusto:** confirmar que "Sign-ups" esté habilitado en el dashboard de Clerk (clerk.com) — si está deshabilitado, `/sign-up` se ve pero Clerk rechaza el alta igual. Nota de seguridad: al ser self-serve completamente abierto (sin allowlist de dominio/email), cualquiera con el link de `/sign-up` puede crear una cuenta y ver los datos del negocio — aceptado como riesgo consciente por Augusto dado el tamaño (1-2 usuarios reales) | done (código) 2026-07-23 (orquestador `npm start F-0029`) — verificar toggle de Clerk Dashboard | auto |
| TS-014 | ✅ | **App no forzaba login en ningún lado — bug de seguridad real, no cosmético** — descubierto por Augusto en vivo 2026-07-23: entrando a `https://tres-saltenas-beta.vercel.app/` sin sesión, veía el dashboard completo con datos reales del negocio. Causa raíz doble (código leído y confirmado): `src/middleware.ts` llamaba `clerkMiddleware()` sin ningún `.protect()` (wireado pero sin efecto); `src/server/api/trpc.ts` solo exponía `publicProcedure`, sin `auth()` ni chequeo de `userId` en ningún router — el `<Show when="signed-in">` del NavBar (F-0029) era puramente cosmético, no protegía nada. Resuelto vía orquestador (`F-0030`, 9/9 steps, 2 escalaciones a Opus-fixer en steps 5/6 por tests viejos que creaban el caller sin `userId` — resueltas automático, sin quedar bloqueado). Fix: middleware ahora usa `createRouteMatcher(["/sign-in(.*)", "/sign-up(.*)"])` + `auth.protect()` para todo lo demás (sintaxis verificada contra doc oficial de Clerk v7, no asumida); `trpc.ts` agrega `protectedProcedure` (chequea `ctx.userId`, tira `TRPCError UNAUTHORIZED`) y todos los routers de negocio migrados de `publicProcedure` a `protectedProcedure`; test nuevo (`auth.test.ts`) cubre el rechazo sin sesión. Verificado en vivo (Cowork, browser real): navegar a `/` sin sesión ahora redirige automático a sign-in (`fresh-finch-16.accounts.dev/sign-in?redirect_url=...`), cero datos visibles. Commit merge `9f2e76c7`, deploy `dpl_8SSN9eS4rRYUXtbAbdtEQCgpBDRi` verificado READY en producción (Vercel API). 4 ADRs (0086-0089) marcados "supuesto del agente" para revisión — todos de detalle de testing (cómo inyectar `userId` en los callers de test), sin implicancia de seguridad, revisados sin objeción | done 2026-07-23 (orquestador `npm start F-0030`, verificado Cowork en vivo) | auto |
| TS-005 | 2 | Módulo Compras — F-0013. Steps 1-6 done, mergeado y pusheado a `main` en GitHub 2026-07-09 (verificaciones locales limpias). **Corrección:** el log de release dice "Vercel deploya prod automáticamente" pero es texto genérico — no hay proyecto Vercel para tres-saltenas (confirmado vía API: solo existen kredy-ap/augusto-os/spensiv-tracker/portfolio-tracker-argos2026). No hay nada en vivo todavía, solo el push a GitHub. Steps 4 y 6 se trabaron en rechazos repetidos del reviewer sin que el builder corrigiera el bug real — intervención manual (Cowork): fecha local en `compras/page.tsx`, `calcularNuevoStock` eliminado + `compras.test.ts` reescrito contra el router real vía `createCallerFactory` | done 2026-07-09 (código en main, sin deploy) | cc |
| TS-011 | ✅ | **Clerk real en Vercel** — errores de `/middleware` ("Missing publishableKey" / "Publishable key not valid", 2026-07-11/12) confirmados resueltos: `get_runtime_errors` (Vercel API, 2026-07-23) sin errores en los últimos 7 días, y `get_runtime_logs` muestra pedidos reales exitosos (200) contra el deploy actual (`dpl_FG3zqqvCoT8MgASAGXcPnVExFvqK`, commit `f9752b1`) — `GET /`, `GET /gastos`, `dashboard.resumenPorPeriodo`, `gasto.list` — solo posibles si el login de Clerk ya funciona. MVP completo de tres-saltenas (TS-005 a TS-010) usable en producción | done (confirmado por Vercel API, Cowork 2026-07-23) | manual |
| TS-006 | ✅ | Módulo Insumos/Stock — F-0014, mergeado (`80236ff`) | done (loop, hallado ya mergeado — reconciliado 2026-07-16, Cowork) | cc |
| TS-007 | ✅ | Módulo Producción por lote — F-0015, mergeado (`6f4a980`) | done (loop, hallado ya mergeado — reconciliado 2026-07-16, Cowork) | cc |
| TS-008 | ✅ | Módulo Ventas — F-0016, mergeado (`04631fc`) | done (loop, hallado ya mergeado — reconciliado 2026-07-16, Cowork) | cc |
| TS-009 | ✅ | Dashboard de costeo — F-0017, mergeado (`b7d60af`) | done (loop, hallado ya mergeado — reconciliado 2026-07-16, Cowork) | cc |
| TS-010 | ✅ | Gastos operativos + Retiros + saldo del negocio — F-0018, mergeado (`8abff94`) + fix post-merge `f9752b1` (prisma generate en postinstall, arreglaba el build roto de Vercel) | done (loop, hallado ya mergeado — reconciliado 2026-07-16, Cowork). MVP completo de tres-saltenas (TS-005 a TS-010) en producción: deploy `dpl_FG3zqqvCoT8MgASAGXcPnVExFvqK` verificado READY (Vercel API, commit `f9752b1` = HEAD de main) | cc |
