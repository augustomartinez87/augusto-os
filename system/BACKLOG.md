# Backlog â€” augusto-os

Formato: `P<n>` = prioridad (1 = mÃ¡s urgente) Â· `[target]` = repo destino Â· `Ejecutor` = quiÃ©n ejecuta (ver CONVENTIONS Â§5)

---

## Sistema (orchestrator / augusto-os)

| ID | P | DescripciÃ³n | Estado | Ejecutor |
|----|---|-------------|--------|----------|
| S-000 | âœ… | Aislamiento de prod: DB no-prod + guard anti-prod en el loop | done 2026-06-19 | cc |
| S-000b | âœ… | Dev DB de Spensiv = Neon; secreto gitignored; esquema materializado | done 2026-06-19 | cc |
| S-000c | âœ… | Seed data de prueba para Spensiv en Neon (no hay seed script â€” datos vacÃ­os) | done 2026-07-03 (F-0010, resuelto tambiÃ©n SPT-001) | cc |
| S-001 | âœ… | **Code Reviewer** â€” rol independiente (Opus, solo lectura) que revisa el `git diff HEAD` de cada step antes de commitear: scope creep vs spec, reglas de dominio (TNA, mutuo/pagarÃ©, prisma), errores de lÃ³gica que tsc/tests no agarran. Insertado entre Verifier/QA y commitStep, mismo tope de 3 reintentos compartido, sin gate humano salvo rechazo persistente | done 2026-06-26 (Claude Code) | cc |
| S-002 | âœ… | **Gating por OPERATOR_STATE** â€” `operator-state.ts` centraliza lectura de `OPERATOR_STATE.yaml` (fail-safe a PRODUCT). Telegram (`notifyGate`/`notifyDeployed`/`notifyReleaseFailed`) muteado en SLEEP, acortado en OFFICE+short, sin cambios en PRODUCT. El gate humano sigue bloqueando igual en SLEEP, solo no avisa. Base de S-004 | done 2026-06-26 (Claude Code) | cc |
| S-003 | âœ… | Fase 3: Multi-target routing (spensiv vs argos por feature) â€” `targets.json` define `verifyCmd`/`lintCmd`/`testCmd`/`qaBaseUrl` por target, consumido por `targets.ts`/`executor.ts`/`planner.ts`; features target-aware desde F-0001 | done (retroactivo, reconciliado 2026-07-07) | cc |
| S-007b | âœ… | **Toggle remoto de OPERATOR_STATE** â€” selector PRODUCT/OFFICE/SLEEP en el dashboard (3 chips arriba del header), `sync.ts` pullea `orch_operator_state` cada 5s y reescribe `OPERATOR_STATE.yaml` local si cambiÃ³ (dedup por `updated_at`, preserva comentarios de cabecera). Tabla nueva con RLS anon select+update solo en id=1, sin insert/delete. Fast-follow de S-007/S-002 | done 2026-06-26 (Claude Code) | cc |
| S-004 | âœ… | **Loops nocturnos** â€” `autopilot.ts`, llamado desde `sync.ts` cada tick: en modo SLEEP y con el loop libre, toma la primera item `pending`/P2+/sin keywords de riesgo (dinero/prod/legal/migraciÃ³n) del backlog por orden de prioridad, fuerza el spec vÃ­a Architect (target tomado de la secciÃ³n, no por keyword-detection), spawnea `npm start <featureId>` fire-and-forget. Marca la fila `armado (autopilot)` al elegir y `done (autopilot)` al deployar; si falla el release no se reintenta solo. Cap 5/dÃ­a, lock con timeout 10min, ADR-log de la decisiÃ³n. Gates duros (S-002) siguen pausando igual en silencio | done 2026-06-27 (Claude Code) | cc |
| S-005 | 5 | Fase 5: Product Analyst (backlog desde mÃ©tricas de uso real) | pending | cc |
| S-006 | âœ… | Celu Fase 1: bot de Telegram (AlantORCH) â€” notifica gates y permite aprobar/idear desde el celu. `telegram.ts`+`bot.ts`+hook en gates+`npm run bot`, sin deps (fetch nativo). Probado- responde y guarda ideas | done 2026-06-25 | cc |
| S-007 | âœ… | **Dashboard web mobile-first** (control plane Supabase) â€” v1 entregado como HTML estÃ¡tico single-file (`dashboard/index.html`), NO Next.js (decisiÃ³n real, ver RUNBOOK): tabs Loop/Backlog/Ideas, polling 5s, deploy Vercel auto. Fase A (sync) + Fase B (la web) completas. Fast-follow: agent presence (S-015), realtime, diagramas (S-016) | done 2026-06-26 | cc |
| S-008 | âœ… | **Intake + Architect agent** â€” split en 2: (a) **Intake barato** (heurÃ­stica, sin LLM): detecta target, busca ADR/features/backlog relacionados (grep), clasifica bug/feature/arquitectura, resume contexto; (b) **Architect (Opus on-demand)** solo cuando la idea necesita arquitectura â†’ escribe el `features/F-XXXX.md`. CLI `npm run intake`. ADR-0028/0029 | done 2026-06-26 (Claude Code) | cc |
| S-016 | âœ… | **Diagramas de flujo de trabajo** â€” guÃ­a visual de quÃ© hacer en cada caso (gate falla, deploy, etc.); candidata a pestaÃ±a en el dashboard mobile (S-007). Idea de Augusto | done 2026-07-06 (F-0011, Claude Code) | cc |
| S-017 | âœ… | Fix ADR auto-log: inserta los ADR nuevos DENTRO del fence del template (rompe el markdown). Anclar la inserciÃ³n despuÃ©s del bloque de template, no tras el header | done 2026-06-26 (Claude Code) | cc |
| S-018 | âœ… | Auto-deploy en verde + sin gates por-step + avisos deploy/fallo por Telegram (ADR-0019) | done 2026-06-25 | cc |
| S-019 | âœ… | **Hardening del loop** (review OpenCode): `getNextPendingStep` saltea steps `blocked`; `pushMain` verifica estar en main; dedup `appendProgress` al resumir; remover `awaitingPushApproval` (muerto); centralizar strings de modelo en config (sirve para S-014) | done 2026-06-26 (Claude Code) | cc |
| S-020 | âœ… | **Builder ve restricciones del spec** â€” pasar "Fuera de alcance" + "Restricciones clave" del F-XXXX.md al prompt del executor (hoy solo ve `step.desc`). Afecta calidad/cumplimiento de reglas de dominio | done 2026-06-26 (Claude Code) | cc |
| S-021 | âœ… | **Tests del core** â€” `state.ts`, `index.ts` (loop), `gates.ts`. Hoy solo existe `adr.test.ts`; el sistema que deploya a prod es el menos testeado | done 2026-06-26 (Claude Code) | cc |
| S-022 | âœ… | **Dashboard â†’ vista Operaciones** (rediseÃ±o de IA: roster honesto de etapas reales, hero "quiÃ©n tiene la posta", feed de deltas no-logs; preserva ideas/mode/backlog). Fast-follow de S-007/S-015 | done 2026-06-27 (Claude Code) | cc |
| S-025 | âœ… | **RotaciÃ³n/retenciÃ³n de logs** â€” `log-cleanup.ts`: disco 30d (`logs/*.log`, `loop-*.log`, `blocked.log`; preserva `orchestrator.log` + run activo), Supabase 7d (`orch_logs` DELETE por `ts`), throttle 1h desde `sync.ts`. ADR-0036 | done 2026-06-28 (Claude Code) | cc |
| S-024 | âœ… | **Evaluador de posts de X** â€” pegÃ¡s un post guardado y el Architect lo juzga contra tu arquitectura real (lee `system/`): Â¿ya implementado? / Â¿vale la pena + beneficio concreto? / Â¿es bait y por quÃ©? VersiÃ³n free = un prompt/skill; conecta con S-008 | done 2026-07-07 (F-0012, Claude Code) | cc |
| S-034 | âœ… | **Fallback + visibilidad de saldo DeepSeek (Scout KGL)** â€” `deepseek.ts`: `DeepSeekInsufficientBalanceError` por status code real (402), nunca substring; `AbortController` compartido entre las 3 investigaciones para cortarlas de una apenas se detecta. `fetchDeepSeekBalance`/`shouldRunBalanceCheck` (throttle 7min) integrados en `sync.ts` â†’ tabla `orch_scout_status` (Supabase). Barra + pill en `dashboard/index.html` (single-file, sin deps nuevas). 12 tests nuevos | done 2026-07-07 (Claude Code) | cc |
| S-023 | 4 | **Closed learning loop** (idea de Hermes) â€” cuando el loop resuelve un patrÃ³n recurrente, auto-generar un spec/template reutilizable para no re-razonar. EvoluciÃ³n del ADR/specs, en NUESTRO sistema (no adoptar Hermes como motor) | pending | cc |
| S-014 | 2 | **Routing multi-modelo** â€” Builder barato (DeepSeek V4 vÃ­a Claude Code Router) con Opus de Planner; el Verifier cubre el riesgo. Pilot en 1 feature, medir reintentos. Eval en `system/MODEL-ROUTING.md` | pending | cc |
| S-036 | âœ… | **Hardening de limits + STATE** (bugs reales vistos 2026-07-11 en F-0019): (a) `parseResetTime` fallback = ahora+5h ciego cuando el output de CC no trae hora de reset; (b) `saveState` no atÃ³mico, corrompÃ­a STATE.json con Ctrl+C a mitad de escritura. F-0028, 7/7 steps completos y deployado (`ccd2747`). `saveState` con temp+`renameSync`, `loadState` tolerante a basura trailing; `probeAvailability()`/`isProbeAvailable()` en `limits.ts` (ping barato vÃ­a `MODEL_INTAKE`, decide disponibilidad por `isUsageLimitError`, no por exit code â€” evita el falso negativo del `--max-turns 1`); `handleUsageLimit` sondea cada `PROBE_INTERVAL_MS` cuando no hay hora de reset parseable; probe al arranque si `pausedUntil` es futuro. Step 4 se trabÃ³ por reviewer 2 veces y escalÃ³ a Opus (S-039) en condiciones reales por primera vez â€” el fixer lo resolviÃ³ en 1 intento, sin marcar el step bloqueado; validaciÃ³n real de S-039 registrada. **Nota de calidad:** el reviewer pidiÃ³ en step 5 extraer los regexes de `parseResetTime`/`hasExplicitResetTime` a constantes compartidas (riesgo de drift si se edita una sin la otra) â€” el fix aprobado NO lo hizo, quedaron duplicados inline; no es un bug activo hoy (son idÃ©nticos) pero es deuda real que pasÃ³ el review sin resolverse | done 2026-07-14 (loop, con escalaciÃ³n S-039 validada en vivo) | cc |
| S-037 | âœ… | **QA visual ignora `qaBaseUrl` del target** â€” `index.ts:447` hacÃ­a `process.env.QA_BASE_URL ?? 'http://localhost:3000'` sin leer `targets.json` (argos = 5173): el QA de argos nunca corrÃ­a salvo env var manual. Fix: `resolveQaBaseUrl()` nueva en `targets.ts` (chequeo de string vacÃ­o, no `??`, porque `sistema` tiene `qaBaseUrl: ""`) â€” prioridad env var â†’ `target.qaBaseUrl` â†’ `localhost:3000`. 5 tests nuevos (`targets.test.ts`). Visto 2026-07-11 en F-0020. Incidente en el camino: otra sesiÃ³n escribiÃ³ `BACKLOG.md` (auditorÃ­a UX tres-saltenas, TS-015/TS-022) mientras corrÃ­a el loop â€” contenciÃ³n tipo S-042, resuelta por el fixer sin pÃ©rdida de datos (ver ADR-0091) | done 2026-07-24 (F-0031, orquestador, verificado en `origin/master` por Cowork: cÃ³digo+tests leÃ­dos completos) | cc |
| S-044 | 3 | **Panel remoto de ejecuciÃ³n de features (celu â†’ PC)** â€” idea a desarrollar, sin spec todavÃ­a: tocar "ejecutar F-XXXX" desde el celu (dashboard o Telegram) abre una ventana de cmd en la PC de Augusto corriendo `npm start F-XXXX`. Requiere dos piezas nuevas: (a) servicio local en la PC que reciba el pedido y spawnee el cmd; (b) acceso remoto vÃ­a Tailscale (red privada punto a punto, sin exponer puertos a internet) para que el celu le hable a la PC estando afuera de la red de casa. Se apoya en S-006 (bot Telegram) y S-007 (dashboard web) ya existentes en vez de partir de cero | idea (guardada 2026-07-24, no iniciar sin spec ni prioridad definida) | cc |
| S-041 | âœ… | **STATE.json puede desincronizarse del git real bajo contenciÃ³n de lock** â€” incidente real 2026-07-13/14 en F-0028 step 3: el fixer (S-039) resolviÃ³ el step correctamente (commit `099927c`, 75 inserciones vÃ¡lidas, verificado a mano), pero `STATE.json` quedÃ³ registrando `blocked`/`commit: null` con un mensaje de error que ya no correspondÃ­a al resultado final. **Causa raÃ­z confirmada (Cowork, 2026-07-14, ver `state.ts`/`index.ts`/`autopilot.ts`):** `STATE_PATH` es una ruta Ãºnica y fija (`state.ts:7`), sin namespacing por feature/target; `index.ts main()` (run manual) nunca llama `acquireLock()`/`releaseLock()` â€” el mutex `AUTOPILOT.lock` solo protege la ventana de spawn de autopilot contra auto-duplicarse, un run manual no lo consulta; cada target tiene un Ãºnico working tree fijo (`targets.json`), sin `git worktree` en ningÃºn lado. Cualquier par de runs concurrentes (manual+autopilot o manual+manual, mismo target o no) comparten el mismo STATE.json y pueden chocar en el working tree. Fix diseÃ±ado: extender `acquireLock`/`releaseLock` (ya testeados, ya usan heartbeat de `loop-heartbeat.ts` para liveness) para que cubran la ejecuciÃ³n completa de `index.ts main()`, no solo el spawn de autopilot â€” resuelve STATE.json y working tree como efecto colateral, sin tocar `state.ts`/`git.ts`. Prompt listo: `system/prompts/S-041-lock-cross-run-git-state.md`. **Implementado** (Claude Code, 2026-07-14): `acquireRunLock()` nueva en `index.ts`, envuelve `acquireLock`/`releaseLock` existentes (sin tocarlos); adquirida como primera acciÃ³n de `main()` (arranque y resume), liberada en `finally` que cubre todo el run. Guard `isDirectRun` agregado para que `index.test.ts` pueda importar sin disparar el CLI real (ADR-0081). 4 tests nuevos en `index.test.ts`. `state.ts`/`git.ts` intactos. Verificado por Cowork: diff (156 inserciones/38 borrados, solo `index.ts`+`index.test.ts`) leÃ­do completo y coherente con el spec, lÃ³gica de lock/try-finally correcta, `isDirectRun` correcto al final del archivo, master/`HEAD` sin tocar (branch `fix/S-041-lock-cross-run`, commit `3eeccc2`, sin merge/push). ADR-0082 (decisiÃ³n central) + ADR-0081 (guard de testing) | âœ… done 2026-07-14 (mergeado a `master` por Augusto sobre commit `3eeccc2` â€” sin push todavÃ­a) | cc |
| S-043 | âœ… | **`_prisma_migrations` de Kredy prod (Neon) sin reconciliar + `prisma/migrations` nunca versionado** â€” descubierto 2026-07-15 al aplicar la migraciÃ³n de SP-014: `prisma migrate status` mostraba 15 pendientes en vez de 1. Causa raÃ­z doble, confirmada de punta a punta: (a) el paso 2 de `system/MIGRATION-kredy-to-neon.md` (S-010, corte 2026-07-08) usÃ³ `prisma db push` para materializar el schema en Neon â€” sincroniza estructura pero nunca escribe en `_prisma_migrations`, dejando el tracking vacÃ­o aunque la estructura real estuviera al dÃ­a; (b) hallazgo mayor no anticipado: `.gitignore` tenÃ­a `prisma/migrations` desde siempre â€” la carpeta ENTERA nunca estuvo versionada, ni siquiera la migraciÃ³n nueva de SP-014 (cada checkout podÃ­a tener un historial distinto). De las 14 migraciones "viejas" pendientes, 12 eran reconciliaciÃ³n de tracking legÃ­tima (estructura ya existÃ­a, solo faltaba el registro â€” resueltas vÃ­a `prisma migrate resolve --applied`, con reintentos espaciados por contenciÃ³n de advisory lock en Neon, causa exacta no determinada pero descartada relaciÃ³n con deploys de Vercel â€” verificado vÃ­a API que no habÃ­a ningÃºn deploy corriendo); 2 (`add_card_closing_schedule`, `add_tags`) eran basura real de la era "Spensiv" (antes del pivot a Kredy) sin ningÃºn modelo correspondiente en `schema.prisma` actual (confirmado por grep) â€” una de ellas habÃ­a quedado marcada `--applied` por error (la tabla no existe en prod), corregida con `--rolled-back` antes de borrar la carpeta. Resultado: `migrate deploy` aplicÃ³ Ãºnicamente `20260715120000_collection_reminders_phone_alias` (la Ãºnica real), backfill corrido (19 Persons sin phone, 0 con Contact vinculado â€” 0 backfilled, esperado), `tsc` limpio. `prisma/migrations` sacado del `.gitignore` y las 13 migraciones reales versionadas por primera vez (commit `aa47da4`, pusheado a main). Ver `system/prompts/SP-014-aplicar-migracion-prod.md` (evolucionÃ³ varias veces durante la sesiÃ³n, cada gate resuelto antes de avanzar al siguiente). Pendiente menor: `system/MIGRATION-kredy-to-neon.md` no quedÃ³ actualizado con la nota de reconciliar `_prisma_migrations` tras un `db push` â€” de baja prioridad ahora que ya no hay next arrastre pendiente | done 2026-07-15 (Claude Code + Cowork, verificado paso a paso) | cc |
| S-042 | âœ… | **Sesiones manuales de Claude Code CLI fuera del loop pueden truncar archivos en carrera** â€” incidente real 2026-07-15 en `kredy`: 2 sesiones de Claude Code corridas directo (fuera de `npm start`, sin pasar por `index.ts`/`acquireRunLock` de S-041) dejaron 6 archivos truncados a mitad de escritura + `.git/index.lock` huÃ©rfano. El fix de S-041 (`acquireRunLock`) NO cubre sesiones CLI directas fuera del orquestador. RecuperaciÃ³n manual documentada en `RUNBOOK.md` Â§5. **Resuelto vÃ­a F-0032 (2026-07-24, orquestador, 7/7 steps):** nuevo `orchestrator/src/check-repo-health.ts` + script `npm run check-repo-health -- <target>` â€” chequeo solo diagnÃ³stico (no bloquea nada): working tree sucio (`git status --porcelain`, distingue "sucio" de "git no pudo correr"), `.git/index.lock` huÃ©rfano con antigÃ¼edad, y `tsc --noEmit`; resumen âœ…/âš ï¸ + exit code 0/1. Motivado por reproducciÃ³n en vivo del mismo patrÃ³n esta sesiÃ³n (otra sesiÃ³n escribiÃ³ `BACKLOG.md` durante F-0031). 4 ADRs marcados "supuesto del agente" (0092-0095), revisados por Cowork sin objeciones â€” cÃ³digo leÃ­do completo en `origin/master`, diseÃ±o defensivo correcto (distingue fallo de ejecuciÃ³n de git vs. working tree sucio, no mata procesos, no asume nada del lock). Pendiente menor no bloqueante: probar `npm run check-repo-health` en vivo contra un target real (kredy/argos) para confirmar la salida en la prÃ¡ctica | done 2026-07-24 (F-0032, orquestador, verificado en `origin/master` por Cowork) | cc |
| S-039 | âœ… | **EscalaciÃ³n automÃ¡tica de steps trabados (Opus-fixer) en vez de gate/parada manual** â€” reemplaza `haltBlocked` (S-038/0c53d49) por `escalateOrHalt`: cuando un step falla 3 veces (builder/verifier/QA/reviewer), antes de bloquear se escala a `MODEL_FIXER` (Opus, `models.ts`) en sesiÃ³n fresca (no resume la sesiÃ³n trabada del builder) con el historial completo de fallos acumulado en `Step.failureHistory` (`state.ts`, cap 6 entradas Ã— 800 chars, `appendFailureHistory`) â€” no solo el Ãºltimo error. Nuevo `orchestrator/src/escalation.ts`: `escalateStep()` corre el verifier tras cada intento del fixer (presupuesto propio de `MAX_FIXER_ATTEMPTS=2`), reusa `RESTRICCIONES_ABSOLUTAS` (extraÃ­da de `executor.ts`) sin relajarlas para el fixer. Si el fixer resuelve el step, nunca se marca `blocked` de cara al usuario y el flujo sigue normal (si fue el reviewer quien originÃ³ la escalaciÃ³n, se re-corre el reviewer sobre el fix antes de commitear â€” no bypasea ese control). Si el fixer tambiÃ©n falla, ahÃ­ sÃ­ se marca `blocked`, se notifica por Telegram y el loop continÃºa (sin `return` forzado) hasta que `getNextPendingStep` corta limpio por falta de trabajo elegible. Gates de dinero/prod/legal (`checkHumanGate`) sin tocar. 11 tests nuevos (`escalation.test.ts` + `appendFailureHistory` en `state.test.ts`) â€” 351/351 en la suite completa, typecheck limpio. **VerificaciÃ³n real pendiente**: cubierto con mocks de `execa`/verifier inyectados (patrÃ³n `ReviewerOpts`), no se disparÃ³ una escalaciÃ³n real contra un target vivo (requiere una sesiÃ³n real de Claude Code + repo target) â€” validar en el prÃ³ximo step que se trabe de verdad | done 2026-07-13 (Claude Code) | cc |
| S-038 | âœ… | **ADR auto-log genera ADRs vacÃ­os** â€” `executor.ts` pasaba el stdout crudo JSON-stringificado a `parseAdrBlocks` en vez del `.text` ya extraÃ­do por `parseClaudeJson`; sin saltos de lÃ­nea reales, la regex multilÃ­nea de `extractField` no matchea nada. Fix: `executor.ts` ahora usa el texto plano extraÃ­do por `parseClaudeJson`, con fallback al output crudo solo si el JSON no parsea. Defensa agregada en `appendAdr` (adr.ts): si tÃ­tulo+decisiÃ³n vienen vacÃ­os, no escribe y devuelve `null` (no quema ID). 2 tests de regresiÃ³n en `adr.test.ts` (roundtrip con JSON real vs. bug reproducido) + 2 tests de la defensa nueva â€” 19/19 verdes, 340/340 en la suite completa. Saneadas ADR-0075 a 0078 (vacÃ­os post-2026-07-11) en DECISIONS.md, backup `DECISIONS.md.bak-20260713`. **Verificado con dato real**: se corriÃ³ el fix contra `orchestrator/logs/F-0025-step2.log` (el log real que generÃ³ el ADR-0076 vacÃ­o) â€” antes del fix da campos vacÃ­os, despuÃ©s extrae correctamente target/titulo/decision/contexto/alternativas/consecuencias | done 2026-07-13 (Claude Code) | cc |
| S-015 | âœ… | **Agent team view** â€” presencia real con heartbeat: tabla `orch_presence` (planner+builder), sync.ts emite cada 5s, staleness >30sâ†’"sin seÃ±al" />2minâ†’"posible cuelgue". Dashboard usa presencia como fuente de verdad del roster; `derivePosta` como fallback. Muestra modelo por rol (Opus/Sonnet) | done 2026-06-27 (Claude Code) | cc |
| S-013 | âœ… | Guard anti-reejecuciÃ³n (avisa "ya finalizÃ³" + `--force` antes de gastar tokens) + reintento del aviso de gate por Telegram | done 2026-06-25 | cc |
| S-009 | âœ… | **ADR auto-log** â€” el loop escribe en DECISIONS.md cada decisiÃ³n de diseÃ±o con su Origen. Helper `appendAdr()` + `parseAdrBlocks` + prompt del executor + idempotencia. 13 tests verdes | done 2026-06-25 (Claude Code) | cc |
| S-010 | âœ… | **Migrar Kredy prod: Supabase â†’ Neon** â€” unificar todo lo Prisma en Neon (prod + dev). Runbook en `system/MIGRATION-kredy-to-neon.md`. Toca dinero real â†’ ejecutÃ³ Augusto con OK explÃ­cito | done 2026-07-13 (Augusto) â€” corte de trÃ¡fico + smoke test OK (2026-07-08); proyecto viejo `jymdblurkpadupdqzfzo` borrado definitivamente por Augusto (verificado: ya no aparece en `list_projects`, org `supabase.amartinez` queda con solo `argoscapital` + augusto-os) | manual |
| S-035 | 2 | **Consolidar cuentas Supabase** â€” mover augusto-os desde su cuenta Supabase actual ("supabase2", org `martinezstreet21Org`) a la org `supabase.amartinez` (`gbztwnshsmcwqnqjouhi`, "supabase1"), donde ya vive Argos, vÃ­a Project Transfer nativo de Supabase. Hecho 2026-07-08 (Cowork): invitado `martinezstreet21@gmail.com` como Developer en `supabase.amartinez`, transfer ejecutado desde el lado supabase2 (Brave), confirmado vÃ­a MCP â€” proyecto `exyhnrynpuflbuprmdto` ("martinezstreet21@gmail.com's Project") ahora vive en `supabase.amartinez`, mismo ref/host/keys, sin downtime, sin cambios en Vercel/cÃ³digo. Estado final: **supabase1 = Argos + augusto-os + Kredy viejo (pausado)**; supabase2 (`martinezstreet21Org`) quedÃ³ con 0 proyectos, dado de baja de facto (no se eliminÃ³ la cuenta, solo se vaciÃ³); Neon = Spensiv + Kredy | done 2026-07-08 (Cowork) | manual |
| S-026 | âœ… | **ReconciliaciÃ³n del backlog** â€” auditorÃ­a de estados reales cruzando BACKLOG/PROGRESS/prompts/git; correcciÃ³n de S-009 en CONVENTIONS.md (era "spec, no implementado", estÃ¡ implementado); nota de mapeo S-022(old)â†’S-025; regla append-only de IDs (CONVENTIONS Â§3); huecos S-011/S-012 documentados | done 2026-06-27 (Claude Code) | cc |
| S-027 | âœ… | **Hardening heartbeat del loop + lock por liveness** â€” `LOOP_HEARTBEAT.json` desde `index.ts`; `orch_presence` con rol `loop`; `acquireLock` respeta lock si heartbeat fresco (nunca pisa un proceso vivo); `markBacklogState` retorna bool; dashboard usa heartbeat del loop para Builder | done 2026-06-27 (Claude Code) | cc |
| S-028 | âœ… | **Fix: aprobaciÃ³n remota desde Telegram no desbloqueaba el loop** â€” `pollApprovalOnce()` exportado de `telegram.ts`, embebido en el gate-wait de `runLoop` (~8s latencia mÃ¡x). `npm run bot` ya no es requisito para que el botÃ³n âœ… funcione. ADR-0034 | done 2026-06-28 (Claude Code) | cc |
| S-029 | âœ… | **Fix: 409 Conflict Telegram al correr bot + loop simultÃ¡neamente** â€” `bot-heartbeat.ts`; bot escribe `BOT_HEARTBEAT.json` en cada ciclo; loop chequea `isBotAlive()` antes de `pollApprovalOnce()`: si bot vivo â†’ solo pollea STATE.json, si bot caÃ­do â†’ fallback automÃ¡tico en â‰¤30s. ADR-0035 | done 2026-06-28 (Claude Code) | cc |
| S-030 | âœ… | **ClasificaciÃ³n de ejecutor del backlog (auto/cc/manual) + autopilot por allowlist** â€” campo `Ejecutor` en BACKLOG.md (fuente de verdad), espejado a `orch_backlog`; autopilot cambia a allowlist primaria (`Ejecutor=auto`), keywords de riesgo como red de seguridad secundaria. ADR-0037 | done 2026-06-28 (Claude Code) | cc |
| S-031 | âœ… | **Fix max-turns en Architect/Planner/Reviewer + visibilidad de errores** â€” `MAX_TURNS=15` centralizado en `models.ts`; los tres agentes usan `--max-turns 15` en lugar del literal `1` que cortaba el loop cuando claude emitÃ­a `tool_use` en turno 1. Error de visibilidad: stdout incluido en el `throw` de los tres. Planner refactorizado con `PlannerOpts.callClaude?` injectable (patrÃ³n de Architect/Reviewer). ADR-0038 | done 2026-06-29 (Claude Code) | cc |
| S-045 | âœ… | **Prompt largo a `claude` como argv rompÃ­a el loop en Windows â€” lÃ­mite de lÃ­nea de comandos, no bug de Claude Code** â€” descubierto en vivo (Cowork, 2026-07-24) al correr `npm start F-0033`: falla instantÃ¡nea (~20ms) con `cÃ³digo undefined`, stdout/stderr vacÃ­os, reproducible siempre. Causa raÃ­z confirmada por repro aislado: los 6 puntos donde el orquestador invoca `claude` (`planner.ts`, `architect.ts`, `executor.ts`, `reviewer.ts`, `escalation.ts`, `evaluate.ts`) pasaban el prompt completo (spec + research del Scout, spec+diff en executor/reviewer) como argumento `-p <prompt>` de la lÃ­nea de comandos vÃ­a `execa`. Windows tiene un lÃ­mite duro de ~32.767 caracteres para toda la lÃ­nea de comandos combinada (`CreateProcess`) â€” con prompts grandes (F-0033: ~33.000 caracteres entre spec+research), `CreateProcess` falla ANTES de que el proceso arranque, lo que `execa` con `reject:false` devuelve como `exitCode: undefined` + todo vacÃ­o, indistinguible a simple vista de un crash real de Claude Code. Confirmado con repro mÃ­nimo (prompt de 33k chars fallaba igual invocado directo con `execa`, sin pasar por el orquestador) y con el fix (mismo prompt via `input`/stdin de `execa` en vez de argv â†’ Ã©xito, exit 0). No afectaba features anteriores porque sus specs+research entraban bajo el lÃ­mite â€” F-0033 fue la primera vez que se cruzÃ³. Fix: los 6 archivos ahora pasan `'-p'` sin el prompt como argumento y usan `input: prompt` (stdin) en las opciones de `execa`, sacando el `stdin: 'ignore'` que tenÃ­an antes. `npx tsc --noEmit` limpio. Verificado en vivo: `npm start F-0033` con el fix pasÃ³ el Planner y arrancÃ³ el Builder en el step 1 sin problema | done 2026-07-24 (Cowork, diagnosticado y fixeado en vivo, verificado con `npm start F-0033` real en la mÃ¡quina de Augusto) | cc |
| S-046 | âœ… | **`pullWebIdeas` duplicaba ideas en `FEATURE-INTAKE.md` en cada restart de `sync`** â€” descubierto en revisiÃ³n de estado (Cowork, 2026-07-27): `orch_ideas` nunca borra filas, y el dedup de `sync.ts` vivÃ­a solo en `seenIdeas` (un `Set` en memoria) â€” cada reinicio del proceso `sync` (varios por dÃ­a, ver `orchestrator.log`) volvÃ­a a leer las 7 ideas `source=web` existentes y las reapendeaba a `FEATURE-INTAKE.md`, que llegÃ³ a 333 lÃ­neas (11 ideas reales, el resto duplicado ~40 veces). Fix: `existingIntakeTimestamps()` nueva en `sync.ts` â€” lee el archivo real y usa `created_at` (Ãºnico por idea) como clave de dedup persistente, ademÃ¡s del `Set` en memoria (que sigue evitando relecturas dentro del mismo proceso). `FEATURE-INTAKE.md` saneado a mano a sus 11 lÃ­neas Ãºnicas (4 telegram + 7 web) | done 2026-07-27 (Cowork) | cc |
| S-047 | âœ… | **ReconciliaciÃ³n automÃ¡tica de `system/BACKLOG.md` al liberar un feature a prod** â€” hasta ahora, cuando un F-XXXX resolvÃ­a filas del backlog, habÃ­a que pasar su Estado a `done` a mano despuÃ©s de cada release porque el spec no declaraba en ningÃºn campo estructurado quÃ© filas resolvÃ­a (esa relaciÃ³n solo vivÃ­a en prosa dentro del "Contexto"). Fix, mismo patrÃ³n que `appendAdr`/`appendProgress`: (a) campo opcional `resolves: [ID, ...]` en el frontmatter de los specs (documentado en `_TEMPLATE.md`), parseado por `parseResolvesField()` nueva en `planner.ts`; (b) `orchestrator/src/backlog.ts` nuevo â€” `updateBacklogStatus(featureId, resolves, date)` busca cada ID matchÃ©andolo completo entre pipes (evita colisiones de substring tipo TS-1 vs TS-17), pisa la columna Prioridad con âœ… y la columna Estado con `done <date> (<featureId>, orquestador, liberado a prod)`, preservando intacta la DescripciÃ³n; IDs no encontrados van a `missing` sin crashear. Integrado en `index.ts` justo despuÃ©s del log `RELEASED (push hecho)`, envuelto en try/catch (no bloquea el release â€” el gate de push ya pasÃ³) y logueando `missing` como warning. `resolves` ausente/vacÃ­o â†’ no-op silencioso (la mayorÃ­a de features S-XXX del propio sistema no mapean a filas de backlog por este mecanismo). No se tocÃ³ `markBacklogState`/`autopilot.ts` (mecanismo distinto: mapea 1:1 featureIdâ†”backlogId solo para picks del autopilot nocturno, formato de Estado distinto) ni `sync.ts` (ya lee `BACKLOG.md` del disco en cada tick para espejar a `orch_backlog`, asÃ­ que el fix de acÃ¡ alcanza sin tocar nada ahÃ­). 13 tests nuevos (9 en `backlog.test.ts` con filas reales de este archivo como fixture + 4 de `parseResolvesField` en `planner.test.ts`), typecheck y 409/409 tests de `orchestrator/` en verde (suite completa, incluye los 13 nuevos) | done 2026-07-27 (orquestador) | cc |
| S-048 | 2 | **El verifier confunde "me quede sin cuota" con "el codigo esta roto" y quema los intentos del fixer** â€” visto en vivo 2026-08-31 corriendo F-0042: Claude Code devolvio HTTP 429 (`"result":"You've hit your session limit Â· resets 4:40pm"`) con todos los contadores de tokens en 0. El builder no escribio nada, el verifier vio los tests fallar y lo trato como fallo de implementacion: reintento, escalo a Opus, y el fixer gasto sus 2 intentos contra un problema inexistente antes de bloquear el step. Costo real: 2 llamadas a Opus desperdiciadas + un `STATE.json` que hay que editar a mano para desbloquear. `ExecutorResult` YA tiene un campo `usageLimit` â€” hay que detectar el 429/session-limit y pausar el loop (o reintentar despues del reset que el propio mensaje informa) en vez de entrar al ciclo de correccion. Detectado en la sesion de Cowork del 2026-08-31 | pending |  |
| S-049 | 3 | **El Scout (DeepSeek) devuelve JSON truncado y las 3 investigaciones fallan juntas** â€” visto en F-0039 (2026-08-28): las tres investigaciones (`mapa`, `detective`, `riesgos`) fallaron en la misma corrida con `No se encontro JSON en la respuesta final` y `Unterminated string in JSON at position 1510`. La respuesta se corta a mitad de un string, lo que sugiere limite de tokens de salida y no un problema de formato. El loop degrada bien (`continuando sin research`) y F-0039 salio igual, asi que no bloquea â€” pero el research se pierde entero cada vez y nadie se entera salvo leyendo el log. Revisar el `max_tokens` de las llamadas a DeepSeek y/o agregar reparacion/reintento del JSON cortado | pending |  |
| S-050 | 2 | **BACKLOG.md se actualizaba en disco pero nunca se pusheaba a GitHub** — descubierto por Augusto (2026-09-14) al pedirle a Claude el estado de pendientes y recibir una respuesta desactualizada. Causa raíz: `updateBacklogStatus()` (S-047) solo escribe `system/BACKLOG.md` en disco; el push del release (`pushMain()` en `git.ts`) empuja el repo del TARGET (kredy/argos/tres-saltenas), un repo distinto al de augusto-os donde vive el backlog — nadie comiteaba/pusheaba augusto-os. Confirmado comparando el archivo local contra `raw.githubusercontent.com/augustomartinez87/augusto-os/master/system/BACKLOG.md`: el remoto mostraba SP-016/SP-017/AR-003/AR-006/AR-008/AR-010/SP-012/SP-013 como pending/armado estando done hace días en local. Fix: `commitAndPushBacklog()` nuevo en `backlog.ts`, invocado en `index.ts` justo después de `updateBacklogStatus()` — comitea y pushea únicamente `system/BACKLOG.md` (nunca `-A`), respeta el mismo chequeo de default-branch que `pushMain()`. Para reconciliaciones manuales (fuera del loop, la mayoría de los "reconciliado — Cowork" de este archivo) se agregó `pushBacklogFile()` + `npm run backlog:push -- "mensaje"` (`backlog-cli.ts` nuevo). 6 tests nuevos en `backlog.test.ts` (commit+push contra un repo git real en tmpdir, idempotencia, rechazo fuera de la rama default) — 15/15 verdes, typecheck limpio contra `git.ts` real, verificado en un sandbox aislado (Cowork) por caída de la consola en la PC (falla de Windows del 8/9 en curso) | done (código, sin verificar en el repo real) 2026-09-14 (Cowork) — **pendiente de Augusto:** revisar el diff y correr `npm test` + `git add`/`commit`/`push` en augusto-os una vez a mano (sin consola disponible del lado de Cowork en este momento); a partir de ahí el mecanismo queda autosuficiente | cc |

## Kredy [kredy]  (prÃ©stamos/crÃ©dito â€” ex "Spensiv", renombrado 2026-06-20)

| ID | P | DescripciÃ³n | Estado | Ejecutor |
|----|---|-------------|--------|----------|
| SP-001 | âœ… | Sprint S-D: BÃºsqueda unificada por CUIL/DNI en consola â€” resuelto por el buscador de Personas (`p.cuit.includes(search)`; el DNI queda embebido en las posiciones 2-9 del CUIL, asÃ­ que un DNI suelto matchea por substring) sobre el identity backbone de SP-003 | done 2026-06-11 (commit `32f2bc7`, reconciliado 2026-07-08 â€” Cowork) | manual |
| SP-002 | âœ… | Sprint S-E: LÃ­mite de originaciÃ³n por CUIL en frontend â€” `lib/risk/debtorLimit.ts` + banners de identidad/exposiciÃ³n en `components/loans/pre-approved-loan-card.tsx` | done 2026-06-11 (commit `32f2bc7`, reconciliado 2026-07-08 â€” Cowork) | manual |
| SP-003 | âœ… | MigraciÃ³n SA identity backbone â€” `lib/identity/resolvePerson.ts` (CUILâ†’DNIâ†’nombreâ†’crear) + campos `dni`/`identityStatus` en `Person` | done 2026-06-10/11 (commits `6ee650a`, `32f2bc7`, reconciliado 2026-07-08 â€” Cowork) | manual |
| SP-004 | âœ… | MigraciÃ³n AP Commission V2 â€” `docs/migration-ap-commission-v2.sql` aplicada en prod (`ap_withdrawals`/`ap_ledger_events` con datos reales), `server/services/commission.service.ts` (`realizeCommissionsForPayment`), consumida hoy por el cron `/api/cron/ap-reconcile` activo en prod | done 2026-06-16 (commits `5988972`/`9c40c03`; hecho fuera del loop F-XXXX, sin spec formal; reconciliado 2026-07-08 â€” Cowork) | manual |
| SP-005 | âœ… | RefinanciaciÃ³n Fase 2: UI de propuesta de refinanciaciÃ³n | done (reconciliado 2026-09-04, Cowork) â€” ya estaba hecho, construido fuera del backlog en el "sprint refinanciaciÃ³n" (commits `fd7c7ca`/`f2b5781`/`5272c10`, 2026-06-13, manual/Claude Code, nunca reconciliado). `RefinanceDialog` (`components/loans/refinance-dialog.tsx`, 458 lÃ­neas): wizard de 3 pasos (params+preview â†’ confirmar directo â†’ propuesta enviada con mensaje copy-paste para WhatsApp), `RefinancingProposalCard` en la tabla de prÃ©stamos | manual |
| SP-006 | âœ… | RefinanciaciÃ³n Fase 3: router refinanceLoan + Prisma RefinancingRequest | done (reconciliado 2026-09-04, Cowork) â€” mismo sprint que SP-005 (`fd7c7ca` etc., 2026-06-13). Implementado con un diseÃ±o distinto al original pero funcionalmente equivalente: en vez de un modelo `RefinancingRequest` separado, el router (`server/routers/loans/operations.ts`) expone `previewRefinancingOptions`/`proposeRefinancing`/`confirmRefinancing`/`discardRefinancingProposal`/`getPendingRefinancingProposal`, y el historial/auditorÃ­a queda en el propio `Loan` (`refinancedByLoanId`, `originalLoanId`, `refinancedAt`, campo `preApprovedExpiresAt` para la propuesta congelada). XIRR encadenado (`lib/xirr-builder.ts`) y tests dedicados (`tests/refinancing-proposal.test.ts`, `tests/xirr-chain.test.ts`) | manual |
| SP-007 | âŒ | RefinanciaciÃ³n Fase 4: webhook Stripe / flujo de pago | descartado 2026-09-04 â€” decisiÃ³n de Augusto: el flujo manual por WhatsApp (ya resuelto en SP-005/SP-006) alcanza, no hace falta un webhook de Stripe para cobros automÃ¡ticos. No reabrir sin pedido explÃ­cito | manual |
| SP-008 | âœ… | AP Score como gate en pre-aprobaciÃ³n | done 2026-06-20 (F-0002, mergeado a main) | cc |
| SP-009 | âœ… | **Gate de lÃ­mite por vÃ­nculo** (conocido 500k / referido 200k+aval / desconocido 0) en preApprove. F-0005 | done 2026-06-25 (deployado) | cc |
| SP-010 | âœ… | **Mutuo + pagarÃ© generados** â€” pagarÃ© pre-llenado por el total (sin protesto), clÃ¡usula TERCERA fija 8% mensual, SEXTA con intimaciÃ³n previa 5 dÃ­as, selector CABA/Mar del Plata, gate de datos del Person. F-0006, revisiÃ³n legal cerrada 2026-06-26 | done 2026-06-26 | cc |
| SP-011 | âœ… | **Documentos pendientes del prÃ©stamo** â€” fotos de mutuo firmado + pagarÃ© vÃ­a LoanAttachment, badge "pendiente" NO bloqueante. F-0007 | done 2026-06-25 (deployado) | cc |
| SP-012 | âœ… | UI para que el AP elija/edite el tier de vÃ­nculo y el referente (follow-up de SP-009) | done 2026-09-04 (F-0045, orquestador, liberado a prod) | manual |
| SP-013 | âœ… | Eliminar prÃ©stamos de testing completamente â€” un prÃ©stamo creado desde link de AP y aprobado sigue apareciendo en el historial sin poder borrarse | done 2026-09-04 (F-0044, orquestador, liberado a prod) | cc |
| SP-014 | âœ… | **Recordatorios masivos de cobranza** â€” botÃ³n "Recordar cobro" en `/dashboard/loans` (cartera de Augusto) y `/ap?token=...` (portal del AP): panel con la cuota mÃ¡s prÃ³xima/vencida por deudor, link `wa.me` por fila (monto, vencimiento, alias de cobro â€” sin TNA nunca) + "Copiar todos" (no se abren N pestaÃ±as, bloqueado por el navegador â€” ADR-0083). Dos endpoints en vez de uno scoped por `userId` porque Kredy es single-tenant y el AP opera sin sesiÃ³n Clerk, vÃ­a token (ADR-0084). `normalizePhone` (E.164, 6 tests), `persons.setPhone` para cargar telÃ©fonos faltantes. Commit `40e6a1e`, deployado (Vercel `dpl_2MtpFTvwjGUeDqYzzguUJYbwXnQx`, READY). MigraciÃ³n (`Person.phone`+`AgentConfig.collectionAlias`) aplicada a prod con OK explÃ­cito de Augusto 2026-07-15 â€” destapÃ³ S-043 en el camino (14 migraciones fantasma + `prisma/migrations` gitignoreado, ver esa entrada). Backfill corrido: 19 Persons sin phone, 0 con Contact vinculado con phone â†’ 0 backfilled (esperado, no error) â€” quedan 19 deudores sin telÃ©fono cargado, hay que completarlos a mano desde el dashboard (`persons.setPhone`) a medida que se necesite mandar el recordatorio | done 2026-07-15 (Claude Code + Cowork, migraciÃ³n prod verificada paso a paso) | cc |
| SP-015 | âœ… | **Nav mobile incompleta** â€” el bottom nav (`app/dashboard/layout.tsx`) solo exponÃ­a 4 de los 10 destinos del sidebar. Fix: 5to Ã­tem "MÃ¡s" con `Sheet` (bottom, ya disponible en `components/ui/sheet.tsx`, sin dependencias nuevas) que lista los 6 antes inalcanzables (Riesgo, Consulta 360Â°, Reglas de Tasas, Agente (AP), PolÃ­ticas de Riesgo, Usuarios). La lista del sheet se deriva de `navigation` filtrando lo ya presente en `mobileNavBase`/`mobileNavAdmin` â€” no duplica la fuente de verdad del sidebar de desktop, asÃ­ que un Ã­tem nuevo en el sidebar aparece solo en el sheet sin tocar este archivo de nuevo. "MÃ¡s" se marca activo cuando el pathname actual es uno de los 6 ocultos. No tocÃ³ desktop, `mobileNavBase`/`mobileNavAdmin` como conjunto, Perfil, ni `/ap`. Commit `e216c23`, deploy `dpl_A5YEeLem4kmatdwp1h3s1onRAUSS` verificado READY en producciÃ³n (Vercel API). `tsc`/lint limpios; verificaciÃ³n de DOM real no fue posible por requerir sesiÃ³n Clerk (documentado por Claude Code, no bloqueante) | done 2026-07-16 (Claude Code, deploy verificado por Cowork) | cc |
| SP-016 | âœ… | Plazo personalizado en el simulador del AP (`/ap`) â€” hoy solo se puede elegir entre `config.terms` (preset fijo); el backend (`ap.ts`, `termMonths: z.number().int().min(1).max(360)`) ya soporta cualquier plazo, falta el input libre en el frontend | done 2026-09-03 (F-0040, orquestador, liberado a prod) | cc |
| SP-017 | âœ… | ConfirmaciÃ³n + redirect tras crear/pre-aprobar prÃ©stamo â€” `createLoanMutation`/`preApproveMutation` en `app/dashboard/simulator/page.tsx` solo cierran el modal (sin toast ni redirect a `/dashboard/loans`) | done 2026-09-03 (F-0041, orquestador, liberado a prod) | cc |
| SP-019 | âœ… | **ComisiÃ³n del AP invisible en "Mi cartera" hasta la primera cuota cobrada** â€” pedido de Augusto (2026-07-28): el portal AP solo muestra `commissionPerInstallment` durante la simulaciÃ³n/preaprobaciÃ³n y el agregado en "Mi ComisiÃ³n"; una vez el prÃ©stamo estÃ¡ activo, la tab "Mi cartera" no muestra nada de comisiÃ³n por prÃ©stamo hasta que `commissionRealized > 0` (primera cuota cobrada). El dato total ya existe en la API (`apMyPortfolio` ya devuelve `commissionExpected`) â€” es puramente un fix de UI, sin backend/schema. Spec listo: `orchestrator/features/F-0037.md` | done 2026-07-28 (F-0037, orquestador, liberado a prod) | cc |
| SP-020 | âœ… | **ComisiÃ³n total/por cuota (F-0037) no aparece en prÃ©stamos con AP asignado retroactivamente** â€” hallado por Augusto en vivo (2026-07-28) al revisar F-0037: de 3 prÃ©stamos en "Mi cartera", solo 1 mostraba el bloque nuevo. Causa: el flujo de asignaciÃ³n retroactiva de AP (`server/routers/ap.ts` ~lÃ­nea 1995-2046) crea `ApCommission.expectedCommission = null` a propÃ³sito (no hubo preaprobaciÃ³n), aunque sÃ­ setea `loan.apCommissionRatio`. Spec listo: `orchestrator/features/F-0038.md` (fallback calculado al vuelo, sin persistir nada) | done 2026-07-28 (F-0038, orquestador, liberado a prod) | cc |
| SP-018 | âœ… | **LÃ­mite de originaciÃ³n: unificar exposiciÃ³n cross-AP por CUIL** â€” `getGlobalDebtorExposureByCuil(db, cuil, excludeUserId?)` nueva en `lib/risk/debtorExposure.ts`, suma cross-AP por CUIL normalizado; `checkDebtorLimit` la usa como numerador, denominador (% del portfolio) sigue por-AP. `getDebtorExposure`/`reconcileExposure` intactas. 8 tests nuevos (`tests/debtor-exposure-cross-ap.test.ts`), 194/194 verdes, tsc limpio. ADR-0080 | done 2026-07-14 (commit `478e836`, Claude Code, sin deploy manual â€” solo push a main) | cc |

## Spensiv (tracker finanzas personales) [spensiv]

| ID | P | DescripciÃ³n | Estado | Ejecutor |
|----|---|-------------|--------|----------|
| SPT-001 | âœ… | Seed data de prueba en el Neon dev `spensiv-dev` (hoy schema vacÃ­o, sin datos) | done 2026-07-03 (F-0010, ver S-000c) | cc |

## Argos [argos]

| ID | P | DescripciÃ³n | Estado | Ejecutor |
|----|---|-------------|--------|----------|
| AR-001 | âœ… | Path (`portfolio-tracker`) + stack + cmds de Argos en targets.json | done 2026-06-20 | cc |
| AR-002 | âœ… | DB dev de Argos: NO aplica â€” `dbModel: none` (Supabase-client, build/test no tocan DB). Guard exime targets no-Prisma | done 2026-06-20 | cc |
| AR-003 | âœ… | Polish: prompt del executor condicional por `dbModel` (sacar frases Prisma-cÃ©ntricas para targets Supabase) | done 2026-08-31 (F-0042, orquestador, liberado a prod) | cc |
| AR-004 | ❌ | Si en algún momento hace falta QA visual con datos: crear Supabase branch dev de Argos | descartado 2026-09-14 — decisión de Augusto: no hace falta, no reabrir sin pedido explícito | cc |
| AR-005 | 2 | **Costo de arancel en TNA (informativo, solo lectura)** â€” calcular el arancel efectivo de las cauciones, anualizarlo a TNA (vÃ­a `annualizeNominalTNA`) y mostrarlo en una vista para comparar contra la TNA de arancel pactada (~1,5) y detectar si la ALyC cobra de mÃ¡s. ESTRICTAMENTE solo lectura: NO modificar `calcularSpreadPorCaucion`, ni la anualizaciÃ³n canÃ³nica, ni el P&L, ni el spread â€” solo un cÃ¡lculo nuevo + vista informativa. DiagnÃ³stico primero: dÃ³nde viven los cargos de las cauciones y quÃ© campos traen. Archivos probables: `features/financing/`. (= ARCH-002 de PENDIENTES.md de Argos) | done 2026-06-29 (autopilot) | auto |
| AR-006 | âœ… | Guardado de distribuciÃ³n (%) de estrategias â€” hoy vive en localStorage, debe persistir por usuario en DB para verse igual en otro dispositivo | done 2026-09-04 (F-0043, orquestador, liberado a prod) | cc |
| AR-007 | ✅ | Notificaciones de FCI: deben salir de un parser automÃ¡tico, nunca de la carga manual â€” decisiÃ³n de Augusto (2026-09-07): "si ve algÃºn movimiento de fci en ese parseo, me genere una notificaciÃ³n... solo me notifican cuando yo hago el movimiento manual en argos, nada que ver, no deberÃ­a ser asÃ­". Investigado en profundidad (Cowork, 2026-09-07): el comprobante diario que ya lee caucion-sync (`Comprobantes Alycbur SA`) es 100% cauciones, verificado leyendo un PDF real â€” no tiene FCI. El dato SÃ estÃ¡ en otro mail semanal de Alycbur, `Informe Semanal de Operaciones` (adjunto `Resumen-Completo_*.pdf`), al mismo buzÃ³n que ya usa caucion-sync (`martinezstreet21@gmail.com`) â€” confirmado leyendo un PDF real, tiene el detalle completo de suscripciones/rescates con fecha, comprobante, cuotapartes y VCP. El cÃ³digo de fondo del PDF (`CAFCI<n>-<cafci_id>`) mapea directo a `fci_master.cafci_id`, verificado contra la tabla real. Especificado como dos features: **F-0053** (target `caucion-sync`, nuevo â€” se registrÃ³ como target del orchestrator; renombrada de F-0046 a F-0053 el 2026-09-07 porque ese ID quedÃ³ tomado por otro spec tuyo sin commitear, del Explorador de FCI/AR-023) parsea el informe semanal e inserta suscripciones completas en `fci_lots` y rescates "en crudo" (sin `mutations`) en `fci_rescates`, con `external_ref` para dedupe (migraciÃ³n `035_fci_external_ref_dedupe` ya aplicada en prod); las suscripciones notifican solas vÃ­a el RPC `sync_fci_notifications` ya existente, sin tocar nada de notificaciones. **F-0054** (target `argos`) agrega un paso manual (click explÃ­cito de Augusto, no automÃ¡tico â€” asÃ­ lo pidiÃ³ 2026-09-07) para aplicar el consumo PPC/FIFO de lotes de esos rescates, reusando `applyRedemptionPPC`/`applyRedemptionFIFO` ya probados en vez de reimplementar esa matemÃ¡tica en Python. Nota: esto NO es AR-011 (conciliaciÃ³n contra el portfolio de la ALyC, descartado) â€” es completar el cÃ¡lculo de lotes de un rescate que el propio sistema ya detectÃ³, sin comparar contra nada externo. Ambas specs armadas y listas para correr â€” recomendado correr F-0053 primero (F-0054 depende de que existan filas con `external_ref` para probar la UI) | done 2026-09-10 (F-0056, orquestador, liberado a prod) | cc |
| AR-008 | âœ… | **Fix "usuarios activos" en Admin** â€” `userService.getSystemStats()` lee `user_profiles.last_sign_in_at`, pero ningÃºn cÃ³digo de la app lo escribe (`grep` sin resultados fuera de las 2 lecturas); si no hay trigger de Supabase Auth sincronizÃ¡ndolo desde `auth.users`, el conteo de activos estÃ¡ estructuralmente roto. Reconciliado 2026-07-08 | ya estaba resuelto â€” no hacÃ­a falta feature nueva: `supabase/migrations/011_sync_user_metadata.sql` ya crea el trigger `on_auth_user_updated` (AFTER UPDATE OF email, last_sign_in_at ON auth.users) que sincroniza `last_sign_in_at` a `user_profiles`. Verificado en vivo contra prod (Cowork, 2026-09-04): trigger presente y `tgenabled=O`, 5/5 usuarios de `user_profiles` con `last_sign_in_at` cargado (mÃ¡s reciente 2026-07-30). El backlog estaba desactualizado, no el cÃ³digo â€” done (reconciliado) 2026-09-04 | cc |
| AR-009 | âœ… | Sparkline no condice con el % mostrado â€” `MobilePositionsList.jsx:271` muestra el sparkline (Ãºltimos ~8 cierres diarios de `useSparklines`) junto a `resultPct` (P&L acumulado desde compra); deberÃ­a ir junto a `dailyPct` (ya calculado en el mismo archivo, lÃ­nea 49) para que ambos hablen de la misma ventana temporal. Reconciliado 2026-07-08 | done 2026-08-28 (F-0039, orquestador, liberado a prod) | cc |
| AR-010 | âœ… | Cauciones: separar arancel como campo propio en el parser/ingestor â€” `csvSpreadIngestor.ts` solo trae `interes` agregado, sin desglose de arancel; AR-005 ya calcula el arancel en TNA pero de forma informativa aparte, no a nivel de ingesta. Motivo de Augusto: dejÃ³ de operar en la ALyC y le empezaron a cobrar aranceles â€” quiere visibilidad de cuÃ¡nto le cobran | ya estaba resuelto, por una vÃ­a que este backlog no trackeaba â€” reconciliado 2026-09-04 (Cowork), a partir de contexto que aportÃ³ Augusto en la conversaciÃ³n. `csvSpreadIngestor.ts` quedÃ³ muerto (cero imports en `src/`, verificado por grep) â€” la ingesta real de cauciones hoy corre en el repo separado `caucion-sync` (github.com/augustomartinez87/caucion-sync, cron diario en GitHub Actions, lee Gmail directo, sin subir PDFs a Argos), que desde el commit `797c07f` (ARCH-002, capturar arancel + costos itemizados) parsea `arancel`, `arancel_iva`, `derecho_mercado`, `derecho_mercado_iva`, `costo_garantia` y `costo_garantia_iva` como campos propios (disponibles en el PDF de Alycbur desde feb-2026) y los upsertea a la tabla `cauciones` de Supabase. En Argos, `financingService.ts` ya lee esos campos (select * en la lÃ­nea 239) y `arancelAudit.js` + `CaucionesTable.jsx` ya muestran una columna Arancel TNA, sorteable, con tooltip +IVA y franja resumen siempre visible, desde el commit `5b0d17d` (2026-06-16, merge arancel audit into CaucionesTable, ARCH-002 UX) â€” antes incluso de que este Ã­tem existiera en el backlog. Nota para Augusto: `caucion-sync` es infraestructura real en producciÃ³n, toca la misma DB de Argos, y este backlog nunca la trackeÃ³ â€” valdrÃ­a la pena sumarla como Ã¡rea propia si vas a seguir tocÃ¡ndola | cc |
| AR-011 | âŒ | Lugares: conciliaciÃ³n contra el portfolio real reportado por la ALyC â€” ya existe el concepto "lugar" (FCI), falta comparar la posiciÃ³n que Argos calcula para un lugar contra el portfolio real de esa cuenta en la web de la ALyC, con recordatorio periÃ³dico (mensual/quincenal) para hacerlo | descartado 2026-08-26 â€” decision de Augusto: la conciliacion contra el portfolio de la ALyC queda fuera de alcance por completo, no es "pendiente de hacer". No reabrir sin pedido explicito. | cc |
| AR-012 | âœ… | RevisiÃ³n UX/UI de mobile por Claude (Sonnet) â€” pedido explÃ­cito de Augusto, sin cÃ³digo a cambiar de entrada, es una auditorÃ­a. Salida: `portfolio-tracker/SPEC-mobile-overview-redesign.md` (gap analysis vs mockups de referencia) + specs ejecutables F-0019..F-0024 (= AR-013..AR-018) | done 2026-07-11 (Cowork) | manual |
| AR-013 | 2 | **Mobile Overview: hero chart limpio** â€” chart sin ejes/grilla full-bleed 200px, jerarquÃ­a centrada (label â†’ valor 34px â†’ P&L â†’ En vivo), period selector minimal en una fila. Spec listo: `features/F-0019.md` (M1+M2+M3 del SPEC). Correr primero: F-0020 depende de este | done 2026-07-11 (F-0019 released) | cc |
| AR-014 | 2 | **Mobile: ojo de censura + settings en MobileHeader** â€” censura toggleable desde cualquier pÃ¡gina; settings gated por permisos â†’ /sistema/administracion; sin campana (no hay backend de notificaciones â€” eso es AR-007). Spec listo: `features/F-0020.md` (M5). Depende de AR-013/F-0019 | done 2026-07-11 (F-0020 released) | cc |
| AR-015 | 2 | **AllocationPanel: donut con contador central + leyenda coloreada** â€” anillo fino con "N pos." al centro, % de leyenda en el color de cada slice; toggle tipo/estrategia se conserva. Spec listo: `features/F-0021.md` (M4). Independiente | done 2026-07-11 (F-0021 released) | cc |
| AR-016 | 2 | **Mobile Overview: Posiciones Destacadas** â€” top-5 plano por valuaciÃ³n con sparklines de Ã¡rea 72Ã—28 y "Ver todas â€º"; la lista agrupada queda solo en /portfolio/posiciones. Spec listo: `features/F-0022.md` (M6). Toca `MiniSpark` en `MobilePositionsList.jsx` â€” mismo archivo que AR-009: si AR-009 se hace, coordinar orden para evitar conflicto | done 2026-07-12 (F-0022 released) | cc |
| AR-017 | 3 | **Mobile: bottom nav con pill activo** â€” indicador teal-dim en Ã­tem activo (incl. "MÃ¡s" con sheet abierto), sin saltos de layout. Spec listo: `features/F-0023.md` (M7). Independiente, cualquier momento | done 2026-07-12 (F-0023 released) | cc |
| AR-018 | 3 | **Mobile Overview: reorden final** â€” hero â†’ asignaciÃ³n â†’ destacadas â†’ KPI carousel al final; normalizar spacing. Spec listo: `features/F-0024.md` (M8). Correr Ãºltimo: depende de AR-015 y AR-016 | done 2026-07-12 (F-0024 released) | cc |
| AR-019 | âœ… | **Mobile Overview iteraciÃ³n 2: paridad con mockup** â€” sacar PageHeader "Overview" solo en mobile, selector de portfolio con chevron + row 2 compacta, gradiente del hero visible (hoy casi negro), recortar gap valorâ†’chart. Feedback de Augusto con screenshots 2026-07-12. DecisiÃ³n: period chips quedan bajo el chart (no dropdown en header) | done 2026-07-12 (F-0025, 4/4 steps, merged+pushed a main â€” reconciliado 2026-07-14, Cowork) | cc |
| AR-020 | âœ… | **Bottom nav: pill prominente estilo mockup** â€” el pill de F-0023 quedÃ³ tÃ­mido (teal-dim 12% casi invisible); subir presencia, padding, strokeWidth del icono activo | done 2026-07-12 (F-0026, 3/3 steps, merged+pushed a main â€” reconciliado 2026-07-14, Cowork) | cc |
| AR-021 | âœ… | **Sparklines faltantes para bonos** â€” causa real: `data912` no tiene histÃ³rico para T15E7/TTD26 (letras/bonos CER cortos, responde 200 con `{"Error":...}`, no array). Fix: `useSparklines.js` cae a `getEodSeries` (mismo fallback ya usado por `PositionDetailModal`/`portfolioHistoryService`, sin cliente nuevo) cuando data912 no devuelve array; `MobilePositionsList.jsx` reserva el espacio del sparkline (`div w-[72px] h-[28px]`) cuando no hay data, en los 2 puntos (featured + lista agrupada). El loop automÃ¡tico (`npm start F-0027`) se trabÃ³ en step 1 por un lÃ­mite de scoping del plan (diagnÃ³stico-only vs. implementaciÃ³n), no por el cÃ³digo â€” el fixer (Opus, vÃ­a S-039) ya habÃ­a encontrado la causa y el fix correctos en su primer intento, rechazados por el reviewer solo por tocar el fetch antes de tiempo. Cerrado manual fuera del loop sobre la misma rama, sin descartar el trabajo del fixer. Verificado en Supabase (lectura): T15E7 resuelve 352 filas, TTD26 354 vÃ­a `daily_prices`. `tsc` limpio, 99/99 tests. Commit `ad2b285`, mergeado a main y pusheado, deploy `dpl_7L7fhuvn9DcZXVHnGYExAnxwe9g8` verificado READY (Vercel API) | done 2026-07-16 (cierre manual Claude Code + Cowork, loop quedÃ³ en `blocked` sin tocar) | cc |
| AR-022 | 2 | **ReclasificaciÃ³n carry/portfolio de `fci_lots` no es atÃ³mica + posible desincronizaciÃ³n UIâ†”DB** â€” `fciService.js` hacÃ­a `UPDATE fci_lots.tipo` y el `INSERT` de auditorÃ­a (`fci_lot_context_changes`) en dos llamadas separadas sin transacciÃ³n. **Fix causa #1:** RPC `reclassify_fci_lot` (`supabase/migrations/031_reclassify_fci_lot_rpc.sql`, `FOR UPDATE` + UPDATE+INSERT atÃ³micos), `fciService.moveLotContext` migrado a llamarla, 4 tests de regresiÃ³n en `fciService.moveLotContext.test.js` (camino feliz, fallo revierte, no-op cuando ya estÃ¡ en destino) â€” **migraciÃ³n aplicada a `wwzocpcolgdzkvcigchj` (Cowork, 2026-07-13, OK explÃ­cito de Augusto), funciÃ³n `reclassify_fci_lot` verificada en `pg_proc`**. Mergeado a `main` y pusheado (`61564f3`, 2026-07-13) â€” falta el deploy para que el cÃ³digo en prod use la RPC. **Causa #2 (investigada, resultado distinto al diagnÃ³stico original):** a la fecha de este fix (2026-07-13, mismo dÃ­a) los 4 lotes de Alpha Renta Capital Pesos - Clase B en Alycbur tienen `tipo='carry'` en la base â€” NO `'portfolio'` como decÃ­a el diagnÃ³stico previo â€” y coincide con el Ãºltimo registro de auditorÃ­a (`fci_lot_context_changes`, 2026-06-06 02:10 UTC) y con lo que muestra la UI; no hay desync activo hoy, asÃ­ que no se tocÃ³ ningÃºn dato. `updated_at` de 3 de los 4 lotes no cambia desde 2026-06-06 (confirma que nadie escribiÃ³ `tipo` desde entonces vÃ­a trigger `update_fci_lots_updated_at`), por lo que el diagnÃ³stico original probablemente leyÃ³ mal la consulta, no un cambio de estado posterior. SÃ­ se encontrÃ³ evidencia directa de la causa #1 en el propio historial: para estos mismos 4 lotes, el tramo 2026-06-05 22:29â†’2026-06-06 02:10 muestra un `tipo_anterior` en el audit que no coincide con el Ãºltimo `tipo_nuevo` logueado â€” un UPDATE aplicado sin su INSERT de auditorÃ­a correspondiente, el patrÃ³n exacto que la RPC corrige. React Query/cache descartado como causa (esta data no pasa por react-query, se carga con `useState`+`useEffect` directo en `useFciLotEngine`, sin persistencia en localStorage); RLS descartado (policy simple `auth.uid()=user_id`, no filtra por valor). **Confirmado por Augusto en UI (2026-07-20):** ve 'carry' para los 4 lotes, coincide con DB/auditorÃ­a â€” es la clasificaciÃ³n correcta, sin acciÃ³n adicional. Rama `fix/AR-022-reclasificacion-carry-portfolio-atomica` mergeada a `main` y pusheada a `portfolio-tracker` (sin deploy manual â€” depende del pipeline de Vercel). Prompt: `system/prompts/AR-022-fix-reclasificacion-carry-portfolio.md` | done 2026-07-13 â€” migraciÃ³n aplicada + cÃ³digo mergeado/pusheado a main (`61564f3`); deploy a Vercel confirmado en vivo (`dpl_D38c8aCnzMjBxACWTedP9pqNTYDS`, READY, producciÃ³n, verificado vÃ­a Vercel API por Cowork); tipo real de los 4 lotes en disputa sin corregir (ya estaban en 'carry', consistentes con DB/UI/auditorÃ­a, no hacÃ­a falta) | cc |
| AR-023 | âœ… | **Agrupamiento de clases FCI (1 fondo = N clases)** â€” vista `fci_explorador_grupos` (agrupa por nombre-base sin sufijo "- Clase X" + `sociedad_gerente`, clase default = mayor antigÃ¼edad de precios en `fci_prices`, override manual vÃ­a tabla nueva `fci_class_overrides`) + toggle "1 clase por fondo" (on por defecto) + fila expandible con clases hermanas, en `FciExplorador.jsx`/`mercadoService.js`. MigraciÃ³n nueva (`fci_explorador_grupos` + `fci_class_overrides`, additive-only) â€” gate humano, el loop no la aplica. Spec: `features/F-0046.md`. Foundational â€” AR-024 depende de esto | done 2026-09-07 (F-0046, orquestador, liberado a prod) | cc |
| AR-024 | âœ… | **Ranking curado (Fase 1)** â€” excluir del listado cierres stale (Ãºltima fecha en `fci_prices` distinta a la fecha hÃ¡bil mÃ¡s reciente del universo) + usar `fci_explorador_grupos` cuando el toggle de AR-023 estÃ¡ activo. **CorrecciÃ³n de rumbo vs. el draft original:** `rend_ytd`/`rend_1y` NO estÃ¡n rotos â€” verificado contra Supabase (`wwzocpcolgdzkvcigchj`, 2026-09-07): la funciÃ³n `refresh_fci_rendimientos()` corre bien (cron diario + `cafci_sync.py` post-sync), y sÃ³lo 4 de 3.974 fondos activos tienen algÃºn precio en `fci_prices` anterior al 1/1 de este aÃ±o o a hace 365 dÃ­as â€” el resto de la base reciÃ©n tiene historial reciente. Recalcular YTD/1Y desde `fci_prices` en el frontend (como proponÃ­a la Fase 2 original) pegarÃ­a contra la misma pared de datos â€” no hay fix de cÃ³digo posible sin backfill de histÃ³rico (item ya trackeado aparte: "VCP backfill script" en Argos overview). Se deja YTD/1Y como estÃ¡ (guion cuando no hay dato) y queda fuera de este feature. Spec: `features/F-0047.md`. Depende de AR-023 | done 2026-09-08 (F-0047, orquestador, liberado a prod) | cc |
| AR-025 | ✅ | **TNA (30D): tooltip explicativo** â€” el header de la columna ya dice "TNA (30D)" (verificado en `FciExplorador.jsx`, ya no dice solo "TNA"), falta agregar el tooltip explicando la ventana de 30 dÃ­as. El reemplazo de naming "ARS ClÃ¡sico/DinÃ¡mico" â†’ T+0/T+1/T+2 queda fuera de este item: depende de dato de liquidez que hoy no estÃ¡ confirmado en el bulk de CAFCI (ver AR-031). Spec: `features/F-0048.md`. Independiente | done 2026-09-10 (F-0048, orquestador, liberado a prod) | cc |
| AR-026 | ✅ | **Referencia de cauciÃ³n + KPI row del Explorador** â€” franja fija arriba del Explorador con: total de fondos activos visibles, TNA promedio del universo filtrado, mejor TNA del universo filtrado, y la Ãºltima tasa de cauciÃ³n operada por Augusto (tabla `cauciones`, ya usada por FinanciaciÃ³n). **PrecisiÃ³n de naming:** `cauciones` guarda las operaciones propias de Augusto (vÃ­a `caucion-sync`/PDFs Alycbur), no una tasa de mercado en vivo â€” el ribbon se etiqueta como "tu Ãºltima cauciÃ³n" o similar, no "cauciÃ³n en vivo", para no sugerir un feed de mercado que no existe. Spec: `features/F-0049.md`. Independiente | done 2026-09-10 (F-0049, orquestador, liberado a prod) | cc |
| AR-027 | ✅ | **Chips de categorÃ­a (clasificaciÃ³n 1-10) con contador en vivo** â€” ya no estÃ¡ bloqueado: `src/features/fci/constants.js` (`FCI_CLASIFICACION`) ya tiene el mapeo completo de los 10 cÃ³digos, derivado empÃ­ricamente y documentado como fuente de verdad (auditorÃ­a 2026-05-31) â€” el draft original asumÃ­a que 6-10 seguÃ­an sin confirmar, desactualizado. Hoy `FciExplorador.jsx` solo expone 5 de los 10 chips (`CATEGORIAS` hardcodeado). Extender a los 10 + contador por chip que respeta los demÃ¡s filtros activos. Spec: `features/F-0050.md`. Independiente | done 2026-09-10 (F-0050, orquestador, liberado a prod) | cc |
| AR-028 | ✅ | **Filtros del Explorador: gestora buscable + contador de resultados + ayuda inline** â€” combobox buscable para `sociedad_gerente` (63 valores, hoy `<select>` plano) en vez de select plano, contador de resultados visible + botÃ³n "limpiar filtros", texto de ayuda inline corto debajo del buscador. Spec: `features/F-0051.md`. Independiente | done 2026-09-11 (F-0051, orquestador, liberado a prod) | cc |
| AR-029 | ✅ | **Sparkline de tendencia + alineaciÃ³n tabular-nums** â€” columna nueva con mini-chart de los Ãºltimos ~30 `fci_prices.vcp` por fondo (reusa `src/components/ui/Sparkline.jsx`, ya usado en Portfolio â€” no crear un componente nuevo) + `font-variant-numeric: tabular-nums` en las columnas numÃ©ricas de la tabla. Spec: `features/F-0052.md`. Independiente | done 2026-09-11 (F-0052, orquestador, liberado a prod) | cc |
| AR-030 | ✅ | **AUM/patrimonio por clase â€” dato YA disponible, falta persistirlo** â€” correcciÃ³n de rumbo vs. el draft original: verificado en `scripts/cafci_sync.py`/`cafci_inspect.py` que `pb_get` SÃ trae patrimonio por clase (columna 14, "Patrimonio â€” Actual") en cada corrida â€” hoy se descarga y se usa transitoriamente solo para detectar fondos inactivos (`es_inactivo()`), pero nunca se persiste. No hace falta pegarle a `estadisticas.cafci.org.ar` (~4.000 pÃ¡ginas/dÃ­a) como se temÃ­a. Falta: migraciÃ³n additiva (columna `patrimonio` en `fci_master` o tabla aparte) + una lÃ­nea en `cafci_sync.py` que la incluya en el upsert, luego sÃ­ el filtro/columna de AUM en el Explorador. Toca el pipeline de sync (cron), que es un approval gate explÃ­cito (ver ways-of-working) â€” no specear como F-XXXX todavÃ­a, confirmar con Augusto antes de tocar `cafci_sync.py` | done 2026-09-12 (F-0057, orquestador, liberado a prod) | manual |
| AR-031 | ✅ | **Liquidez de rescate (T+0/T+1/T+2/T+3+) y honorarios/comisiones por clase** â€” verificado contra el mapeo completo de columnas de `pb_get` (40 columnas auditadas en `cafci_inspect.py`, dict `COLS`): no aparece plazo de liquidaciÃ³n ni honorarios/comisiones en ninguna. SeÃ±al razonable (no 100% concluyente, puede haber columnas mÃ¡s allÃ¡ de la 39 sin mapear) de que si estos datos existen, viven solo en la pÃ¡gina de detalle por clase de `estadisticas.cafci.org.ar` â€” confirmarlo ahÃ­ antes de specear (implicarÃ­a scraping, no un archivo bulk) | done 2026-09-13 (F-0058, orquestador, liberado a prod) | manual |
| AR-032 | ❌ | **Tenencias del Mercado** — composición agregada de mercado por categoría de activo (visto en fonditos.ar). No explorado si CAFCI publica un agregado equivalente en su sección de estadísticas ni su estructura. Sin diagnóstico, sin spec. Fase 3, después de AR-030/AR-031 | descartado 2026-09-14 — verificado (Cowork) que la ingesta diaria de CAFCI (`pb_get`, `cafci_sync.py`) NO trae composición de cartera por fondo ni un agregado de mercado: las 40 columnas mapeadas en `cafci_inspect.py` (mismo mapeo usado y confirmado por AR-030/AR-031) son metadata/clasificación/precio/patrimonio, sin ningún campo de tenencias. Conseguir esto requeriría una fuente distinta (posible página de estadísticas de CAFCI, nunca confirmada) fuera del pipeline que ya se mantiene — decisión de Augusto: no vale la pena, no reabrir sin pedido explícito | cc |

| AR-033 | ✅ | **Compact desktop: breakpoint `compact` (infraestructura)** — nuevo screen custom en `tailwind.config.js` basado en alto de viewport (`max-height: 820px`), más el flag `COMPACT_OVERVIEW_ENABLED` en `src/config/layoutFlags.js` para poder apagar todo el rediseño de un saque si no convence. Spec: `SPEC-compact-desktop-overview-redesign.md` (M1). Spec listo: `features/F-0059.md`. Sin cambios visibles por sí solo | done 2026-09-14 (F-0059, orquestador, liberado a prod) | cc |
| AR-034 | ✅ | **Compact desktop: hero chart con alto propio, ya no heredado del sibling** — causa raíz del "cuadro muy grande" reportado por Augusto: el hero se estira hoy para igualar el alto de la columna KPI+Allocation por el `align-items: stretch` default del grid. Pasa a alto fijo (~300px full desktop, ~220px compact). Depende de AR-033. Spec: `SPEC-compact-desktop-overview-redesign.md` (M2). Spec listo: `features/F-0060.md` | done 2026-09-14 (F-0060, orquestador, liberado a prod) | cc |
| AR-035 | ✅ | **Compact desktop: KPI cards + AllocationPanel más compactos** — menos padding, sparkline oculto en tier compact, para que la columna derecha también baje de alto. Depende de AR-033. Spec: `SPEC-compact-desktop-overview-redesign.md` (M3). Spec listo: `features/F-0061.md` | done 2026-09-14 (F-0061, orquestador, liberado a prod) | cc |
| AR-036 | ✅ | **Compact desktop: ocultar columnas de baja prioridad en la tabla de posiciones** — `P. Prom.` y `Día %` (redundante con `Día $`) se ocultan en tier compact, bajando el ancho mínimo de la tabla de ~995px a ~825px y eliminando el scroll horizontal reportado por Augusto. Depende de AR-033. Spec: `SPEC-compact-desktop-overview-redesign.md` (M4). Spec listo: `features/F-0062.md` | done 2026-09-14 (F-0062, orquestador, liberado a prod) | cc |
| AR-037 | ✅ | **Compact desktop: sidebar colapsado por default en tier compact** — recupera ~184px de ancho horizontal; el toggle manual del usuario sigue teniendo prioridad. Depende de AR-033. Spec: `SPEC-compact-desktop-overview-redesign.md` (M5). Spec listo: `features/F-0063.md` | done 2026-09-14 (F-0063, orquestador, liberado a prod) | cc |
| AR-038 | 3 | **Compact desktop: paddings y gaps generales del bloque desktop** — ajuste final de aire vertical/horizontal en tier compact, después de que AR-034/AR-036 ya liberaron espacio. Depende de AR-033 (idealmente corre último). Spec: `SPEC-compact-desktop-overview-redesign.md` (M6). Spec listo: `features/F-0064.md` | pending | cc |
| AR-039 | ✅ | **Cron `fci-sync` (repo `caucion-sync`): primera corrida falló con Postgres 42P10 en el upsert a `fci_lots`/`fci_rescates`** — reportado por Augusto vía mail de GitHub Actions (Run #1, disparado por el cron, commit `0365346`, 20/09 22:51 ART). Causa: el índice único parcial de la migración `035_fci_external_ref_dedupe` (`WHERE external_ref IS NOT NULL`) no es inferible por el `ON CONFLICT (external_ref)` sin `WHERE` que emite PostgREST (`fci_sync.py` manda `on_conflict=external_ref`) → Postgres devuelve 42P10. Fix (migración `047_fci_external_ref_unique_constraint`): reemplazados los 2 índices parciales por `UNIQUE (external_ref)` real en ambas tablas — mismo comportamiento con `external_ref` NULL de cargas manuales (NULL nunca es igual a NULL), ahora sí inferible por PostgREST. Sin cambios en `fci_sync.py`. Verificado con `workflow_dispatch`: Run #2 success (17s), `Suscripciones: 2 | Rescates: 1`, 2/2 mails procesados. Detalle: `PROGRESS.md` | done 2026-09-21 (Cowork) | manual |
| AR-040 | 2 | **Dashboard demora 20-30s en cargar tras login** — pedido explícito de Augusto (2026-09-20/21, Cowork), tras rechazar un fix cosmético (banner) y pedir en su lugar diagnóstico real de la demora. Diagnosticado a fondo contra Supabase (`wwzocpcolgdzkvcigchj`) y código real: causa principal, `useFciLotEngine.loadLots()` bajaba el historial COMPLETO de precios de cada fondo activo (`fciService.getPrices(id)` sin `fromDate`) solo para encontrar el precio del día anterior — hasta 2.844 filas/13 años para un solo fondo (Alpha Renta Capital Pesos - Clase B), confirmado por conteo real en Supabase. Causa secundaria, `FundingEngine.jsx`'s `loadVcpHistory` pedía el historial VCP de cada fondo carry en un loop por-fondo (N requests vía `Promise.allSettled`) en vez de usar `fciService.getPricesBatch` (ya existía sin uso). Fix de 3 cambios: (1) método nuevo `fciService.getRecentPrices(fciId, limit=30)` — mismo `fci_prices` pero acotado por cantidad de filas (`.order(fecha,desc).limit(30)`, revertido a ascendente) en vez de traer todo; da el mismo resultado exacto que el historial completo para "precio más reciente + el anterior" (ADR-0177). (2) `useFciLotEngine.loadLots()` usa `getRecentPrices` en vez de `getPrices` sin filtro. (3) `FundingEngine.loadVcpHistory` reemplaza el loop por-fondo por una sola llamada a `getPricesBatch` (ADR-0178 — cambia aislamiento de fallas de por-fondo a atómico). No se tocó `calcularSpreadPorCaucion` ni ningún cálculo financiero — la consulta original de Augusto sobre "Ganancia FCI" y Alpha B (variación negativa) se investigó y cerró aparte, confirmado correcto (el neteo de variaciones negativas funciona bien), sin necesidad de fix. Se evaluó y descartó por alcance (ADR-0179) unificar este fetch con el de `FundingEngine` en una sola fuente compartida — quedan como dos fetches separados, más chicos que antes pero no 100% deduplicados. Código escrito, verificado y pusheado directamente por pedido explícito de Augusto ("no vamos a revisar, pushea directamente"): resultó que sí había un canal de consola disponible del lado de Cowork (Windows-MCP PowerShell, distinto del `device_bash` bloqueado por el bug de Plan9/virtiofs del 8/9 — ver nota abajo), así que se corrió verificación real antes de pushear: `npx tsc --noEmit` limpio, `npx vitest run` 361/361 tests verdes (22 test files, ninguno específico de los 3 archivos tocados pero sin regresiones en el resto), `npm run build` OK (build de producción completo sin errores). Se detectó y preservó sin tocar un WIP no relacionado ya presente sin commitear en esa misma carpeta (`DashboardOverview.jsx`, cambio de paddings de AR-038/F-0064) — se hizo `git stash` de ese archivo puntual, se cambió a `main`, se commiteó y pusheó solo los 3 archivos de AR-040, y se volvió a la rama `feat/F-0064-...` con `git stash pop` para restaurar ese WIP intacto. Commit `9d2418c8` en `portfolio-tracker-argos`, pusheado a `origin/main` — dispara el auto-deploy de Vercel. **Nota para el sistema:** el `device_bash` (shell dentro de la VM Linux de Cowork) sigue roto por el bug de Windows del 8/9, pero `Windows-MCP__PowerShell` (ejecución directa en Windows, otro canal) sí funciona — vale la pena revisar si S-050 y otros ítems bloqueados por "consola no disponible" en realidad podían haberse resuelto por esta vía | done (verificado y pusheado a producción) 2026-09-21 (Cowork) — commit `9d2418c8`, deploy de Vercel disparado automáticamente, no verificado en vivo post-deploy (Augusto pidió pushear sin esperar revisión) | cc |

## 3 SalteÃ±as [tres-saltenas]

Microemprendimiento de empanadas de Dani (no Augusto). Augusto = backup/analista, Dani = mano de obra que carga datos desde el celular. Spec completa en `tres-saltenas/SPEC-MVP.md`. Target agregado a `targets.json` 2026-07-08 (Cowork) con placeholders â€” falta Neon real (ver TS-001).

| ID | P | DescripciÃ³n | Estado | Ejecutor |
|----|---|-------------|--------|----------|
| TS-001 | 1 | Bootstrap del repo (Next.js/tRPC/Prisma/Clerk) segÃºn `BOOTSTRAP-PROMPT.md`. `tsc` verde, `prisma db push` + seed corridos contra `tres-saltenas-dev` (Producto "Empanada JyQ" + receta + ConfigManoDeObra reales). Repo en GitHub `augustomartinez87/tres-saltenas` (privado), commit `126ec6e` con SPEC-MVP/BOOTSTRAP-PROMPT/NEXT-STEPS ya versionados | done 2026-07-08 (Cowork + Claude Code local) â€” Ãºnico pendiente no bloqueante: Clerk real (hoy `.env` tiene keys placeholder, no impide typecheck/build) | cc |
| TS-002 | 2 | `targets.json` + `orchestrator/.env` completos 2026-07-08: prod `tres-saltenas` (host `ep-tiny-voice-atetw10j`) en `prodDbPatterns`; dev `tres-saltenas-dev` (host `ep-frosty-wildflower-atkvtbza`) en `TRES_SALTENAS_DEV_DATABASE_URL` y en `tres-saltenas/.env` (DATABASE_URL pooled + DIRECT_URL direct) | done 2026-07-08 (Cowork) | cc |
| TS-003 | 3 | ~~Cerrar preguntas abiertas del spec con Dani~~ â€” resuelto por Augusto directamente 2026-07-08 sin necesidad de Dani (ver SPEC-MVP.md Â§8): patrÃ³n de ventas no se hardcodea, sueldo $170k confirmado ajeno al negocio, precios de compra se cargan en runtime | done 2026-07-08 (Cowork) | manual |
| TS-004 | 3 | Reportes mensuales comparativos + grÃ¡fico de evoluciÃ³n de costo/margen â€” pedido explÃ­cito de Dani | pending (esperar feedback real de Dani antes de construir esto â€” ver TS-012) | cc |
| TS-012 | âœ… | **Sin navegaciÃ³n entre pantallas â€” bloquea que Dani lo use en la prÃ¡ctica** â€” `layout.tsx` no tenÃ­a ningÃºn nav ni links entre secciones (verificado 2026-07-23, Cowork). Resuelto vÃ­a orquestador (`F-0029`, 4/4 steps, primer feature de tres-saltenas corrido 100% por `npm start` en vez de prompt manual â€” cambio de flujo pedido explÃ­citamente por Augusto). `src/components/NavBar.tsx` nuevo (tira de pills `overflow-x-auto`, `max-w-lg mx-auto`, sticky, pill activa vÃ­a `usePathname()`), 9 destinos (Dashboard/Ventas/Compras/ProducciÃ³n/Insumos/Gastos/Retiros/Mi Plata/Recetas). Montado en `layout.tsx` envuelto en `<Show when="signed-in">` de `@clerk/nextjs` (no `<SignedIn>` â€” el reviewer lo marcÃ³ como ADR-0085 "supuesto del agente" para revisiÃ³n; **verificado contra doc oficial de Clerk (`@clerk/nextjs` `^7.5.14` real en `package.json`): `<Show when="signed-in">` es API real y actual de v7, no alucinada â€” ADR confirmado, sin acciÃ³n**). IncluyÃ³ tambiÃ©n `/sign-up` (ver TS-013). Step 1 tuvo un CHANGES_REQUESTED real del reviewer (NavBar se montaba sin gating, visible en /sign-in) resuelto en el reintento â€” control funcionando como debe. Commit merge `e910d288`, push a main, deploy `dpl_2qxoHHbQqcEboWSQo9mbVZQkubhK` verificado READY en producciÃ³n (Vercel API) | done 2026-07-23 (orquestador `npm start F-0029`, verificado Cowork) | auto |
| TS-013 | 1 | **Auto-registro sin invitaciÃ³n** â€” solo existÃ­a `/sign-in`, ninguna forma de que alguien (Dani, o cualquiera con el link) se registre solo; habÃ­a que invitar a mano desde el dashboard de Clerk. Resuelto en el mismo `F-0029`: `src/app/sign-up/[[...sign-up]]/page.tsx` con `<SignUp/>` (mismo layout centrado que sign-in), cross-links agregados en ambas pÃ¡ginas (`Â¿No tenÃ©s cuenta? â†’ Registrate` / `Â¿Ya tenÃ©s cuenta? â†’ IniciÃ¡ sesiÃ³n`). **Pendiente fuera de cÃ³digo, acciÃ³n de Augusto:** confirmar que "Sign-ups" estÃ© habilitado en el dashboard de Clerk (clerk.com) â€” si estÃ¡ deshabilitado, `/sign-up` se ve pero Clerk rechaza el alta igual. Nota de seguridad: al ser self-serve completamente abierto (sin allowlist de dominio/email), cualquiera con el link de `/sign-up` puede crear una cuenta y ver los datos del negocio â€” aceptado como riesgo consciente por Augusto dado el tamaÃ±o (1-2 usuarios reales) | done (cÃ³digo) 2026-07-23 (orquestador `npm start F-0029`) â€” verificar toggle de Clerk Dashboard | auto |
| TS-014 | âœ… | **App no forzaba login en ningÃºn lado â€” bug de seguridad real, no cosmÃ©tico** â€” descubierto por Augusto en vivo 2026-07-23: entrando a `https://tres-saltenas-beta.vercel.app/` sin sesiÃ³n, veÃ­a el dashboard completo con datos reales del negocio. Causa raÃ­z doble (cÃ³digo leÃ­do y confirmado): `src/middleware.ts` llamaba `clerkMiddleware()` sin ningÃºn `.protect()` (wireado pero sin efecto); `src/server/api/trpc.ts` solo exponÃ­a `publicProcedure`, sin `auth()` ni chequeo de `userId` en ningÃºn router â€” el `<Show when="signed-in">` del NavBar (F-0029) era puramente cosmÃ©tico, no protegÃ­a nada. Resuelto vÃ­a orquestador (`F-0030`, 9/9 steps, 2 escalaciones a Opus-fixer en steps 5/6 por tests viejos que creaban el caller sin `userId` â€” resueltas automÃ¡tico, sin quedar bloqueado). Fix: middleware ahora usa `createRouteMatcher(["/sign-in(.*)", "/sign-up(.*)"])` + `auth.protect()` para todo lo demÃ¡s (sintaxis verificada contra doc oficial de Clerk v7, no asumida); `trpc.ts` agrega `protectedProcedure` (chequea `ctx.userId`, tira `TRPCError UNAUTHORIZED`) y todos los routers de negocio migrados de `publicProcedure` a `protectedProcedure`; test nuevo (`auth.test.ts`) cubre el rechazo sin sesiÃ³n. Verificado en vivo (Cowork, browser real): navegar a `/` sin sesiÃ³n ahora redirige automÃ¡tico a sign-in (`fresh-finch-16.accounts.dev/sign-in?redirect_url=...`), cero datos visibles. Commit merge `9f2e76c7`, deploy `dpl_8SSN9eS4rRYUXtbAbdtEQCgpBDRi` verificado READY en producciÃ³n (Vercel API). 4 ADRs (0086-0089) marcados "supuesto del agente" para revisiÃ³n â€” todos de detalle de testing (cÃ³mo inyectar `userId` en los callers de test), sin implicancia de seguridad, revisados sin objeciÃ³n | done 2026-07-23 (orquestador `npm start F-0030`, verificado Cowork en vivo) | auto |
| TS-005 | 2 | MÃ³dulo Compras â€” F-0013. Steps 1-6 done, mergeado y pusheado a `main` en GitHub 2026-07-09 (verificaciones locales limpias). **CorrecciÃ³n:** el log de release dice "Vercel deploya prod automÃ¡ticamente" pero es texto genÃ©rico â€” no hay proyecto Vercel para tres-saltenas (confirmado vÃ­a API: solo existen kredy-ap/augusto-os/spensiv-tracker/portfolio-tracker-argos2026). No hay nada en vivo todavÃ­a, solo el push a GitHub. Steps 4 y 6 se trabaron en rechazos repetidos del reviewer sin que el builder corrigiera el bug real â€” intervenciÃ³n manual (Cowork): fecha local en `compras/page.tsx`, `calcularNuevoStock` eliminado + `compras.test.ts` reescrito contra el router real vÃ­a `createCallerFactory` | done 2026-07-09 (cÃ³digo en main, sin deploy) | cc |
| TS-011 | âœ… | **Clerk real en Vercel** â€” errores de `/middleware` ("Missing publishableKey" / "Publishable key not valid", 2026-07-11/12) confirmados resueltos: `get_runtime_errors` (Vercel API, 2026-07-23) sin errores en los Ãºltimos 7 dÃ­as, y `get_runtime_logs` muestra pedidos reales exitosos (200) contra el deploy actual (`dpl_FG3zqqvCoT8MgASAGXcPnVExFvqK`, commit `f9752b1`) â€” `GET /`, `GET /gastos`, `dashboard.resumenPorPeriodo`, `gasto.list` â€” solo posibles si el login de Clerk ya funciona. MVP completo de tres-saltenas (TS-005 a TS-010) usable en producciÃ³n | done (confirmado por Vercel API, Cowork 2026-07-23) | manual |
| TS-006 | âœ… | MÃ³dulo Insumos/Stock â€” F-0014, mergeado (`80236ff`) | done (loop, hallado ya mergeado â€” reconciliado 2026-07-16, Cowork) | cc |
| TS-007 | âœ… | MÃ³dulo ProducciÃ³n por lote â€” F-0015, mergeado (`6f4a980`) | done (loop, hallado ya mergeado â€” reconciliado 2026-07-16, Cowork) | cc |
| TS-008 | âœ… | MÃ³dulo Ventas â€” F-0016, mergeado (`04631fc`) | done (loop, hallado ya mergeado â€” reconciliado 2026-07-16, Cowork) | cc |
| TS-009 | âœ… | Dashboard de costeo â€” F-0017, mergeado (`b7d60af`) | done (loop, hallado ya mergeado â€” reconciliado 2026-07-16, Cowork) | cc |
| TS-010 | âœ… | Gastos operativos + Retiros + saldo del negocio â€” F-0018, mergeado (`8abff94`) + fix post-merge `f9752b1` (prisma generate en postinstall, arreglaba el build roto de Vercel) | done (loop, hallado ya mergeado â€” reconciliado 2026-07-16, Cowork). MVP completo de tres-saltenas (TS-005 a TS-010) en producciÃ³n: deploy `dpl_FG3zqqvCoT8MgASAGXcPnVExFvqK` verificado READY (Vercel API, commit `f9752b1` = HEAD de main) | cc |
| TS-015 | âœ… | **Texto casi invisible en todos los inputs numÃ©ricos/texto de los formularios (Ventas, Compras, ProducciÃ³n, Insumos, Gastos, Retiros)** â€” auditorÃ­a UX en vivo (Cowork, browser real, viewport ~492px) confirmÃ³ vÃ­a `getComputedStyle` que el texto tipeado en `<input>` usa `color: rgb(237,237,237)` sobre fondo de card `rgb(255,255,255)` (contraste ~1.2:1, mÃ­nimo WCAG es 4.5:1). Dani no puede ver con confianza lo que tipeÃ³ en ningÃºn campo â€” solo se distingue el placeholder gris del valor real por posiciÃ³n, no por color. Riesgo directo de carga de datos mal verificada | done 2026-07-24 (F-0033, orquestador, liberado a prod) | cc |
| TS-016 | âœ… | **Bug de duplicaciÃ³n de valor en input numÃ©rico â€” genera datos corruptos reales en producciÃ³n** â€” en `/ventas`, cargando Cantidad vendida=5 y Precio unitario=1500 y tocando "Guardar venta", se persistiÃ³ (reproducido una vez, de forma intermitente, con la secuencia normal clickâ†’tipearâ†’clickâ†’tipearâ†’guardar) una venta real con precio duplicado: "Empanada JyQ, 5 u. x $15001500.00 = $75007500.00", visible en Historial de ventas y arrastrado al saldo de `/mi-plata` (Ingresos acumulados $75.007.500). Rompe directamente el cÃ¡lculo de costeo/margen si Dani no nota el valor mal escrito. **Registro de prueba ya borrado de la base (Cowork, 2026-07-24) â€” ver TS-023 para el detalle de dÃ³nde estaba realmente la base de prod** | done 2026-07-24 (F-0033, orquestador â€” mitigado vÃ­a confirmaciÃ³n de valores atÃ­picos, no se persiguiÃ³ el bug puntual, ver spec) | cc |
| TS-017 | âœ… | **Retiros sin validaciÃ³n de monto contra saldo disponible** â€” en `/retiros`, se cargÃ³ un retiro de $999.999.999 y se guardÃ³ sin ninguna advertencia (a diferencia de `/produccion`, que sÃ­ valida contra stock disponible y muestra "Stock insuficiente" con el detalle). El saldo en `/mi-plata` pasÃ³ a mostrar "-$924.992.499" reciÃ©n despuÃ©s de guardado, sin aviso alguno en el momento de la carga. Un error de tipeo (un cero de mÃ¡s) en el monto rompe el cÃ¡lculo de caja sin que Dani se entere hasta despuÃ©s. **Registro de prueba ya borrado de la base (Cowork, 2026-07-24)** | done 2026-07-27 (F-0034, orquestador, mergeado `5d62441`) â€” verificado 2026-08-26 contra git| cc |
| TS-018 | âœ… | **No hay forma de editar ni borrar una carga ya guardada** â€” confirmado en `/ventas`: el Historial de ventas es de solo lectura (sin botones de editar/borrar en el DOM, verificado con `read_page`). Combinado con TS-015/TS-016 (texto invisible + duplicaciÃ³n de valores), un error de carga queda para siempre sin que Dani pueda corregirlo desde el celular â€” requiere intervenciÃ³n manual en la base. No se relevÃ³ individualmente en Compras/ProducciÃ³n/Gastos/Retiros pero el patrÃ³n de UI (mismas cards de "Historial") sugiere que aplica igual en todas | done 2026-07-27 (F-0034, orquestador, mergeado `5d62441`) â€” verificado 2026-08-26 contra git| cc |
| TS-019 | âœ… | **Formularios sin feedback visible cuando falla la validaciÃ³n** â€” en `/ventas`, tocar "Guardar venta" con Cantidad vendida negativa o con campos vacÃ­os no muestra ningÃºn mensaje de error, borde rojo ni toast; la Ãºnica seÃ±al es que el formulario no se limpia y el historial no cambia, algo que un usuario no tÃ©cnico no va a notar ni entender como "no se guardÃ³". Mismo patrÃ³n en `/produccion`: con "Stock insuficiente" visible, el botÃ³n "Guardar lote" se ve igual de habilitado (azul, sin `disabled` visual) pero no hace nada al tocarlo | done 2026-07-24 (F-0033, orquestador, liberado a prod) | cc |
| TS-020 | âœ… | **No hay forma de agregar un insumo o producto nuevo desde la UI** â€” `/insumos` solo permite editar el campo "MÃ­nimo" de los insumos que ya existen (Paleta, Queso mozzarella, Tapa de empanada); no hay botÃ³n "agregar insumo nuevo". El selector de producto en `/produccion` y el de insumo en `/compras` estÃ¡n fijos a las opciones ya seedeadas en la base, sin opciÃ³n "otro" o "agregar". Si Dani necesita sumar un ingrediente o una variedad de empanada nueva, queda bloqueada sin intervenciÃ³n de un dev | done 2026-07-27 (F-0035, orquestador, liberado a prod) | cc |
| TS-021 | âœ… | **`/recetas` es un dead-end que expone jerga interna a Dani** â€” el tab "Recetas" estÃ¡ en la nav principal (visible y clickeable) pero al entrar muestra literalmente "MÃ³dulo pendiente â€” ver features F-0013..F-0018 en augusto-os". Son referencias a IDs de features y al repo interno del proyecto, sin sentido para una usuaria no tÃ©cnica, que puede interpretar el mensaje como que la app estÃ¡ rota en vez de que la secciÃ³n todavÃ­a no existe | done 2026-07-27 (F-0036, orquestador, liberado a prod) | cc |
| TS-022 | âœ… | **Nav principal sin indicio visual de que hay mÃ¡s pestaÃ±as fuera de pantalla** â€” en viewport angosto (~375-492px) solo se ven ~5 de las 9 pestaÃ±as (Dashboard/Ventas/Compras/ProducciÃ³n/Insumos visibles; Gastos/Retiros/Mi Plata/Recetas quedan fuera a la derecha). La Ãºnica seÃ±al de que el nav es scrolleable es una barra de scroll gris fina sin flecha ni gradiente, que en Safari/Chrome mobile real no se muestra de forma persistente. Dani puede no descubrir Gastos, Retiros, Mi Plata o Recetas si nadie le explica que hay que deslizar el nav con el dedo | done 2026-07-27 (F-0036, orquestador â€” gradiente siempre visible, ADR-0101 marcado para revisar) | cc |
| TS-023 | âœ… | **Vercel prod pegaba contra el Neon "dev" del loop, no contra el "prod" real â€” el sandbox automÃ¡tico tenÃ­a member para escribir sobre los datos de Dani** â€” descubierto en vivo (Cowork, 2026-07-24) al ir a limpiar 2 registros de prueba de la auditorÃ­a UX (TS-016/TS-017): `console.neon.tech` mostrÃ³ que el proyecto "tres-saltenas" (host `ep-tiny-voice-atetw10j`, el que `targets.json` marcaba como prod) tiene 0 tablas â€” nunca se usÃ³. Los datos reales de Dani (Venta/Compra/Retiro/Insumo/etc.) estaban en el proyecto "tres-saltenas-dev" (host `ep-frosty-wildflower-atkvtbza`), que ademÃ¡s es exactamente el host apuntado por `TRES_SALTENAS_DEV_DATABASE_URL` en `orchestrator/.env` â€” es decir, el sandbox que el loop automÃ¡tico (autopilot, tests, migraciones de `npm start`) trata como descartable estaba en verdad conteniendo la base real del negocio. Mismo tipo de riesgo que S-000 (aislamiento de prod) fue creado para prevenir, pero para este target especÃ­fico el aislamiento apuntaba al revÃ©s. **DecisiÃ³n de Augusto: no mantener un ambiente separado â€” el Neon con datos reales pasa a ser el Ãºnico prod oficial.** Resuelto por swap de nombres en Neon (sin migrar un solo dato): proyecto con datos reales (`ep-frosty-wildflower-atkvtbza`) renombrado `tres-saltenas-dev` â†’ `tres-saltenas` (prod); proyecto vacÃ­o (`ep-tiny-voice-atetw10j`) renombrado `tres-saltenas` â†’ `tres-saltenas-dev` (sandbox nuevo del loop, todavÃ­a sin schema materializado). Vercel no requiere cambios (la connection string real no cambiÃ³, solo la etiqueta en Neon). Actualizado: `targets/targets.json` (`prodDbPatterns` â†’ `ep-frosty-wildflower-atkvtbza*`), `orchestrator/.env` (`TRES_SALTENAS_DEV_DATABASE_URL` â†’ apunta ahora al sandbox vacÃ­o real), `tres-saltenas/.env` local (`DATABASE_URL`/`DIRECT_URL` â†’ sandbox vacÃ­o, para que `npm run dev` local tampoco toque datos reales). **Pendiente antes de correr cualquier feature de tres-saltenas por el loop:** `npx prisma db push` contra el nuevo sandbox (`ep-tiny-voice-atetw10j`) para materializar el schema â€” hoy estÃ¡ en blanco, `npm start` fallarÃ­a contra Ã©l tal cual | done 2026-07-24 (Cowork, aprobado por Augusto en el momento) | cc |
